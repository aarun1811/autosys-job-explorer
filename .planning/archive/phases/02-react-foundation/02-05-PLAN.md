---
phase: 02-react-foundation
plan: 05
type: execute
wave: 3
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/parity-matrix.md
  - frontend-react/README.md
autonomous: true
requirements:
  - REACT-01
  - REACT-02
  - REACT-03
  - REACT-04
  - REACT-05
  - REACT-06
  - REACT-07
  - REACT-08

must_haves:
  truths:
    - "ROADMAP.md Phase 2 SC#5 no longer says 'angular'; it says 'backend, tlm-stats, and React'"
    - "ROADMAP.md Phase 3 SC#1 no longer mentions '/v6/' prefix"
    - "REQUIREMENTS.md REACT-08 no longer says 'angular components registered'"
    - "REQUIREMENTS.md SEARCH-07 no longer has '(e.g. /v6/)' example"
    - "STATE.md Deferred Items table has the chart/series/ramp tokens row"
    - "parity-matrix.md has a note at the top confirming D-2.18 gate relaxation"
    - "frontend-react/README.md has pnpm quickstart and npm fallback instructions"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated ROADMAP with Phase 2 plan count and corrected SC#5 and Phase 3 SC#1"
      contains: "5 plans"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated REQUIREMENTS without /v6/ and without angular in REACT-08"
      contains: "REACT-08"
    - path: ".planning/STATE.md"
      provides: "Updated deferred items row for chart tokens"
      contains: "chart/series/ramp tokens"
    - path: "frontend-react/README.md"
      provides: "Developer quickstart"
      contains: "corepack enable"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "D-2.5 + D-2.17"
      via: "Doc supersessions locked in CONTEXT.md"
      pattern: "D-2.5"
    - from: ".planning/STATE.md"
      to: "frontend-react/src/index.css"
      via: "Deferred items row auto-surfaces the chart token requirement"
      pattern: "chart/series/ramp"
---

<objective>
Apply the documentation supersessions from D-2.5 and D-2.17, update ROADMAP.md to reflect the final plan count (5 plans), add the chart-token deferred-items row to STATE.md (D-2.7), add a D-2.18 gate-relaxation note to parity-matrix.md, and write `frontend-react/README.md` with pnpm quickstart and npm fallback instructions.

Purpose: These edits are housekeeping with locked-decision backing. D-2.5 supersedes the `/v6/` example URL. D-2.17 supersedes the angular row in REACT-08. D-2.7 requires STATE.md to carry the chart-token deferred row.

Output: Clean planning documents with no stale references; README for onboarding new developers to the React scaffold.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-react-foundation/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ROADMAP.md and REQUIREMENTS.md supersession edits (D-2.5, D-2.17)</name>
  <files>
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </files>

  <read_first>
    1. .planning/ROADMAP.md — read Phase 2 section (Plans count, SC#5) and Phase 3 section (SC#1 which contains "/v6/" mention)
    2. .planning/REQUIREMENTS.md — read REACT-08 line (contains "angular") and SEARCH-07 line (contains "/v6/")
    3. .planning/phases/02-react-foundation/02-CONTEXT.md — D-2.5 (SEARCH-07 edit) and D-2.17 (REACT-08 edit) for exact wording changes
  </read_first>

  <action>
    **ROADMAP.md edits** — Three targeted changes:

    1. Phase 2 `**Plans:**` line: Change from `**Plans**: TBD` to `**Plans**: 5 plans`

    2. Phase 2 Plans list: Add after the Plans count line:
    ```
    Plans:
    - [ ] 02-01-PLAN.md — Frontend scaffold: Vite 7 + React 19 + shadcn + tokens + ESLint hex rule + vitest
    - [ ] 02-02-PLAN.md — Backend tracing: micrometer-tracing-bridge-brave + logback-spring.xml + X-Correlation-Id baggage
    - [ ] 02-03-PLAN.md — App shell: ThemeProvider + ThemeSwitch + footer SHA + QueryClient + SmokeGrid SSRM
    - [ ] 02-04-PLAN.md — Ops scripts: rectrace-ops.sh v1 + build.sh react + smoke-ssrm + smoke-correlation-id
    - [ ] 02-05-PLAN.md — Doc supersessions: ROADMAP/REQUIREMENTS/STATE edits + frontend-react README
    ```

    3. Phase 2 **Success Criteria** SC#5: Change "registers backend, tlm-stats, angular, and React components and can start/stop/status each one" → "registers backend, tlm-stats, and React components and can start/stop/status each one" (per D-2.17; angular removed)

    4. Phase 3 **Success Criteria** SC#1: The line currently reads "A user can open the React app at `/v6/` (or chosen prefix)..." — change to "A user can open the React app at `/rectrace/` (the production base path; no `/v6/` prefix — see Phase 2 D-2.4), run a search in the ported category, and see results in the SSRM grid identical to the Angular app's output." (per D-2.5)

    **REQUIREMENTS.md edits** — Two targeted changes:

    1. REACT-08 line: Change "backend, tlm-stats, angular components registered; React added once `npm run dev` boots" → "backend, tlm-stats, and React components registered; no angular component (decommissioned at React go-live per D-2.15 / D-2.17)" (per D-2.17)

    2. SEARCH-07 line: Change "React app served behind a distinct URL prefix (e.g. `/v6/`) so Angular and React run side-by-side until the parity matrix says shutdown" → "React app served at `/rectrace/` (no `/v6/` prefix — D-2.4; Angular is decommissioned at React go-live); React and Angular run side-by-side during development only (manual `npm start` in `frontend/rectrace/`)" (per D-2.5)
  </action>

  <verify>
    <automated>
      grep -c "5 plans" /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/ROADMAP.md
      grep -v "angular" /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/ROADMAP.md | grep -c "SC#5\|tlm-stats.*React" || true
      grep -c "no.*v6" /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/ROADMAP.md
      grep -c "no angular component" /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/REQUIREMENTS.md
      grep -c "no.*v6.*prefix" /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/REQUIREMENTS.md
    </automated>
  </verify>

  <done>
    - ROADMAP.md Phase 2 shows `**Plans**: 5 plans` and the 5-item plan list
    - ROADMAP.md Phase 2 SC#5 does not contain "angular"
    - ROADMAP.md Phase 3 SC#1 does not contain "/v6/"
    - REQUIREMENTS.md REACT-08 does not contain "angular components registered" (contains "no angular component")
    - REQUIREMENTS.md SEARCH-07 does not contain "(e.g. `/v6/`)"
  </done>
</task>

<task type="auto">
  <name>Task 2: STATE.md deferred items, parity-matrix.md gate note, and frontend-react/README.md</name>
  <files>
    .planning/STATE.md,
    .planning/parity-matrix.md,
    frontend-react/README.md
  </files>

  <read_first>
    1. .planning/STATE.md — read Deferred Items table to find correct insertion point for the chart-token row
    2. .planning/parity-matrix.md — read the gate section at the top to add D-2.18 note
    3. .planning/phases/02-react-foundation/02-CONTEXT.md — D-2.7 (chart token auto-surface mechanism: STATE.md row), D-2.18 (gate relaxation note)
  </read_first>

  <action>
    **STATE.md** — Add a row to the "Deferred Items" table for the chart/series/ramp tokens per D-2.7:

    Insert after the existing "Docs hygiene" row (or at the end of the table if it is the last row):
    ```
    | Design tokens | Add chart/series/ramp tokens to `frontend-react/src/index.css` "Rectrace extensions" overlay block — surfaces when first chart/data-viz component is planned. Reference recviz `src/index.css` `--series-1..8`, `--ramp-low/high`, `--chart-positive/negative/warning`. Phase 8 DESIGN-01 audits the lot. See also: `frontend-react/src/index.css` RECTRACE EXTENSIONS comment block. | Open | Phase 2 plan (2026-05-13) |
    ```

    Also update the `stopped_at` field in the frontmatter to `Phase 2 React Foundation plan complete` and update `last_updated` to today's date.

    **parity-matrix.md** — Read the file and add a gate-relaxation note at the top of the document (after any frontmatter or existing heading). Insert:
    ```
    > **Phase 2 Foundation Gate Note (D-2.18):** The strict FOUND-04 gate ("Phase 2 can begin once every row has a non-`tbd` value in Target") is relaxed for Phase 2 Foundation. Phase 2 ships only scaffolding (empty SSRM smoke grid) — no cell renderers, modals, or search tabs are ported. Target values are locked **per-port-phase**: Phase 3 locks Targets for search tab + renderers it ports; Phase 4 locks Targets for the modals it owns.
    ```

    **frontend-react/README.md** — Write a developer quickstart document. Content must include:
    - Project title: "Rectrace — React Frontend"
    - Brief description: "Net-new React SPA for Rectrace, built with Vite 7 + React 19 + shadcn (Tailwind v4) + AG-Grid Enterprise 35. Runs side-by-side with the existing Angular app during development; replaces Angular in production."
    - Prerequisites section: Node ≥ 20.19.0, pnpm 9 via Corepack (preferred), or npm (fallback)
    - Quickstart (pnpm — preferred):
      ```
      corepack enable
      corepack prepare pnpm@9.15.0 --activate
      pnpm install
      cp .env.local.example .env.local
      # Set VITE_AG_GRID_LICENSE_KEY in .env.local (copy from frontend/rectrace/src/environments/environment.ts)
      pnpm dev
      # App at http://localhost:5173/
      ```
    - Quickstart (npm fallback — for developers without pnpm):
      ```
      npm install
      cp .env.local.example .env.local
      # Set VITE_AG_GRID_LICENSE_KEY in .env.local
      npm run dev
      # App at http://localhost:5173/
      ```
    - Available scripts table: `pnpm dev`, `pnpm build`, `pnpm preview`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:watch`
    - Ops script reference: "To start all services: `bash ops/rectrace-ops.sh start all` (from repo root)"
    - Build for production: "`bash ops/build.sh react` builds and deploys to `backend/rectrace/src/main/resources/static/`"
    - Backend connection: "Dev server proxies `/rectrace/api` → `http://localhost:6088`; start backend first: `bash ops/rectrace-ops.sh start backend`"
    - Design tokens note: "Design tokens live in `src/index.css` (OKLCH mist palette). The `RECTRACE EXTENSIONS` block is intentionally empty — see STATE.md Deferred Items for auto-surface mechanism."
  </action>

  <verify>
    <automated>
      grep -c "chart/series/ramp tokens" /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/STATE.md
      grep -c "D-2.18" /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/parity-matrix.md
      grep -c "corepack enable" /Users/aarun/Workspace/Projects/autosys-job-explorer/frontend-react/README.md
      grep -c "pnpm@9.15.0" /Users/aarun/Workspace/Projects/autosys-job-explorer/frontend-react/README.md
    </automated>
  </verify>

  <done>
    - STATE.md Deferred Items table has "Add chart/series/ramp tokens" row with "Open" status (D-2.7 auto-surface mechanism)
    - parity-matrix.md has D-2.18 gate-relaxation note at the top
    - frontend-react/README.md has pnpm-first quickstart and npm fallback; `corepack enable` step documented
    - README references `ops/rectrace-ops.sh` and `ops/build.sh react`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation files | No trust boundaries — these are plain-text edits to planning docs and a README |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Information Disclosure | AG-Grid license key | N/A — README.md documents that the license key comes from .env.local (gitignored); real value never committed | README explicitly says "Do NOT commit the real value" |
| T-2-02 | Injection | X-Correlation-Id | N/A — out of scope for Plan 05 | Documentation plan only |
| T-2-03 | Information Disclosure | CORS | N/A | Documentation plan only |
| T-2-04 | Elevation of Privilege | Dev proxy | N/A | Documentation plan only |
| T-2-05 | Tampering | build.sh rm -rf | N/A — out of scope for Plan 05 | Mitigated in Plan 04 |
</threat_model>

<verification>
End-of-plan verification:
1. `grep -c "5 plans" .planning/ROADMAP.md` → 1
2. `grep -c "chart/series/ramp tokens" .planning/STATE.md` → 1
3. `grep -c "D-2.18" .planning/parity-matrix.md` → 1
4. `grep -c "corepack enable" frontend-react/README.md` → 1
5. `grep "angular" .planning/ROADMAP.md | grep -i "sc#5"` → should show the updated SC#5 without "angular" (or 0 matches if line no longer contains both)
6. `grep -c "/v6/" .planning/ROADMAP.md` → 0 (all /v6/ references removed)
7. `grep -c "/v6/" .planning/REQUIREMENTS.md` → 0
</verification>

<success_criteria>
- ROADMAP.md Phase 2 has correct plan count (5) and plan list; SC#5 says "backend, tlm-stats, and React" (no angular)
- ROADMAP.md Phase 3 SC#1 says "/rectrace/" (not "/v6/")
- REQUIREMENTS.md REACT-08 does not mention "angular"; SEARCH-07 does not have "(e.g. `/v6/`)"
- STATE.md Deferred Items has chart-token row (D-2.7 auto-surface mechanism satisfied)
- parity-matrix.md has D-2.18 gate note
- frontend-react/README.md documents pnpm-first quickstart with Corepack and npm fallback
- No new /v6/ references introduced anywhere in the edits
</success_criteria>

<output>
After completion, create `.planning/phases/02-react-foundation/02-05-SUMMARY.md` with:
- Files modified (list with change summary)
- Supersession confirmations: D-2.5 (SEARCH-07 + ROADMAP Ph3 SC#1 /v6/ removed), D-2.17 (REACT-08 + ROADMAP Ph2 SC#5 angular removed)
- D-2.7 confirmation: STATE.md deferred row added
- D-2.18 confirmation: parity-matrix.md gate note added
- README completeness note
</output>
