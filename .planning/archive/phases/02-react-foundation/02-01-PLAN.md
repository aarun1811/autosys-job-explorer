---
phase: 02-react-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-react/package.json
  - frontend-react/pnpm-lock.yaml
  - frontend-react/vite.config.ts
  - frontend-react/tsconfig.json
  - frontend-react/tsconfig.app.json
  - frontend-react/tsconfig.node.json
  - frontend-react/eslint.config.js
  - frontend-react/components.json
  - frontend-react/vitest.config.ts
  - frontend-react/index.html
  - frontend-react/.gitignore
  - frontend-react/.env.local.example
  - frontend-react/src/vite-env.d.ts
  - frontend-react/src/main.tsx
  - frontend-react/src/App.tsx
  - frontend-react/src/index.css
  - frontend-react/src/lib/utils.ts
  - frontend-react/src/lib/theme.ts
  - frontend-react/src/test-setup.ts
  - frontend-react/tests/fixtures/raw-hex.tsx
autonomous: true
requirements:
  - REACT-01
  - REACT-04

must_haves:
  truths:
    - "pnpm dev starts at localhost:5173 and the browser receives an HTML document"
    - "ESLint reports an error on the raw-hex.tsx fixture (no max-warnings=0 pass)"
    - "tokens.css contains the full mist OKLCH :root block and the empty RECTRACE EXTENSIONS marker (no chart-1..5 tokens)"
    - "theme.ts exports a const tokens object mapping all CSS variable names"
    - "vitest runs and exits 0 on an empty test suite"
    - "package.json declares packageManager pnpm@9.15.0"
  artifacts:
    - path: "frontend-react/package.json"
      provides: "dependency manifest"
      contains: "\"packageManager\": \"pnpm@9"
    - path: "frontend-react/vite.config.ts"
      provides: "build tool config with mode-aware base and BUILD_SHA"
      contains: "__BUILD_SHA__"
    - path: "frontend-react/components.json"
      provides: "shadcn config"
      contains: "\"baseColor\": \"mist\""
    - path: "frontend-react/src/index.css"
      provides: "Tailwind v4 + mist tokens + empty extensions block (no chart tokens)"
      contains: "RECTRACE EXTENSIONS"
    - path: "frontend-react/src/lib/theme.ts"
      provides: "TS token mirror"
      exports: ["tokens", "TokenKey"]
    - path: "frontend-react/src/test-setup.ts"
      provides: "vitest DOM matcher setup"
      contains: "@testing-library/jest-dom"
    - path: "frontend-react/tests/fixtures/raw-hex.tsx"
      provides: "ESLint hex-rejection test fixture"
      contains: "#"
  key_links:
    - from: "frontend-react/vite.config.ts"
      to: "frontend-react/src/vite-env.d.ts"
      via: "define.__BUILD_SHA__ consumed as ambient declare const"
      pattern: "__BUILD_SHA__"
    - from: "frontend-react/eslint.config.js"
      to: "frontend-react/tests/fixtures/raw-hex.tsx"
      via: "Literal[value=/#/] no-restricted-syntax rule"
      pattern: "no-restricted-syntax"
    - from: "frontend-react/vitest.config.ts"
      to: "frontend-react/src/test-setup.ts"
      via: "setupFiles: ['./src/test-setup.ts'] loads jest-dom matchers for all tests"
      pattern: "test-setup"
---

<objective>
Scaffold the `frontend-react/` directory as the net-new React SPA. Produces a bootable Vite 7 + React 19 + TypeScript + shadcn (Tailwind v4) shell with the canonical design-token file (mist baseline only — no chart tokens per D-2.7), TS token mirror, ESLint flat config with hex-rejection, vitest harness, test-setup.ts DOM matcher bootstrap, and the raw-hex fixture file that proves the ESLint rule is enforced.

Purpose: Establishes the project skeleton that Plans 02–04 build on. Every subsequent React phase reads patterns from this scaffold.

Output: `frontend-react/` directory committed with all config files, src/index.css (mist tokens, no chart-1..5), src/lib/theme.ts, eslint.config.js (hex rule), vitest.config.ts, src/test-setup.ts, and the raw-hex ESLint fixture.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-react-foundation/02-CONTEXT.md
@.planning/phases/02-react-foundation/02-RESEARCH.md
@.planning/phases/02-react-foundation/02-PATTERNS.md
@.planning/phases/02-react-foundation/02-UI-SPEC.md

<interfaces>
<!-- SSRM contract — executor does not need these in Plan 01 but they inform index.css AG-Grid token bridge -->
<!-- AG-Grid theme bridge variables (from PATTERNS.md): -->
<!-- .ag-theme-quartz { --ag-background-color: var(--background); --ag-row-height: 36px; --ag-header-height: 40px; ... } -->

<!-- Recviz analog files (read before implementing): -->
<!-- /Users/aarun/Workspace/Projects/recviz/frontend/package.json -->
<!-- /Users/aarun/Workspace/Projects/recviz/frontend/vite.config.ts (lines 1-14) -->
<!-- /Users/aarun/Workspace/Projects/recviz/frontend/eslint.config.js (lines 1-23) -->
<!-- /Users/aarun/Workspace/Projects/recviz/frontend/components.json (lines 1-24) -->
<!-- /Users/aarun/Workspace/Projects/recviz/frontend/src/index.css (full file) -->
<!-- /Users/aarun/Workspace/Projects/recviz/frontend/src/lib/utils.ts (lines 1-6) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold frontend-react/ project files (package.json, configs, CSS tokens, test-setup)</name>
  <files>
    frontend-react/package.json,
    frontend-react/vite.config.ts,
    frontend-react/tsconfig.json,
    frontend-react/tsconfig.app.json,
    frontend-react/tsconfig.node.json,
    frontend-react/components.json,
    frontend-react/vitest.config.ts,
    frontend-react/index.html,
    frontend-react/.gitignore,
    frontend-react/.env.local.example,
    frontend-react/src/vite-env.d.ts,
    frontend-react/src/index.css,
    frontend-react/src/lib/utils.ts,
    frontend-react/src/lib/theme.ts,
    frontend-react/src/App.tsx,
    frontend-react/src/main.tsx,
    frontend-react/src/test-setup.ts
  </files>

  <read_first>
    1. /Users/aarun/Workspace/Projects/recviz/frontend/package.json — dependency list to mirror; apply deltas documented in PATTERNS.md "package.json" section
    2. /Users/aarun/Workspace/Projects/recviz/frontend/vite.config.ts (lines 1-14) — base plugin setup; wrap in function form and add base/define/server.proxy
    3. /Users/aarun/Workspace/Projects/recviz/frontend/tsconfig.json — copy verbatim; drop tsconfig.e2e.json reference
    4. /Users/aarun/Workspace/Projects/recviz/frontend/tsconfig.app.json — copy verbatim
    5. /Users/aarun/Workspace/Projects/recviz/frontend/tsconfig.node.json — copy verbatim; change include to ["vite.config.ts","vitest.config.ts"]
    6. /Users/aarun/Workspace/Projects/recviz/frontend/components.json — copy verbatim; "config": "" is correct Tailwind v4 marker
    7. /Users/aarun/Workspace/Projects/recviz/frontend/src/index.css — full file read; verbatim :root OKLCH tokens EXCEPT strip chart-1..5 tokens (D-2.7); strip series/ramp/chart-positive tokens; add RECTRACE EXTENSIONS empty block
    8. /Users/aarun/Workspace/Projects/recviz/frontend/src/lib/utils.ts (lines 1-6) — copy verbatim
    9. .planning/phases/02-react-foundation/02-RESEARCH.md (offset 200, limit 300) — Pattern 1 (vite config), Pattern 2 (components.json); NOTE: Pattern 3 (tokens.css) includes chart-1..5 OKLCH values — these are SUPERSEDED by D-2.7. Do not copy chart-1..5 from Pattern 3.
    10. .planning/phases/02-react-foundation/02-PATTERNS.md — "package.json" section for exact dep deltas; "tsconfig" section; "vitest.config.ts" section; "src/lib/theme.ts" section
  </read_first>

  <behavior>
    - Test: `pnpm --dir frontend-react test --run` exits 0 (no test files = pass; vitest must not error on missing setup)
    - Test: `grep -c "packageManager" frontend-react/package.json` returns 1
    - Test: `grep -c "RECTRACE EXTENSIONS" frontend-react/src/index.css` returns 1
    - Test: `grep -c "background:" frontend-react/src/lib/theme.ts` returns at least 1 (confirms token map)
    - Test: `grep -c "chart-1" frontend-react/src/index.css` returns 0 (no chart tokens — D-2.7)
  </behavior>

  <action>
    Create `frontend-react/` at repo root (sibling to `frontend/rectrace/`). Do NOT use `pnpm create vite` interactively — write all files directly.

    **package.json** — Use the PATTERNS.md "package.json" spec verbatim:
    - `"name": "rectrace-frontend"`, `"version": "0.0.0"`, `"type": "module"`, `"private": true`
    - `"packageManager": "pnpm@9.15.0"` (per D-2.2 — Corepack version pin)
    - `"engines": { "node": "^20.19.0 || >=22.12.0" }` (Vite 7 Node floor)
    - Scripts: `"dev": "vite"`, `"build": "tsc -b && vite build"`, `"lint": "eslint ."`, `"preview": "vite preview"`, `"typecheck": "tsc -b --noEmit"`, `"test": "vitest run"`, `"test:watch": "vitest"`
    - Dependencies (exact versions from PATTERNS.md "Recviz dependency list"):
      - `react: ^19.2.0`, `react-dom: ^19.2.0`, `@tanstack/react-router: ^1.159.5`, `@tanstack/react-query: ^5.90.20`, `zustand: ^5.0.11`, `ag-grid-community: ^35.0.1`, `ag-grid-enterprise: ^35.0.1`, `ag-grid-react: ^35.0.1`, `next-themes: ^0.4.6`, `lucide-react: ^0.563.0`, `sonner: ^2.0.7`, `class-variance-authority: ^0.7.1`, `clsx: ^2.1.1`, `tailwind-merge: ^3.4.0`, `tw-animate-css: ^1.4.0`, `react-hook-form: ^7.75.0`, `zod: ^3.24.2`, `@radix-ui/react-slot: ^1.2.4`
      - CRITICAL version pins (do not upgrade): `vite: ^7.3.1`, `shadcn: ^3.8.4`, `typescript: ~5.9.3`, `lucide-react: ^0.563.0`
    - devDependencies: `@eslint/js: ^9.39.1`, `eslint: ^9.39.1`, `eslint-plugin-react-hooks: ^7.0.1`, `eslint-plugin-react-refresh: ^0.4.24`, `typescript-eslint: ^8.48.0`, `globals: ^16.5.0`, `@types/react: ^19.2.7`, `@types/react-dom: ^19.2.3`, `@types/node: ^24.10.1`, `vitest: ^4.1.2`, `@testing-library/react: ^16.3.2`, `@testing-library/jest-dom: ^6.9.1`, `jsdom: ^29.0.1`, `@tailwindcss/vite: ^4.1.18`, `@vitejs/plugin-react: ^5.1.1`, `@tanstack/router-plugin: ^1.159.5`, `@tanstack/react-query-devtools: ^5.91.3`
    - Remove from recviz deps: `@monaco-editor/react`, `ag-charts-enterprise`, `ag-charts-react`, `cmdk`, `date-fns`, `echarts`, `echarts-for-react`, `motion`, `radix-ui`, `react-day-picker`, `react-grid-layout`, `react-resizable-panels`, `sql-formatter`

    **vite.config.ts** — Implement the PATTERNS.md function-form with all four Phase 2 deltas:
    - Import: `defineConfig` from vite, `react` from `@vitejs/plugin-react`, `tailwindcss` from `@tailwindcss/vite`, `TanStackRouterVite` from `@tanstack/router-plugin/vite`, `path` from `path`, `execSync` from `child_process`
    - `getGitSha()`: `try { return execSync('git rev-parse --short HEAD', { encoding: 'utf8' }).trim() } catch { return 'dev' }`
    - Export `defineConfig(({ mode }) => ({ ... }))` with:
      - `base: mode === 'production' ? '/rectrace/' : '/'` (D-2.4)
      - `plugins: [TanStackRouterVite({ routesDirectory: './src/routes', generatedRouteTree: './src/routeTree.gen.ts' }), react(), tailwindcss()]`
      - `resolve.alias: { '@': path.resolve(__dirname, './src') }`
      - `define: { __BUILD_SHA__: JSON.stringify(getGitSha()) }` (REACT-06)
      - `server.proxy: { '/rectrace/api': { target: 'http://localhost:6088', changeOrigin: true } }` (prod-parity dev proxy)

    NOTE on WARNING 7 (agGrid.ts): The AG-Grid `LicenseManager.setLicenseKey` call is inlined directly in `src/main.tsx` (single-call discipline). `src/lib/agGrid.ts` from PATTERNS.md File Classification is intentionally NOT created — the inline approach in main.tsx satisfies D-2.14 with less indirection.

    **tsconfig.json** — Copy verbatim from recviz (PATTERNS.md); drop `tsconfig.e2e.json` reference.

    **tsconfig.app.json** — Copy verbatim from recviz (PATTERNS.md); include all strict flags: `erasableSyntaxOnly: true`, `noUncheckedSideEffectImports: true`, `verbatimModuleSyntax: true`.

    **tsconfig.node.json** — Copy verbatim from recviz; set `"include": ["vite.config.ts", "vitest.config.ts"]` (no playwright.config.ts).

    **components.json** — Copy verbatim from recviz (PATTERNS.md). `"config": ""` is mandatory for Tailwind v4. `"baseColor": "mist"`, `"style": "new-york"`, `"cssVariables": true`, `"iconLibrary": "lucide"`.

    **vitest.config.ts** — Use PATTERNS.md vitest template: `environment: 'jsdom'` (NOT 'node' — Phase 2 tests render React DOM). Include `setupFiles: ['./src/test-setup.ts']` so `@testing-library/jest-dom` matchers load.

    **index.html** — Standard Vite entrypoint: `<!doctype html>`, `<html lang="en">`, `<title>Rectrace</title>` (per UI-SPEC copywriting), `<div id="root"></div>`, `<script type="module" src="/src/main.tsx"></script>`.

    **.gitignore** — Include: `.env.local`, `dist/`, `node_modules/`, `.vite/`, `src/routeTree.gen.ts` (TanStack Router generated file).

    **.env.local.example** — Single line: `VITE_AG_GRID_LICENSE_KEY=` with comment: `# Copy the license key from frontend/rectrace/src/environments/environment.ts (agGridLicenseKey field). Do NOT commit the real value.`

    **src/vite-env.d.ts** — Must contain BOTH lines:

        /// <reference types="vite/client" />
        declare const __BUILD_SHA__: string

    **src/index.css** — Structure must be:
    1. `@import "tailwindcss";`
    2. `@import "tw-animate-css";`
    3. `@import "shadcn/tailwind.css";` (note: shadcn package, not @shadcn)
    4. `@custom-variant dark (&:is(.dark *));`
    5. `@theme inline { ... }` — full radius scale + full color bridge using `var(--)` references ONLY (no OKLCH inside this block). Include all sidebar-* variables. End block with the RECTRACE EXTENSIONS comment block per D-2.7. Do NOT include `--color-chart-1` through `--color-chart-5` in this block.
    6. `:root { ... }` — OKLCH values from recviz src/index.css for the mist theme. Include background, foreground, card, popover, primary, secondary, muted, accent, destructive, sidebar suite, border, input, ring, radius. Do NOT include `--chart-1` through `--chart-5` OKLCH values — those are superseded by D-2.7 and will be added in the phase that introduces the first chart component. NOTE: RESEARCH.md Pattern 3 includes chart-1..5 OKLCH values — these are SUPERSEDED by D-2.7. Do not copy them.
    7. `.dark { ... }` — dark-mode OKLCH values for the same set (no chart tokens).
    8. `.ag-theme-quartz { ... }` — AG-Grid token bridge from PATTERNS.md: `--ag-background-color: var(--background)`, `--ag-foreground-color: var(--foreground)`, `--ag-header-background-color: var(--muted)`, `--ag-header-foreground-color: var(--foreground)`, `--ag-border-color: var(--border)`, `--ag-row-hover-color: var(--accent)`, `--ag-selected-row-background-color: var(--accent)`, `--ag-odd-row-background-color: transparent`, `--ag-font-family: "Inter", system-ui, sans-serif`, `--ag-font-size: 13px`, `--ag-header-font-size: 12px`, `--ag-header-font-weight: 500`, `--ag-cell-horizontal-padding: 12px`, `--ag-row-height: 36px`, `--ag-header-height: 40px`

    SELF-CONSISTENCY NOTE: Do not include `--color-chart-1..5` in the `@theme inline` block or `--chart-1..5` in the `:root`/`.dark` blocks. The RECTRACE EXTENSIONS labeled empty block (with comment pointing to recviz's `--series-*`, `--ramp-*`, `--chart-positive/negative/warning` tokens and the Phase 8 DESIGN-01 audit anchor) IS the auto-surface mechanism per D-2.7 and MUST be kept.

    **src/lib/utils.ts** — Copy verbatim from recviz (PATTERNS.md): `import { clsx, type ClassValue } from "clsx"`, `import { twMerge } from "tailwind-merge"`, `export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)) }`

    **src/lib/theme.ts** — Export `const tokens` with every CSS variable name from `index.css :root` mapped to its `var(--)` string (excluding chart-1..5 which are not in :root). End with RECTRACE EXTENSIONS comment (empty). Export `type TokenKey = keyof typeof tokens`. Example first two lines: `background: 'var(--background)'`, `foreground: 'var(--foreground)'`.

    **src/App.tsx** — Minimal RouterProvider wrapper (TanStack Router). Import `{ RouterProvider, createRouter }` from `@tanstack/react-router`, import `{ routeTree }` from `./routeTree.gen`, create `const router = createRouter({ routeTree })`, export `default function App() { return <RouterProvider router={router} /> }`. TypeScript module augmentation: `declare module '@tanstack/react-router' { interface Register { router: typeof router } }`.

    **src/main.tsx** — Implement per PATTERNS.md "src/main.tsx" section. CRITICAL ORDER (AG-Grid requirement): LicenseManager.setLicenseKey FIRST, then ModuleRegistry.registerModules, then createRoot. No chart modules. Only `ServerSideRowModelModule`. AG-Grid license key inlined here via `import.meta.env.VITE_AG_GRID_LICENSE_KEY` (satisfies D-2.14; no separate src/lib/agGrid.ts needed).

    **src/test-setup.ts** — Create with a single import:

        import '@testing-library/jest-dom'

    This file is referenced by `vitest.config.ts` `setupFiles: ['./src/test-setup.ts']` and enables DOM matchers (toBeInTheDocument, etc.) for all vitest tests that render React components. Creating it here (in Task 1) ensures vitest.config.ts can reference it immediately without a "file not found" error at test time.

    After writing all files, run: `cd /Users/aarun/Workspace/Projects/autosys-job-explorer/frontend-react && pnpm install` to generate pnpm-lock.yaml. If pnpm is not available, run `npm install` as fallback but note that pnpm-lock.yaml is preferred.
  </action>

  <verify>
    <automated>
      grep -c '"packageManager": "pnpm@9' /Users/aarun/Workspace/Projects/autosys-job-explorer/frontend-react/package.json
      grep -c '__BUILD_SHA__' /Users/aarun/Workspace/Projects/autosys-job-explorer/frontend-react/vite.config.ts
      grep -c '"baseColor": "mist"' /Users/aarun/Workspace/Projects/autosys-job-explorer/frontend-react/components.json
      grep -c 'RECTRACE EXTENSIONS' /Users/aarun/Workspace/Projects/autosys-job-explorer/frontend-react/src/index.css
      grep -c 'background:' /Users/aarun/Workspace/Projects/autosys-job-explorer/frontend-react/src/lib/theme.ts
      grep -c "declare const __BUILD_SHA__" /Users/aarun/Workspace/Projects/autosys-job-explorer/frontend-react/src/vite-env.d.ts
      grep -c "@testing-library/jest-dom" /Users/aarun/Workspace/Projects/autosys-job-explorer/frontend-react/src/test-setup.ts
      grep -c "chart-1" /Users/aarun/Workspace/Projects/autosys-job-explorer/frontend-react/src/index.css
      test -f /Users/aarun/Workspace/Projects/autosys-job-explorer/frontend-react/node_modules/.package-lock.json || test -f /Users/aarun/Workspace/Projects/autosys-job-explorer/frontend-react/pnpm-lock.yaml
    </automated>
  </verify>

  <done>
    - frontend-react/package.json declares `"packageManager": "pnpm@9.15.0"` and all required dependencies at pinned versions (vite ^7.3.1 NOT 8.x; shadcn ^3.8.4 NOT 4.x; typescript ~5.9.3 NOT 6.x; lucide-react ^0.563.0 NOT 1.x)
    - vite.config.ts contains `__BUILD_SHA__`, mode-aware `base`, and `server.proxy` for `/rectrace/api`
    - components.json is verbatim recviz copy with `"baseColor": "mist"`, `"config": ""` (Tailwind v4 marker)
    - src/index.css contains full OKLCH mist token set (no chart-1..5 tokens), the labeled empty RECTRACE EXTENSIONS block, and the AG-Grid token bridge
    - src/lib/theme.ts exports `const tokens` object (no chart-1..5 keys)
    - src/test-setup.ts imports `@testing-library/jest-dom`
    - `grep -c "chart-1" src/index.css` returns 0 (D-2.7 respected)
    - node_modules/ populated (pnpm install or npm install completed)
  </done>
</task>

<task type="auto">
  <name>Task 2: ESLint config and raw-hex fixture</name>
  <files>
    frontend-react/eslint.config.js,
    frontend-react/tests/fixtures/raw-hex.tsx
  </files>

  <read_first>
    1. /Users/aarun/Workspace/Projects/recviz/frontend/eslint.config.js (lines 1-23) — base flat config to copy; apply Phase 2 deltas
    2. .planning/phases/02-react-foundation/02-PATTERNS.md — "eslint.config.js" section (exact hex rule selector string)
    3. .planning/phases/02-react-foundation/02-CONTEXT.md — D-2.8 (hex rejection rule details; vendored components/ui exempt)
  </read_first>

  <action>
    **eslint.config.js** — Implement per PATTERNS.md "eslint.config.js" section. Copy the recviz flat config structure but add:
    - `globalIgnores(['dist', 'src/components/ui/**', 'src/routeTree.gen.ts'])` — exempt vendored shadcn and TanStack Router generated file from hex rule
    - Upgrade `tseslint.configs.recommended` to `tseslint.configs.recommendedTypeChecked` (per D-2.8)
    - Add `languageOptions.parserOptions: { project: ['./tsconfig.app.json'], tsconfigRootDir: import.meta.dirname }`
    - Add the hex-rejection rule (per D-2.8):

          'no-restricted-syntax': ['error', {
            selector: "Literal[value=/^#[0-9a-fA-F]{3,8}$/]",
            message: 'Use CSS tokens (var(--color-*)) instead of raw hex literals. See tokens.css.',
          }]

    - Full imports required: `import js from '@eslint/js'`, `import globals from 'globals'`, `import reactHooks from 'eslint-plugin-react-hooks'`, `import reactRefresh from 'eslint-plugin-react-refresh'`, `import tseslint from 'typescript-eslint'`, `import { defineConfig, globalIgnores } from 'eslint/config'`

    **tests/fixtures/raw-hex.tsx** — Create a minimal React component that contains a raw hex literal so the ESLint rule fires on it. This file intentionally fails lint — its purpose is to be the negative test case:

        // This file intentionally contains a raw hex literal to verify ESLint D-2.8 enforcement.
        // Expected: eslint reports error on the hex literal below.
        // Do NOT fix the hex literal — it is the fixture.
        export function BadColorComponent() {
          return <div style={{ color: '#1a2b3c' }}>intentionally bad hex</div>
        }

    The file lives in `tests/fixtures/` (NOT in `src/`) so it is caught by ESLint but does not affect the production build.

    Verify ESLint correctly rejects the fixture:
    `cd frontend-react && pnpm lint -- tests/fixtures/raw-hex.tsx 2>&1 | grep "raw hex literals"`
    Expected: output contains the error message "Use CSS tokens (var(--color-*)) instead of raw hex literals."

    NOTE: `src/test-setup.ts` was created in Task 1 (not here). This task only owns eslint.config.js and the fixture file.
  </action>

  <verify>
    <automated>
      grep -c "no-restricted-syntax" /Users/aarun/Workspace/Projects/autosys-job-explorer/frontend-react/eslint.config.js
      grep -c "src/components/ui/\*\*" /Users/aarun/Workspace/Projects/autosys-job-explorer/frontend-react/eslint.config.js
      grep -c "#1a2b3c" /Users/aarun/Workspace/Projects/autosys-job-explorer/frontend-react/tests/fixtures/raw-hex.tsx
      cd /Users/aarun/Workspace/Projects/autosys-job-explorer/frontend-react && pnpm lint -- tests/fixtures/raw-hex.tsx 2>&1 | grep -c "raw hex literals"
    </automated>
  </verify>

  <done>
    - eslint.config.js uses flat config with `recommendedTypeChecked`, globalIgnores exempting `src/components/ui/**` and `src/routeTree.gen.ts`, and the hex-rejection `no-restricted-syntax` rule with selector `"Literal[value=/^#[0-9a-fA-F]{3,8}$/]"`
    - Running `pnpm lint tests/fixtures/raw-hex.tsx` reports at least 1 ESLint error mentioning "raw hex literals"
    - src/test-setup.ts already exists from Task 1 (not created here)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → repo | Config files written to disk during scaffold; no user input crosses a trust boundary in this plan |
| build-time → runtime | `__BUILD_SHA__` is injected at build time from `git rev-parse`; the value is a short hex string, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Information Disclosure | `VITE_AG_GRID_LICENSE_KEY` env var | mitigate | `.env.local` in `.gitignore` (this plan creates the `.gitignore` entry); `.env.local.example` template does not contain the real key |
| T-2-02 | Injection | `X-Correlation-Id` backend header | N/A — out of scope for Plan 01 | Handled in Plan 02 (custom Propagation.Factory with 32-hex regex validation) |
| T-2-03 | Information Disclosure | CORS over-permission | N/A — out of scope for Plan 01 | Phase 9 SEC-04 |
| T-2-04 | Elevation of Privilege | Dev proxy exposes backend to local attacker | accept | `server.proxy` is dev-only (Vite does not include it in production builds); macOS dev only; low risk |
| T-2-05 | Tampering | `ops/build.sh` static cleanup | N/A — out of scope for Plan 01 | Handled in Plan 04 |
</threat_model>

<verification>
End-of-plan verification checklist:
1. `test -d frontend-react/node_modules` — dependencies installed
2. `grep -c '"packageManager": "pnpm@9.15.0"' frontend-react/package.json` → 1
3. `grep -c 'RECTRACE EXTENSIONS' frontend-react/src/index.css` → 1
4. `grep -c 'chart-1' frontend-react/src/index.css` → 0 (D-2.7 respected)
5. `grep -c '__BUILD_SHA__' frontend-react/vite.config.ts` → 1
6. `grep -c '"baseColor": "mist"' frontend-react/components.json` → 1
7. `grep -c "@testing-library/jest-dom" frontend-react/src/test-setup.ts` → 1
8. `cd frontend-react && pnpm lint tests/fixtures/raw-hex.tsx 2>&1 | grep -c "raw hex literals"` → 1
9. `cd frontend-react && pnpm test --run 2>&1 | grep -E "0 failed|no test files"` → exits 0
</verification>

<success_criteria>
- `frontend-react/` scaffold exists with all config files, dependency manifest, and src layout
- `pnpm install` completes without errors (or npm install fallback)
- ESLint flat config with hex-rejection rule is active; running lint against `tests/fixtures/raw-hex.tsx` reports an error
- `src/index.css` contains full mist OKLCH token set (no chart-1..5 tokens — D-2.7 honored), empty RECTRACE EXTENSIONS block
- `src/lib/theme.ts` exports `const tokens` mirroring all CSS variables (no chart-1..5 keys)
- `src/test-setup.ts` imports `@testing-library/jest-dom` (created in Task 1 alongside vitest.config.ts)
- `package.json` declares `"packageManager": "pnpm@9.15.0"` (D-2.2 honored)
- No `/v6/` URL prefix anywhere in frontend-react (D-2.4 honored)
- No chart/series/ramp tokens in index.css (D-2.7 honored)
- `src/lib/agGrid.ts` is NOT created; LicenseManager.setLicenseKey is inlined in main.tsx (WARNING 7 resolved)
</success_criteria>

<output>
After completion, create `.planning/phases/02-react-foundation/02-01-SUMMARY.md` with:
- Files created (list)
- Versions pinned (table: library, version pinned, reason for pin vs latest)
- Key decisions executed (D-2.1, D-2.2, D-2.4, D-2.6, D-2.7, D-2.8, D-2.14 with one-line status each)
- Deferred items confirmed (chart tokens NOT in index.css; RECTRACE EXTENSIONS empty block present)
- src/lib/agGrid.ts not created — LicenseManager.setLicenseKey inlined in main.tsx (D-2.14 satisfied)
- Any deviations from PATTERNS.md (document with rationale)
</output>
</output>
