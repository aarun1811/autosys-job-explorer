---
phase: 00-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/parity-matrix.md
autonomous: true
requirements:
  - FOUND-04

must_haves:
  truths:
    - ".planning/parity-matrix.md is committed with one row per Angular route/tab/modal/feature"
    - "Every row has a non-empty Target value (may be 'tbd' for undecided items, or a concrete verb for known items)"
    - "The gating rule is stated at the top: React phase can start once every row has a non-tbd target"
    - "TLM Stats Modal V2 row has target replace-content-with-recviz (known from D-11 and user's canonical example)"
    - "TLM Stats Modal V1 row has target drop (dead code per CONCERNS.md)"
    - "The five-valued target vocabulary is documented in the matrix"
  artifacts:
    - path: ".planning/parity-matrix.md"
      provides: "Day-0 React↔Angular parity snapshot with all Angular routes, tabs, modals, grid features"
      contains: "replace-content-with-recviz"
  key_links:
    - from: ".planning/parity-matrix.md gating rule"
      to: "Phase 2 (React Foundation) planning gate"
      via: "D-14: React phase can start once every row has a non-tbd target"
      pattern: "tbd"
---

<objective>
Produce the day-0 React↔Angular parity matrix at .planning/parity-matrix.md.

Purpose: Create the living document that gates the React Foundation phase (Phase 2). Every Angular capability gets a row with a compact renderer list and a target verb. This prevents the dual-SPA failure mode where React phases start without knowing which Angular capabilities to port, drop, or hand to recviz.

Output: .planning/parity-matrix.md with ~16–18 rows covering 13 search tabs, 4 modals, 2 cross-cutting grid features, and 2 toolbar/app-shell features. TLM Stats Modal V2 gets target replace-content-with-recviz (the canonical user example from D-11). TLM Stats Modal V1 gets target drop. All other rows get tbd unless a clear target is obvious from the research findings.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/phases/00-foundation/00-PATTERNS.md
@.planning/phases/00-foundation/00-CONTEXT.md

<interfaces>
<!-- Source data for the parity matrix rows. Extracted from codebase. -->

13 search categories from search-config-v4.json (verified in RESEARCH.md §"Day-0 findings"):
```
fileName     → File Name        → appIDCellRenderer, supportEmailCellRenderer, executionOrderButtonRenderer
reconName    → Recon Name       → executionOrderButtonRenderer
boxName      → Box Name         → executionOrderButtonRenderer
setId        → Set ID           → executionOrderButtonRenderer
subAcc       → Sub Account      → executionOrderButtonRenderer
loadFileName → Load File Name   → executionOrderButtonRenderer
jobName      → Job Name         → appIDCellRenderer, supportEmailCellRenderer, executionOrderButtonRenderer
machineName  → Machine Name     → (none)
runCalendar  → Run Calendar     → (none)
excludeCalendar → Exclude Calendar → (none)
tlmInstance  → TLM Instance     → (none — tlmInstanceV2Renderer registered in grid but NOT in search-config-v4.json)
reconId      → Recon ID         → (none — reconIdRenderer registered in grid but NOT in search-config-v4.json)
reconPortalId → Recon Portal ID → (none — recPortalIdRenderer registered in grid but NOT in search-config-v4.json)
```

8 registered renderer keys in SearchV5GridComponent.gridOptions.components (lines 181–189):
- appIDCellRenderer → AppIDCellRendererComponent
- supportEmailCellRenderer → AppSupportCellRendererComponent
- executionOrderButtonRenderer → ExecutionOrderButtonComponent (opens execution order modal)
- setIdV2Renderer → SetIdV2RendererComponent (registered, not in search-config-v4.json)
- reconV2Renderer → ReconV2RendererComponent (registered, not in search-config-v4.json)
- tlmInstanceV2Renderer → TlmInstanceV2RendererComponent (registered, not in search-config-v4.json)
- reconIdRenderer → ReconIdRendererComponent (registered, not in search-config-v4.json)
- recPortalIdRenderer → RecPortalIdRendererComponent (registered, not in search-config-v4.json)

Modal inventory (STRUCTURE.md + RESEARCH.md):
- custom-interactions/components/modals/execution-order-graph/ + execution-order-modal/ → Cytoscape.js execution order graph
- custom-interactions/components/modals/tlm-stats-modal/ → TLM Stats V1 (dead code per CONCERNS.md MEDIUM)
- custom-interactions/components/modals/tlm-stats-modal-v2/ → TLM Stats V2 (active, triggered by TlmInstanceV2RendererComponent)
- custom-interactions/components/modals/quickrec-stats-modal/ → QuickRec stats

Cross-cutting features:
- Dark/light mode toggle: services/theme.service.ts, ThemeService
- AG-Grid SSRM + group expansion: search-v5-grid.component.ts
- AG-Grid column/filter sidebar: search-v5-grid.component.ts AG-Grid side bar config
- Excel export: search-v5-grid.component.ts, AG-Grid export API
- Recent searches / typeahead: search-v5.component.ts (note: per RESEARCH.md example rows, this is NOT in Angular today — to be built in React)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory Angular codebase and create .planning/parity-matrix.md</name>
  <files>
    .planning/parity-matrix.md
  </files>

  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md §"Parity Matrix Day-0 Inventory" and §"Parity Matrix Markdown Table Shape" — complete row data and column schema
    - .planning/phases/00-foundation/00-PATTERNS.md §".planning/parity-matrix.md" — Markdown conventions from REQUIREMENTS.md and ROADMAP.md
    - .planning/phases/00-foundation/00-CONTEXT.md §"Decisions" D-09 through D-14 — locked decisions on matrix granularity, vocabulary, gating rule
    - .planning/phases/00-foundation/00-CONTEXT.md §"Specific Ideas" — the TLM-stats modal canonical example for replace-content-with-recviz
    - backend/rectrace/src/main/resources/search-config-v4.json — verify all 13 category keys and labels before writing rows
    - frontend/rectrace/src/app/search-v5/components/search-v5-grid/search-v5-grid.component.ts — verify the 8 registered renderer keys (lines 181–189) before writing the renderer column
  </read_first>

  <action>
    Run the following inventory commands to verify the row data before writing the file:

    ```bash
    # Verify 13 search categories
    python3 -c "
    import json
    with open('backend/rectrace/src/main/resources/search-config-v4.json') as f:
        d = json.load(f)
    for c in d['categories']:
        renderers = sorted({col['cellRenderer'] for col in c['columns'] if 'cellRenderer' in col})
        print(c['key'], '-', c['label'], '|', renderers)
    "

    # Verify registered renderer keys
    grep -A 20 'components:' frontend/rectrace/src/app/search-v5/components/search-v5-grid/search-v5-grid.component.ts | head -30

    # Verify modal directories
    find frontend/rectrace/src/app/custom-interactions/components/modals -mindepth 1 -maxdepth 1 -type d
    ```

    If the commands surface any category or renderer not in the pre-verified list above, add those rows.

    Then create .planning/parity-matrix.md with this exact structure and content (use the RESEARCH.md verified row data):

    ```markdown
    # React↔Angular Parity Matrix

    **Last updated:** 2026-05-12
    **Status:** Day-0 snapshot — target vocab in progress

    > **Gate:** The React Foundation phase (Phase 2) can begin once every row in this table
    > has a non-`tbd` value in the **Target** column. Priority and Notes may remain `tbd`
    > at that point — they are filled during each React phase's planning.

    > **Target vocabulary:**
    > - `port` — build the capability in React natively
    > - `replace-content-with-recviz` — React owns the renderer/modal shell; content inside is a recviz iframe
    > - `replace-fully-with-recviz` — capability removed from rectrace entirely; lives in recviz
    > - `drop` — not needed in React; delete from inventory
    > - `tbd` — decide during that capability's React phase planning

    ## Search Tabs (from search-config-v4.json)

    | Angular Feature | Type | Current Location | Renderers / Components | Target | Priority | Notes |
    |---|---|---|---|---|---|---|
    | File Name search tab | search-tab | search-config-v4.json#fileName | `appIDCellRenderer`, `supportEmailCellRenderer`, `executionOrderButtonRenderer` | tbd | tbd | tbd |
    | Recon Name search tab | search-tab | search-config-v4.json#reconName | `executionOrderButtonRenderer` | tbd | tbd | tbd |
    | Box Name search tab | search-tab | search-config-v4.json#boxName | `executionOrderButtonRenderer` | tbd | tbd | tbd |
    | Set ID search tab | search-tab | search-config-v4.json#setId | `executionOrderButtonRenderer` | tbd | tbd | tbd |
    | Sub Account search tab | search-tab | search-config-v4.json#subAcc | `executionOrderButtonRenderer` | tbd | tbd | tbd |
    | Load File Name search tab | search-tab | search-config-v4.json#loadFileName | `executionOrderButtonRenderer` | tbd | tbd | tbd |
    | Job Name search tab | search-tab | search-config-v4.json#jobName | `appIDCellRenderer`, `supportEmailCellRenderer`, `executionOrderButtonRenderer` | tbd | tbd | tbd |
    | Machine Name search tab | search-tab | search-config-v4.json#machineName | (none) | tbd | tbd | tbd |
    | Run Calendar search tab | search-tab | search-config-v4.json#runCalendar | (none) | tbd | tbd | tbd |
    | Exclude Calendar search tab | search-tab | search-config-v4.json#excludeCalendar | (none) | tbd | tbd | tbd |
    | TLM Instance search tab | search-tab | search-config-v4.json#tlmInstance | `tlmInstanceV2Renderer` (registered in grid, not referenced by config) | tbd | tbd | tbd |
    | Recon ID search tab | search-tab | search-config-v4.json#reconId | `reconIdRenderer` (registered in grid, not referenced by config) | tbd | tbd | tbd |
    | Recon Portal ID search tab | search-tab | search-config-v4.json#reconPortalId | `recPortalIdRenderer` (registered in grid, not referenced by config) | tbd | tbd | tbd |

    ## Modals

    | Angular Feature | Type | Current Location | Renderers / Components | Target | Priority | Notes |
    |---|---|---|---|---|---|---|
    | Execution Order Modal | modal | custom-interactions/components/modals/execution-order-graph/, execution-order-modal/ | `ExecutionOrderButtonComponent`, Cytoscape.js | tbd | tbd | tbd |
    | TLM Stats Modal V2 | modal | custom-interactions/components/modals/tlm-stats-modal-v2/ | `TlmInstanceV2RendererComponent` | replace-content-with-recviz | tbd | Canonical example of replace-content-with-recviz: the modal shell and renderer stay in rectrace; the dashboard content inside the modal is a recviz iframe. The React cell renderer that opens the modal stays in rectrace. |
    | TLM Stats Modal V1 | modal | custom-interactions/components/modals/tlm-stats-modal/ | `SetIdCellRendererComponent` (v1, dead code) | drop | tbd | Dead code per CONCERNS.md MEDIUM — V1 renderer and modal are unused by the V5 grid. Do not port to React. |
    | QuickRec Stats Modal | modal | custom-interactions/components/modals/quickrec-stats-modal/ | `QuickRecStatsService`, dedicated renderer | tbd | tbd | tbd |

    ## Grid Features

    | Angular Feature | Type | Current Location | Renderers / Components | Target | Priority | Notes |
    |---|---|---|---|---|---|---|
    | AG-Grid SSRM + group expansion | grid-feature | search-v5-grid.component.ts | `SearchV5GridComponent`, AG-Grid Enterprise SSRM | port | tbd | Core grid functionality — must be ported to AG-Grid React with identical SSRM datasource |
    | AG-Grid column / filter sidebar | grid-feature | search-v5-grid.component.ts | AG-Grid side bar config | port | tbd | tbd |

    ## Toolbar and App Shell Features

    | Angular Feature | Type | Current Location | Renderers / Components | Target | Priority | Notes |
    |---|---|---|---|---|---|---|
    | Excel export | toolbar-feature | search-v5-grid.component.ts | AG-Grid export API | port | tbd | tbd |
    | Recent searches / typeahead | toolbar-feature | search-v5.component.ts | `SearchV5Component` | port | tbd | Not present in Angular today — to be built natively in React using localStorage |
    | Dark / light mode toggle | app-shell | services/theme.service.ts | `ThemeService` | port | tbd | tbd |

    ## Renderer Inventory (Registered, Not All Wired to V4 Config)

    The following renderers are registered in `SearchV5GridComponent.gridOptions.components` but are not currently
    referenced in `search-config-v4.json`. They may be active via legacy V3 config or dynamic category config.
    Assign `port` or `drop` during the React phase that covers their parent tab.

    | Renderer Key | Angular Component | Used By | Target |
    |---|---|---|---|
    | `appIDCellRenderer` | `AppIDCellRendererComponent` | fileName, jobName tabs (V4) | tbd — decide when porting those tabs |
    | `supportEmailCellRenderer` | `AppSupportCellRendererComponent` | fileName, jobName tabs (V4) | tbd — decide when porting those tabs |
    | `executionOrderButtonRenderer` | `ExecutionOrderButtonComponent` | 7 V4 search tabs | tbd — decide when porting those tabs |
    | `setIdV2Renderer` | `SetIdV2RendererComponent` | Not in V4 config | tbd — verify against V3 config; candidate for `drop` |
    | `reconV2Renderer` | `ReconV2RendererComponent` | Not in V4 config | tbd — verify against V3 config; candidate for `drop` |
    | `tlmInstanceV2Renderer` | `TlmInstanceV2RendererComponent` | TLM Instance tab (grid-registered, config-implicit) | tbd |
    | `reconIdRenderer` | `ReconIdRendererComponent` | Not in V4 config | tbd — verify against V3 config; candidate for `drop` |
    | `recPortalIdRenderer` | `RecPortalIdRendererComponent` | Not in V4 config | tbd — verify against V3 config; candidate for `drop` |

    ---

    *Phase 0 — Day-0 snapshot created: 2026-05-12*
    *Matrix is a living document — updated as React phases land.*
    ```

    IMPORTANT: If the live inventory commands (step 1) reveal any category, renderer, or modal not listed above, add those rows before writing the file. Do not silently omit discovered items.
  </action>

  <verify>
    <automated>
      test -f /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/parity-matrix.md && echo "EXISTS" || echo "MISSING"
    </automated>
    Must print EXISTS.

    <automated>
      grep -v '^#' /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/parity-matrix.md | grep -v '^>' | grep -v '^|---' | grep -c '^|'
    </automated>
    Must return at least 18 (covering 13 search tabs + 4 modals + at least 2 grid/shell features + table header rows).

    <automated>
      grep -c 'replace-content-with-recviz' /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/parity-matrix.md
    </automated>
    Must return at least 1 (TLM Stats Modal V2 row).

    <automated>
      grep -c '| drop |' /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/parity-matrix.md
    </automated>
    Must return at least 1 (TLM Stats Modal V1 row).

    <automated>
      grep -c 'Gate' /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/parity-matrix.md
    </automated>
    Must return at least 1 (gating rule present).
  </verify>

  <done>
    .planning/parity-matrix.md exists with at least 18 table rows. TLM Stats Modal V2 has target replace-content-with-recviz. TLM Stats Modal V1 has target drop. Gating rule is stated at the top. Five-valued target vocabulary is documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parity-matrix.md → Phase 2 planning gate | The gating rule in the matrix is an informal contract — if rows are left tbd, Phase 2 planning is blocked. No runtime security surface involved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-07 | Information Disclosure | .planning/parity-matrix.md | accept | Planning artifact only — no credentials, no user data, no runtime impact. Lives in .planning/ alongside other planning docs. Not deployed anywhere. |
| T-00-08 | Tampering | parity-matrix.md target verb accuracy | accept | The matrix is a living doc. Incorrect target assignments are caught during each React phase's discuss-phase review. Phase 0 only requires rows to exist with the day-0 vocabulary, not that every target is final. |
</threat_model>

<verification>
Phase gate for Plan 03 is green when:

1. `test -f .planning/parity-matrix.md` succeeds (file exists)
2. Row count check: at least 18 pipe-table rows (13 search tabs + 4 modals + grid + shell features)
3. `grep -c 'replace-content-with-recviz' .planning/parity-matrix.md` returns at least 1
4. `grep -c '| drop |' .planning/parity-matrix.md` returns at least 1
5. `grep -c 'Gate' .planning/parity-matrix.md` returns at least 1 (gating rule present)
6. Manual check: cross-reference row count against search-config-v4.json category list — no search category is missing a row
</verification>

<success_criteria>
- .planning/parity-matrix.md committed with all 13 search tabs, 4 modals, 2 cross-cutting grid features, and 2 toolbar/app-shell features as rows
- TLM Stats Modal V2 row has target replace-content-with-recviz (canonical example from D-11)
- TLM Stats Modal V1 row has target drop (dead code)
- Gating rule at the top: React phase starts once every row has a non-tbd target
- Five-valued target vocabulary documented in matrix header
- Renderer inventory section present for the 4 registered-but-unconfirmed renderer keys
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-03-SUMMARY.md` using the template at `.claude/get-shit-done/templates/summary.md`. Record: total row count, categories/modals/features covered, which rows received concrete targets vs tbd, and the inventory command outputs.
</output>
