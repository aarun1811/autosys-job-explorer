---
phase: 05-config-driven-select
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ../rectrace-local-dev/init/01-create-schema-users.sql
  - ../rectrace-local-dev/schema/01-rectrace.sql
  - backend/rectrace/src/main/resources/application-local.properties
  - rectrace-tlm-stats/src/main/resources/application-local.properties
autonomous: true
requirements: [SQL-03]
must_haves:
  truths:
    - "A new Oracle user `rectrace_readonly` is created in local Oracle 23c on container init with QUOTA 0 ON USERS and CREATE SESSION only (no RESOURCE, no DML privilege)"
    - "rectrace_readonly has SELECT-only grants on rectrace.rectrace_core (and is denied by structure on every other table)"
    - "backend/rectrace application-local.properties carries `datasource.readonly.*` keys pointing at FREEPDB1 with the new account, plus `queryTimeoutSeconds`/`fetchSize`/`maxRows` caps + `sql-search-config.location`"
    - "`docker compose down -v && docker compose up -d` followed by `sqlplus rectrace_readonly/rectrace_readonly_pwd@//localhost:1521/FREEPDB1` connects successfully and `SELECT COUNT(*) FROM rectrace.rectrace_core` returns >=5 rows"
    - "`INSERT INTO rectrace.rectrace_core (recon) VALUES ('test')` as rectrace_readonly fails with ORA-01031 (insufficient privilege)"
  artifacts:
    - path: "../rectrace-local-dev/init/01-create-schema-users.sql"
      provides: "Idempotent CREATE USER rectrace_readonly + GRANT CREATE SESSION (init-time, runs once on container first boot)"
      contains: "rectrace_readonly"
    - path: "../rectrace-local-dev/schema/01-rectrace.sql"
      provides: "Idempotent GRANT SELECT ON rectrace.rectrace_core TO rectrace_readonly (schema-time, re-applied by apply.py --reset)"
      contains: "GRANT SELECT ON rectrace.rectrace_core TO rectrace_readonly"
    - path: "backend/rectrace/src/main/resources/application-local.properties"
      provides: "datasource.readonly.* + sql-search-config.location keys"
      contains: "datasource.readonly.username=rectrace_readonly"
  key_links:
    - from: "../rectrace-local-dev/init/01-create-schema-users.sql"
      to: "Oracle 23c container first-boot"
      via: "gvenzl/oracle-free entrypoint mounts /docker-entrypoint-initdb.d/init as :ro"
      pattern: "CREATE USER rectrace_readonly"
    - from: "backend/rectrace/src/main/resources/application-local.properties"
      to: "Wave 2 ReadonlyDataSourceConfig @Value bindings"
      via: "Spring property resolution"
      pattern: "datasource\\.readonly\\.(url|username|password|driver-class-name|service-name|db-schema)"
---

<objective>
Stand up the dedicated read-only Oracle account `rectrace_readonly` in local-dev and add the matching Spring properties so Wave 2 can wire a `readonlyDataSource` bean against a real, live, SELECT-only Oracle account from the moment it lands. The user is created at container init with `QUOTA 0` + `CREATE SESSION` only; the SELECT grant lives in the schema DDL so `apply.py --reset` re-applies it.

Purpose: SQL-03 demands a dedicated read-only Oracle account that is structurally incapable of mutation. Standing it up in Wave 1 means Wave 2's `ReadonlyDataSourceConfig` can be smoke-tested live, not stubbed.
Output: 2 DDL edits in the sibling `rectrace-local-dev/` repo + 2 properties-file deltas in this repo.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-config-driven-select/05-CONTEXT.md
@.planning/phases/05-config-driven-select/05-RESEARCH.md
@backend/rectrace/src/main/resources/application-local.properties
@rectrace-tlm-stats/src/main/resources/application-local.properties
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local-dev DDL — create rectrace_readonly user + grant SELECT on rectrace_core</name>
  <read_first>
    - ../rectrace-local-dev/init/01-create-schema-users.sql (existing idempotent schema-user creation pattern)
    - ../rectrace-local-dev/schema/01-rectrace.sql (existing rectrace_core CREATE TABLE; the new GRANT lands after the table definition)
    - ../rectrace-local-dev/README.md if present (apply.py contract — init runs once on container first boot via docker entrypoint, schema/*.sql runs via `apply.py --reset`)
    - .planning/phases/05-config-driven-select/05-RESEARCH.md §Environment Availability and §Code Examples / Example 3
  </read_first>
  <action>
    Edit `../rectrace-local-dev/init/01-create-schema-users.sql` (sibling repo, OUTSIDE this repo): append an idempotent block at the bottom of the file. Use a PL/SQL anonymous block guarded by `BEGIN EXECUTE IMMEDIATE 'CREATE USER ...' EXCEPTION WHEN OTHERS THEN IF SQLCODE != -1920 THEN RAISE; END IF; END;` (Oracle error code for "user already exists" is `-1920`). Specifically:
      - CREATE USER `rectrace_readonly` IDENTIFIED BY `rectrace_readonly_pwd` DEFAULT TABLESPACE USERS QUOTA 0 ON USERS.
      - GRANT CREATE SESSION TO `rectrace_readonly`.
    Do NOT grant RESOURCE. Do NOT grant any *ANY TABLE / DBA / IMP_FULL_DATABASE. The user is read-only by structure (QUOTA 0 prevents any insert anywhere it might have INSERT, CREATE SESSION is the only system priv).

    Edit `../rectrace-local-dev/schema/01-rectrace.sql`: after the existing `CREATE TABLE ... rectrace_core ...` block, append `GRANT SELECT ON rectrace.rectrace_core TO rectrace_readonly;` on its own line. Wrap in PL/SQL `BEGIN EXECUTE IMMEDIATE '...' EXCEPTION WHEN OTHERS THEN IF SQLCODE != -1917 AND SQLCODE != -1918 THEN RAISE; END IF; END;` if and only if the existing 01-rectrace.sql uses that idempotency pattern; otherwise leave the bare GRANT (the apply.py --reset flow drops + recreates the schema each cycle so a bare GRANT after CREATE TABLE is idempotent).

    Do NOT modify any of the other schema files (02-autosys, 03-reconmgmt, 04-recportal) — SQL tabs in Phase 5 only target rectrace_core. Future tabs that need access to autosys / reconmgmt tables will add their own grants in a future plan.

    Append a 3-line comment block at the top of both edited files documenting "Phase 5 (Config-driven SELECT): added rectrace_readonly read-only user for SQL tab evaluation. See .planning/phases/05-config-driven-select/." with date YYYY-MM-DD.
  </action>
  <verify>
    <automated>cd ../rectrace-local-dev && grep -c "rectrace_readonly" init/01-create-schema-users.sql | awk '{if ($1 >= 2) print "OK init"; else { print "FAIL init grep count " $1; exit 1 }}' && grep -q "GRANT SELECT ON rectrace.rectrace_core TO rectrace_readonly" schema/01-rectrace.sql && echo "OK schema"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE USER rectrace_readonly" ../rectrace-local-dev/init/01-create-schema-users.sql` returns >= 1.
    - `grep -c "QUOTA 0 ON USERS" ../rectrace-local-dev/init/01-create-schema-users.sql` returns >= 1.
    - `grep -c "GRANT CREATE SESSION TO rectrace_readonly" ../rectrace-local-dev/init/01-create-schema-users.sql` returns >= 1.
    - `grep -c "GRANT SELECT ON rectrace.rectrace_core TO rectrace_readonly" ../rectrace-local-dev/schema/01-rectrace.sql` returns >= 1.
    - `grep -c "GRANT \\(RESOURCE\\|DBA\\|.*ANY TABLE\\)" ../rectrace-local-dev/init/01-create-schema-users.sql ../rectrace-local-dev/schema/01-rectrace.sql` returns 0 (no over-privileged grants).
    - Live behavior probe: `cd ../rectrace-local-dev && docker compose down -v && docker compose up -d` (wait ~60s for Oracle ready), then `./apply.py --reset` (or whatever the equivalent command is per its README), then run `docker exec rectrace-oracle sqlplus -L rectrace_readonly/rectrace_readonly_pwd@//localhost:1521/FREEPDB1 <<< "SELECT COUNT(*) FROM rectrace.rectrace_core;"` — exits 0 and prints a count >= 5.
    - Negative probe: `docker exec rectrace-oracle sqlplus -L rectrace_readonly/rectrace_readonly_pwd@//localhost:1521/FREEPDB1 <<< "INSERT INTO rectrace.rectrace_core (recon) VALUES ('test');"` — output contains `ORA-01031` (insufficient privileges) OR `ORA-00942` (table or view does not exist — also a deny outcome).
  </acceptance_criteria>
  <done>
    rectrace_readonly user exists in local Oracle 23c, can SELECT from rectrace.rectrace_core, and is refused on INSERT. Verifiable in <2 minutes via docker compose + sqlplus probes.
  </done>
</task>

<task type="auto">
  <name>Task 2: application-local.properties — datasource.readonly.* + sql-search-config.location</name>
  <read_first>
    - backend/rectrace/src/main/resources/application-local.properties (existing primary datasource keys — match style and ordering)
    - backend/rectrace/src/main/resources/application.properties (production default — confirm `datasource.*` prefix convention)
    - rectrace-tlm-stats/src/main/resources/application-local.properties (sibling local config — needs no change for Phase 5, but read to confirm Phase 5 does not affect TLM stats)
    - .planning/phases/05-config-driven-select/05-RESEARCH.md §Code Examples / Example 4 (application-local.properties delta)
  </read_first>
  <action>
    Edit `backend/rectrace/src/main/resources/application-local.properties`. Append a new section at the bottom (preserve all existing keys verbatim). New keys:
      - `datasource.readonly.url=jdbc:oracle:thin:@localhost:1521/FREEPDB1`
      - `datasource.readonly.username=rectrace_readonly`
      - `datasource.readonly.password=rectrace_readonly_pwd`
      - `datasource.readonly.driver-class-name=oracle.jdbc.OracleDriver`
      - `datasource.readonly.service-name=FREEPDB1`
      - `datasource.readonly.db-schema=RECTRACE`
      - `datasource.readonly.hikari.maximum-pool-size=5`
      - `datasource.readonly.hikari.minimum-idle=1`
      - `datasource.readonly.hikari.connection-timeout=20000`
      - `datasource.readonly.queryTimeoutSeconds=30`
      - `datasource.readonly.fetchSize=500`
      - `datasource.readonly.maxRows=10000`
      - `sql-search-config.location=classpath:sql-search-config-v4.json`
    Note: `sql-search-config.location` defaults to `classpath:` not `file:` so the JSON ships with the JAR. Wave 3 creates the JSON; this property only resolves at runtime so absent JSON is fine for Wave 1.
    Prefix the new section with a `# Phase 5: Config-driven SELECT — read-only datasource + SQL tab caps` comment line. Property naming uses camelCase suffix-as-units (`queryTimeoutSeconds`) per Research Pitfall 3 — confirms units to the reader.

    Do NOT modify `rectrace-tlm-stats/src/main/resources/application-local.properties` — Phase 5 changes are confined to `backend/rectrace`. (Listed in `files_modified` only to declare the file was inspected; this task ends with a no-op for tlm-stats. Update the frontmatter `files_modified` to drop the tlm-stats line if you prefer; either is acceptable as long as the executor does not introduce stray edits.)
  </action>
  <verify>
    <automated>grep -c "datasource.readonly" backend/rectrace/src/main/resources/application-local.properties | awk '{if ($1 >= 12) print "OK"; else { print "FAIL: expected >= 12 datasource.readonly keys, got " $1; exit 1 }}' && grep -q "sql-search-config.location=classpath:sql-search-config-v4.json" backend/rectrace/src/main/resources/application-local.properties && echo "OK location"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "datasource\\.readonly\\." backend/rectrace/src/main/resources/application-local.properties` returns >= 12 (12 new keys).
    - `grep -q "datasource.readonly.username=rectrace_readonly"` matches.
    - `grep -q "datasource.readonly.queryTimeoutSeconds=30"` matches (suffix-as-units confirms seconds per Pitfall 3).
    - `grep -q "sql-search-config.location=classpath:sql-search-config-v4.json"` matches.
    - `cd backend/rectrace && mvn -q spring-boot:run -Dspring-boot.run.profiles=local` either (a) starts cleanly if Wave 2 has landed (out-of-order; not expected here), OR (b) fails with `Could not resolve placeholder 'datasource.readonly.url'` (proves the props file is loaded but the consuming bean doesn't exist yet — Wave 2's responsibility). Either outcome is acceptable; what is NOT acceptable is a different startup error caused by the property changes.
    - `cd backend/rectrace && mvn -q -DskipTests compile` exits 0 (properties file does not break compilation).
    - `rectrace-tlm-stats/src/main/resources/application-local.properties` byte-identical to its pre-edit state (run `git diff rectrace-tlm-stats/src/main/resources/application-local.properties` — empty output).
  </acceptance_criteria>
  <done>
    `application-local.properties` carries the 13 new keys (12 datasource.readonly.* + 1 sql-search-config.location). Properties resolve cleanly; no tlm-stats edits.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Oracle init scripts → live Oracle account | DDL runs once at container first-boot; any over-grant lands in the live local-dev account and is invisible until exploited. |
| application-local.properties → JVM | Plaintext password ships in this repo (local-dev only; Citi VM uses ScriptExecutor). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | E (Elevation of Privilege) | DDL grants rectrace_readonly more than SELECT | mitigate | `grep -c "GRANT \\(RESOURCE\\|DBA\\|.*ANY TABLE\\) TO rectrace_readonly"` MUST return 0 across init/ and schema/ — explicit negative gate in acceptance_criteria. QUOTA 0 makes INSERT structurally impossible even if a future grant accidentally permits it. |
| T-05-04 | I (Information Disclosure) | Plaintext password in application-local.properties | accept | Local-dev only; matches existing primary datasource pattern committed in Phase 0.1. Production uses ScriptExecutor (Phase 9). |
| T-05-05 | T (Tampering) | INSERT/UPDATE/DELETE through rectrace_readonly | mitigate | Negative probe in acceptance_criteria asserts ORA-01031 on attempted INSERT. Structural defense via QUOTA 0 + GRANT SELECT only. |
</threat_model>

<verification>
- `grep -c "rectrace_readonly" ../rectrace-local-dev/init/01-create-schema-users.sql ../rectrace-local-dev/schema/01-rectrace.sql` returns >= 3 occurrences total.
- Live probe sequence (executor must run): docker compose down -v && docker compose up -d (wait for healthy) && apply.py --reset && `sqlplus rectrace_readonly/rectrace_readonly_pwd@//localhost:1521/FREEPDB1 -L <<< "SELECT COUNT(*) FROM rectrace.rectrace_core;"` returns count >= 5. INSERT attempt returns ORA-01031.
- `grep -c "datasource.readonly" backend/rectrace/src/main/resources/application-local.properties` returns >= 12.
- `git diff --name-only rectrace-tlm-stats/src/main/resources/application-local.properties` returns empty.
</verification>

<success_criteria>
- rectrace_readonly user exists in local Oracle, can SELECT rectrace_core, cannot INSERT.
- backend/rectrace/src/main/resources/application-local.properties carries 13 new keys.
- No over-privileged grants anywhere in init/ or schema/ DDL files.
- rectrace-tlm-stats not modified.
- No production code touched (backend/rectrace/src/main/java/ unchanged).
</success_criteria>

<output>
After completion, create `.planning/phases/05-config-driven-select/05-02-SUMMARY.md` recording: exact paths edited in sibling repo, the live sqlplus probe outputs (count and ORA-01031), the new property-key list, and a one-line confirmation that tlm-stats is untouched.
</output>
