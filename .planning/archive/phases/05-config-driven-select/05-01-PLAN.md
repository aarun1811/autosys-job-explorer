---
phase: 05-config-driven-select
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/service/v4/SqlShapeValidatorTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/service/v4/SqlSearchConfigServiceV4Test.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/service/v4/SqlValidationBootFailureTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/service/v4/SqlQueryServiceV4Test.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/config/ReadonlyDataSourceConfigTest.java
  - scripts/smoke-sql-search.sh
autonomous: true
requirements: [SQL-02, SQL-04, SQL-05, SQL-06, SQL-07]
must_haves:
  truths:
    - "Five JUnit 5 test classes exist with @Disabled or compile-only skeletons matching the SQL-0X behaviors they will assert"
    - "scripts/smoke-sql-search.sh exists, is chmod +x, and exits non-zero with a clear FAIL message when the backend is not running"
    - "Every test class compiles (mvn -q -DskipTests compile + test-compile both green) before Wave 2 starts"
  artifacts:
    - path: "backend/rectrace/src/test/java/com/citi/gru/rectrace/service/v4/SqlShapeValidatorTest.java"
      provides: "Parameterized JSqlParser visitor assertions — SELECT/WITH accept; INSERT/UPDATE/DELETE/DDL reject; unbounded SELECT reject"
    - path: "backend/rectrace/src/test/java/com/citi/gru/rectrace/service/v4/SqlSearchConfigServiceV4Test.java"
      provides: "Asserts loadsTabs from a happy-path JSON fixture"
    - path: "backend/rectrace/src/test/java/com/citi/gru/rectrace/service/v4/SqlValidationBootFailureTest.java"
      provides: "@SpringBootTest with @TestPropertySource sql-search-config.location pointing to bad fixtures; asserts IllegalStateException with offending tab key"
    - path: "backend/rectrace/src/test/java/com/citi/gru/rectrace/service/v4/SqlQueryServiceV4Test.java"
      provides: "Asserts setQueryTimeout/setFetchSize/setMaxRows applied to PreparedStatement; OFFSET/FETCH wrapper present in executed SQL"
    - path: "backend/rectrace/src/test/java/com/citi/gru/rectrace/config/ReadonlyDataSourceConfigTest.java"
      provides: "Asserts readonlyDataSource bean exists with @Profile activation"
    - path: "scripts/smoke-sql-search.sh"
      provides: "End-of-phase smoke: /config returns reconSummary; /ssrm/reconSummary returns {rows,lastRow} with RECON-XYZ-42 in body"
  key_links:
    - from: "All five test classes"
      to: "Wave 2-4 production classes"
      via: "@Disabled on each @Test until the production class lands; Wave 4 plan removes @Disabled and asserts green"
      pattern: "@Disabled\\(\"Wave [2345]:"
---

<objective>
Create the Wave 0 test scaffolding for Phase 5: five JUnit 5 test classes and one bash smoke script. Each test class declares the assertions Wave 2–5 must turn green, all start `@Disabled` with a `"Wave N: ..."` reason string, and compile clean against the existing classpath. The smoke script exits non-zero with a clear message when the backend is offline so it is wired correctly from day one.

Purpose: Lock the SQL-02/04/05/06/07 behaviors into compile-time test code before any production code exists, so subsequent plans implement against fixed contracts rather than inventing them.
Output: 5 Java test files + 1 bash script.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-config-driven-select/05-CONTEXT.md
@.planning/phases/05-config-driven-select/05-RESEARCH.md
@backend/rectrace/src/test/java/com/citi/gru/rectrace/ContextLoadsTest.java
@backend/rectrace/src/test/resources/application-test.properties
@backend/rectrace/src/main/java/com/citi/gru/rectrace/dto/v4/SSRMRequestV4.java
@scripts/smoke-ssrm.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: SqlShapeValidatorTest + SqlSearchConfigServiceV4Test + SqlValidationBootFailureTest skeletons</name>
  <read_first>
    - backend/rectrace/src/test/java/com/citi/gru/rectrace/ContextLoadsTest.java (existing @SpringBootTest + @ActiveProfiles("test") pattern)
    - backend/rectrace/src/test/resources/application-test.properties (existing Phase 0 test profile)
    - backend/rectrace/pom.xml (confirm spring-boot-starter-test + junit-jupiter-params on classpath; junit-jupiter-params is transitive)
    - .planning/phases/05-config-driven-select/05-RESEARCH.md §Validation Architecture and Pattern 1 (SqlShapeValidator visitor)
  </read_first>
  <action>
    Create three JUnit 5 test classes under `backend/rectrace/src/test/java/com/citi/gru/rectrace/service/v4/`.

    `SqlShapeValidatorTest.java` — package `com.citi.gru.rectrace.service.v4`. Uses `@ParameterizedTest` with `@MethodSource` named `validSelectShapes` and `invalidShapes`. Methods:
      - `acceptsValidSelectShapes(String sql)` calls `SqlShapeValidator.validate("test", sql)` — annotated `@Disabled("Wave 4: enabled when SqlShapeValidator lands")`.
      - `rejectsInvalidShapes(String sql, String expectedFragment)` asserts `IllegalStateException` thrown with message containing `expectedFragment` — same @Disabled.
    `validSelectShapes()` returns Stream.of: `"SELECT a FROM t WHERE x=1"`, `"SELECT a FROM t FETCH FIRST 10 ROWS ONLY"`, `"WITH cte AS (SELECT 1 FROM dual WHERE 1=1) SELECT * FROM cte WHERE 1=1"`, `"SELECT a FROM t WHERE x=1 UNION SELECT b FROM u WHERE y=2"`.
    `invalidShapes()` returns Stream.of arguments: `("INSERT INTO foo VALUES (1)", "not a SELECT")`, `("UPDATE foo SET a=1", "not a SELECT")`, `("DELETE FROM foo", "not a SELECT")`, `("CREATE TABLE x (a NUMBER)", "not a SELECT")`, `("SELECT * FROM rectrace_core", "missing both WHERE and FETCH")`.
    Imports use `org.junit.jupiter.params.ParameterizedTest`, `org.junit.jupiter.params.provider.MethodSource`, `org.junit.jupiter.params.provider.Arguments`, `org.junit.jupiter.api.Disabled`, `org.junit.jupiter.api.Assertions.assertThrows`. Reference `com.citi.gru.rectrace.service.v4.SqlShapeValidator` (compiles only after Wave 4 — for now reference via a `// TODO Wave 4` comment AND a `private static void validate(String key, String sql) { throw new IllegalStateException("stub"); }` private helper inside the test class so the file compiles standalone, with a comment that Wave 4 deletes the stub and switches to the real import).

    `SqlSearchConfigServiceV4Test.java` — package same. Single `@Test` method `loadsTabs()` annotated `@Disabled("Wave 4: enabled when SqlSearchConfigServiceV4 lands")`. Body uses `assertThat(...)` placeholder asserting the future service exposes a `getTabs()` list of size >= 1 containing key `"reconSummary"`. Same stub pattern (private inline placeholder type or `// TODO Wave 4`) so the file compiles.

    `SqlValidationBootFailureTest.java` — package same. `@SpringBootTest` + `@ActiveProfiles("test")`. Three `@Test` methods all `@Disabled("Wave 4: enabled when SqlSearchConfigServiceV4 lands")`: `rejectsInsertStatement()`, `rejectsUnboundedSelect()`, `acceptsValidCte()`. Methods document via comments that Wave 4 will switch to `@TestPropertySource(properties = "sql-search-config.location=classpath:test-sql-config-bad-insert.json")` per-test (separate test classes per fixture OR `@Nested` classes — leave a `// TODO Wave 4: split into @Nested per fixture` comment).

    Each `@Disabled` reason starts with literal `"Wave 4: "` (grep-matchable). All three files use `// SPDX-FileCopyrightText` only if existing files do (check ContextLoadsTest.java first; if it has none, skip).
  </action>
  <verify>
    <automated>cd backend/rectrace && mvn -q -DskipTests test-compile && grep -rc "Wave 4:" src/test/java/com/citi/gru/rectrace/service/v4/Sql*Test.java | awk -F: '{s+=$2} END {if (s >= 6) print "OK"; else { print "FAIL: expected >= 6 Wave 4 @Disabled markers, got " s; exit 1 }}'</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend/rectrace && mvn -q -DskipTests test-compile` exits 0.
    - Files exist at the three exact paths declared in `files_modified`.
    - `grep -l "@Disabled(\"Wave 4:" backend/rectrace/src/test/java/com/citi/gru/rectrace/service/v4/SqlShapeValidatorTest.java backend/rectrace/src/test/java/com/citi/gru/rectrace/service/v4/SqlSearchConfigServiceV4Test.java backend/rectrace/src/test/java/com/citi/gru/rectrace/service/v4/SqlValidationBootFailureTest.java` returns all three paths.
    - `grep -c "ParameterizedTest\|MethodSource" backend/rectrace/src/test/java/com/citi/gru/rectrace/service/v4/SqlShapeValidatorTest.java` returns >= 2 (filtered for non-comment lines).
    - `cd backend/rectrace && mvn -q test -Dtest='Sql*Test' | grep -E "Tests run|Disabled" | grep -q "Disabled"` — running the disabled tests reports them as skipped, not failing.
    - No reference to `SqlShapeValidator` or `SqlSearchConfigServiceV4` as an import at top of file (production classes don't exist yet); references live only in comments + private stubs.
  </acceptance_criteria>
  <done>
    Three test files compile and are visible to the JUnit runner as @Disabled. Wave 4 will enable each by deleting the stub + switching to the real import.
  </done>
</task>

<task type="auto">
  <name>Task 2: SqlQueryServiceV4Test + ReadonlyDataSourceConfigTest skeletons + scripts/smoke-sql-search.sh</name>
  <read_first>
    - backend/rectrace/src/test/java/com/citi/gru/rectrace/ContextLoadsTest.java
    - scripts/smoke-ssrm.sh (sibling smoke script — match shebang, `set -euo pipefail`, BASE_URL pattern)
    - .planning/phases/05-config-driven-select/05-RESEARCH.md §Code Examples / Example 2 (smoke script skeleton) and §Pattern 2 (StatementCallback)
    - backend/rectrace/src/main/java/com/citi/gru/rectrace/service/v4/OracleServiceV4.java (review only the `setQueryTimeout`/`fetchSize` call sites that the test will assert against the new path)
  </read_first>
  <action>
    Create two more test classes + one bash script.

    `SqlQueryServiceV4Test.java` at `backend/rectrace/src/test/java/com/citi/gru/rectrace/service/v4/`. Mockito-based unit test (no @SpringBootTest). Two `@Test` methods, both `@Disabled("Wave 4: enabled when SqlQueryServiceV4 lands")`:
      - `perStatementCapsAppliedNotSingleton()` — uses `Mockito.mock(JdbcTemplate.class)`, captures the `PreparedStatementCreator` lambda via `ArgumentCaptor<PreparedStatementCreator>`, invokes the captured lambda with a `Mockito.mock(Connection.class)` that returns a `Mockito.mock(PreparedStatement.class)`, then verifies `verify(ps).setQueryTimeout(30); verify(ps).setFetchSize(500); verify(ps).setMaxRows(10000);` AND `verify(jdbcTemplate, never()).setQueryTimeout(anyInt());` — proving caps apply per-statement, not on the singleton.
      - `injectsOffsetFetchWrapper()` — captures the SQL string passed to `conn.prepareStatement(sql)` via ArgumentCaptor, asserts it matches the regex `(?is)^\\s*SELECT \\* FROM \\(.*\\) .* OFFSET \\? ROWS FETCH NEXT \\? ROWS ONLY\\s*$`.
    Include `import org.springframework.jdbc.core.PreparedStatementCreator;` and `import org.springframework.jdbc.core.PreparedStatementCallback;` so the file compiles against existing Spring JDBC classpath (already transitive via spring-boot-starter-data-jpa). Stub the not-yet-existing `SqlQueryServiceV4` with `// TODO Wave 4` plus a no-op placeholder class declaration inside a comment.

    `ReadonlyDataSourceConfigTest.java` at `backend/rectrace/src/test/java/com/citi/gru/rectrace/config/`. `@SpringBootTest` + `@ActiveProfiles("test")` + `@Disabled("Wave 2: enabled when ReadonlyDataSourceConfig lands")`. Single `@Test beanExistsUnderNonTestProfile()` documents via comment that the production path is gated `@Profile("!test")`; the test itself uses `@Autowired Environment env` and `@Autowired ApplicationContext ctx` to assert `assertThat(ctx.containsBean("readonlyDataSource")).isFalse()` under the `test` profile (proving the @Profile guard works) — this matches the existing pattern that keeps `DataSourceConfig` out of `ContextLoadsTest`.

    `scripts/smoke-sql-search.sh` — copy structure from `scripts/smoke-ssrm.sh`:
      - Shebang `#!/usr/bin/env bash`, `set -euo pipefail`.
      - `BASE_URL="${BASE_URL:-http://localhost:6088/rectrace}"`.
      - Step 1: `curl -fsS "${BASE_URL}/api/v4/sql-search/config"` piped to `grep -q '"key":"reconSummary"'` with `|| { echo "FAIL: reconSummary not in /config"; exit 1; }`.
      - Step 2: POST to `${BASE_URL}/api/v4/sql-search/ssrm/reconSummary` with body `{"startRow":0,"endRow":100,"sortModel":[],"filterModel":{}}` and `Content-Type: application/json`. Save response into `RESP`.
      - Step 3: grep `RESP` for `"rows"` AND `"lastRow"` (two separate grep -q invocations). On failure: `echo "FAIL: shape missing rows/lastRow"; exit 1`.
      - Step 4: grep `RESP` for `RECON-XYZ-42` with `|| { echo "FAIL: hyphenated recon value missing"; exit 1; }`.
      - Final line: `echo "OK: SQL search smoke green"`.
      - `chmod +x scripts/smoke-sql-search.sh` after creation.
  </action>
  <verify>
    <automated>cd backend/rectrace && mvn -q -DskipTests test-compile && bash -n /Users/aarun/Workspace/Projects/autosys-job-explorer/scripts/smoke-sql-search.sh && test -x /Users/aarun/Workspace/Projects/autosys-job-explorer/scripts/smoke-sql-search.sh && echo "OK compile + bash syntax + exec bit"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/smoke-sql-search.sh` exits 0 (syntax valid).
    - `test -x scripts/smoke-sql-search.sh` true.
    - With backend NOT running: `BASE_URL=http://localhost:9 scripts/smoke-sql-search.sh` exits non-zero and prints a FAIL message (verifies the `|| { echo FAIL; exit 1 }` wiring).
    - `grep -c "Wave 4:\|Wave 2:" backend/rectrace/src/test/java/com/citi/gru/rectrace/service/v4/SqlQueryServiceV4Test.java backend/rectrace/src/test/java/com/citi/gru/rectrace/config/ReadonlyDataSourceConfigTest.java | awk -F: '{s+=$2} END {if (s >= 3) print "OK"; else exit 1}'` — at least 3 `@Disabled` markers between the two files.
    - `mvn -q -DskipTests test-compile` green.
    - `grep -q "ArgumentCaptor<PreparedStatementCreator>" backend/rectrace/src/test/java/com/citi/gru/rectrace/service/v4/SqlQueryServiceV4Test.java`.
    - `grep -q "OFFSET \\\\? ROWS FETCH NEXT \\\\? ROWS ONLY" backend/rectrace/src/test/java/com/citi/gru/rectrace/service/v4/SqlQueryServiceV4Test.java`.
  </acceptance_criteria>
  <done>
    Two more test files compile; smoke script exists, is executable, and fails clean when target is down. All Wave 0 scaffolding in place.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → production runtime | Test scaffolding compiles against not-yet-existing production classes; stub patterns must not leak into production code paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T (Tampering) | SqlShapeValidator test stubs | mitigate | Stubs live only inside test classes; Wave 4 plan asserts no `SqlShapeValidator` stub remains by running `grep -L "private static void validate" backend/rectrace/src/test/...` after import switch. |
| T-05-02 | D (Denial of Service) | smoke-sql-search.sh against wrong host | accept | `curl -fsS` fails fast; documented FAIL message; no retry loop. |
</threat_model>

<verification>
- `cd backend/rectrace && mvn -q -DskipTests test-compile` green.
- `cd backend/rectrace && mvn -q test -Dtest='Sql*Test,ReadonlyDataSourceConfigTest' -DfailIfNoTests=false` — all five test classes report as run but skipped (Disabled), zero failures.
- `bash -n scripts/smoke-sql-search.sh && test -x scripts/smoke-sql-search.sh`.
- `BASE_URL=http://localhost:9 scripts/smoke-sql-search.sh; test $? -ne 0` — script exits non-zero when target unreachable.
</verification>

<success_criteria>
- 5 JUnit 5 test files exist at declared paths, all compile.
- 1 bash smoke script exists at `scripts/smoke-sql-search.sh`, is executable, syntactically valid, and fails clean when target is offline.
- Every test method carries a `@Disabled("Wave N: ...")` annotation grep-matchable for downstream waves to enable.
- `mvn -q -DskipTests test-compile` from `backend/rectrace` exits 0.
- No production code in `src/main/` modified.
</success_criteria>

<output>
After completion, create `.planning/phases/05-config-driven-select/05-01-SUMMARY.md` recording test file paths, exact `@Disabled` marker counts, and the smoke-script behavior under the `localhost:9` (unreachable) probe.
</output>
