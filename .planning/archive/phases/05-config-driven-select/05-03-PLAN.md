---
phase: 05-config-driven-select
plan: 03
type: execute
wave: 2
depends_on: [05-02]
files_modified:
  - backend/rectrace/pom.xml
  - backend/rectrace/src/main/java/com/citi/gru/rectrace/config/ReadonlyDataSourceConfig.java
autonomous: true
requirements: [SQL-03, SQL-04]
must_haves:
  truths:
    - "pom.xml declares com.github.jsqlparser:jsqlparser:5.3 exactly (not a version range, not 5.x)"
    - "mvn dependency:tree shows jsqlparser:5.3 on the compile classpath; no other major version pulled transitively"
    - "ReadonlyDataSourceConfig.java is @Profile(\"!test\") and exposes two beans: `readonlyDataSource` (HikariDataSource) + `readonlyJdbcTemplate` (JdbcTemplate wrapping it)"
    - "readonlyJdbcTemplate bean is constructed with new JdbcTemplate(ds) and NEVER has setQueryTimeout / setFetchSize / setMaxRows called on it during initialization (SQL-04 contract: caps are per-statement)"
    - "Application starts with `--spring.profiles.active=local` and connects to local Oracle as rectrace_readonly; ContextLoadsTest still passes under `test` profile (proves @Profile guard)"
  artifacts:
    - path: "backend/rectrace/pom.xml"
      provides: "com.github.jsqlparser:jsqlparser dependency at version 5.3"
      contains: "<artifactId>jsqlparser</artifactId>"
    - path: "backend/rectrace/src/main/java/com/citi/gru/rectrace/config/ReadonlyDataSourceConfig.java"
      provides: "readonlyDataSource + readonlyJdbcTemplate beans"
      exports: ["readonlyDataSource", "readonlyJdbcTemplate"]
      min_lines: 60
  key_links:
    - from: "ReadonlyDataSourceConfig"
      to: "application-local.properties datasource.readonly.* keys"
      via: "@Value injection"
      pattern: "@Value\\(\"\\$\\{datasource\\.readonly\\.(url|username|password|driver-class-name|service-name|db-schema)"
    - from: "ReadonlyDataSourceConfig"
      to: "ScriptExecutor (Citi-VM password fallback)"
      via: "ScriptExecutor.executeScript(...) when ${datasource.readonly.password:} is blank"
      pattern: "scriptExecutor.executeScript|new ScriptExecutor"
---

<objective>
Add JSqlParser 5.3 as a single pom dependency, then ship the `ReadonlyDataSourceConfig` clone of `AutosysDataSourceConfig`. Two beans land: `readonlyDataSource` (HikariCP, `rectrace_readonly` user, pool size 5) and `readonlyJdbcTemplate` (plain wrapper — no setters called per SQL-04). Both gated `@Profile("!test")` so `ContextLoadsTest` stays green.

Purpose: SQL-03 requires a dedicated read-only `DataSource` bean separate from the primary. Wave 2 lights the connection up against the rectrace_readonly Oracle user created in Wave 1, and brings the JSqlParser dependency in so Wave 3 can implement the validator against a real classpath.
Output: 1 pom edit + 1 net-new Java config class.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-config-driven-select/05-CONTEXT.md
@.planning/phases/05-config-driven-select/05-RESEARCH.md
@backend/rectrace/pom.xml
@backend/rectrace/src/main/java/com/citi/gru/rectrace/config/AutosysDataSourceConfig.java
@backend/rectrace/src/main/java/com/citi/gru/rectrace/config/DataSourceConfig.java
@backend/rectrace/src/main/java/com/citi/gru/rectrace/util/ScriptExecutor.java
@backend/rectrace/src/test/java/com/citi/gru/rectrace/config/ReadonlyDataSourceConfigTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JSqlParser 5.3 to pom.xml</name>
  <read_first>
    - backend/rectrace/pom.xml (locate the `<dependencies>` block; add the new dep grouped with other third-party non-Spring dependencies, e.g. after the ojdbc8 + oracle.database.security cluster, before spring-boot-starter-test)
    - .planning/phases/05-config-driven-select/05-RESEARCH.md §Standard Stack / Core
  </read_first>
  <action>
    Edit `backend/rectrace/pom.xml`. Add a single `<dependency>` block inside the existing `<dependencies>` element:
      - groupId: `com.github.jsqlparser`
      - artifactId: `jsqlparser`
      - version: `5.3` (literal — not `${...}`, not 5.x, not a range)
    Place the block after the oracle security artifacts and before the lombok block (or wherever third-party deps cluster — keep it visually adjacent to ojdbc8 since it's a peer SQL-tooling dep). Do NOT add JSqlParser anywhere else, do NOT introduce a `<properties>` version variable.

    Run `mvn -q dependency:resolve` after the edit to confirm the dep downloads. Run `mvn -q dependency:tree | grep jsqlparser` and confirm exactly one occurrence at `5.3`.
  </action>
  <verify>
    <automated>cd backend/rectrace && grep -A2 "<artifactId>jsqlparser</artifactId>" pom.xml | grep -q "<version>5.3</version>" && mvn -q dependency:tree -DincludeArtifactIds=jsqlparser | grep -q "jsqlparser:jar:5.3" && echo "OK jsqlparser 5.3 resolved"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<artifactId>jsqlparser</artifactId>" backend/rectrace/pom.xml` returns exactly 1.
    - `grep -B1 -A2 "<artifactId>jsqlparser</artifactId>" backend/rectrace/pom.xml | grep -c "<version>5.3</version>"` returns 1 (version is literally `5.3`).
    - `cd backend/rectrace && mvn -q dependency:tree -DincludeArtifactIds=jsqlparser` prints `com.github.jsqlparser:jsqlparser:jar:5.3:compile` exactly once.
    - `cd backend/rectrace && mvn -q -DskipTests compile` exits 0.
    - `cd backend/rectrace && javap -cp $(mvn -q dependency:build-classpath -Dmdep.outputFile=/dev/stdout 2>/dev/null | tail -1) net.sf.jsqlparser.parser.CCJSqlParserUtil` exits 0 (the parser class is on the compile classpath and resolves to the 5.x package).
  </acceptance_criteria>
  <done>
    JSqlParser 5.3 on the compile classpath; pom.xml has exactly one jsqlparser dep; mvn build still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: ReadonlyDataSourceConfig — readonlyDataSource + readonlyJdbcTemplate beans (@Profile("!test"))</name>
  <read_first>
    - backend/rectrace/src/main/java/com/citi/gru/rectrace/config/AutosysDataSourceConfig.java (clone-and-edit template; mirror @Value bindings, HikariConfig setup, bean naming style)
    - backend/rectrace/src/main/java/com/citi/gru/rectrace/config/DataSourceConfig.java (the BOOT-08 fix lives here — dual-source password closure: property first, ScriptExecutor fallback. MUST mirror exactly because the property may be blank in production and non-blank in local-dev)
    - backend/rectrace/src/main/java/com/citi/gru/rectrace/util/ScriptExecutor.java (signature of executeScript — confirm method signature `String executeScript(String scriptPath, String... args)`)
    - .planning/phases/05-config-driven-select/05-RESEARCH.md §Pattern 4 (Read-only DataSource wiring) — full code example
    - backend/rectrace/src/test/java/com/citi/gru/rectrace/config/ReadonlyDataSourceConfigTest.java (the disabled test from Plan 01 — its @Test `beanExistsUnderNonTestProfile()` documents the contract; enable the test in this plan)
  </read_first>
  <action>
    Create `backend/rectrace/src/main/java/com/citi/gru/rectrace/config/ReadonlyDataSourceConfig.java`. Package `com.citi.gru.rectrace.config`. Annotations: `@Configuration` + `@Profile("!test")` + `@Slf4j` (Lombok — matches V4 convention).

    `@Value` injected fields (use the exact property names landed in Plan 02):
      - `String url = ${datasource.readonly.url}`
      - `String username = ${datasource.readonly.username}`
      - `String password = ${datasource.readonly.password:}` (empty default so Citi-VM uses ScriptExecutor)
      - `String driverClassName = ${datasource.readonly.driver-class-name}`
      - `String serviceName = ${datasource.readonly.service-name}`
      - `String dbSchema = ${datasource.readonly.db-schema}`
      - `int maxPoolSize = ${datasource.readonly.hikari.maximum-pool-size:5}`
      - `int minIdle = ${datasource.readonly.hikari.minimum-idle:1}`
      - `long connectionTimeout = ${datasource.readonly.hikari.connection-timeout:20000}`

    `@Bean(name = "readonlyDataSource") public DataSource readonlyDataSource()`:
      - Resolve password: if `password != null && !password.isBlank()` use it; else `new ScriptExecutor().executeScript("/opt/rectify/control/scripts/get_password.sh", serviceName.toUpperCase(), dbSchema.toUpperCase())`. Mirror `DataSourceConfig.java`'s exact closure (the Phase-1 BOOT-08 fix).
      - Build `HikariConfig`: setJdbcUrl(url), setUsername(username), setPassword(pwd), setDriverClassName(driverClassName), setMaximumPoolSize(maxPoolSize), setMinimumIdle(minIdle), setConnectionTimeout(connectionTimeout), setPoolName("Rectrace-Readonly-HikariCP"), setReadOnly(true).
      - addDataSourceProperty("oracle.jdbc.ReadTimeout", "60000"); addDataSourceProperty("oracle.net.CONNECT_TIMEOUT", "10000").
      - Return `new HikariDataSource(cfg)`. Log info "Read-only DataSource initialized (pool={}, user={})" with maxPoolSize and username.

    `@Bean(name = "readonlyJdbcTemplate") public JdbcTemplate readonlyJdbcTemplate(@Qualifier("readonlyDataSource") DataSource ds)`:
      - Return `new JdbcTemplate(ds)`. NO calls to `setQueryTimeout`, `setFetchSize`, or `setMaxRows` on the template. Add a Javadoc comment: `/** SQL-04: the singleton template is deliberately bare. Per-statement caps live in SqlQueryServiceV4 via StatementCallback. */`.

    Imports: `javax.sql.DataSource`, `org.springframework.beans.factory.annotation.Value`, `org.springframework.beans.factory.annotation.Qualifier`, `org.springframework.context.annotation.Bean`, `org.springframework.context.annotation.Configuration`, `org.springframework.context.annotation.Profile`, `org.springframework.jdbc.core.JdbcTemplate`, `com.zaxxer.hikari.HikariConfig`, `com.zaxxer.hikari.HikariDataSource`, `lombok.extern.slf4j.Slf4j`, `com.citi.gru.rectrace.util.ScriptExecutor`.

    Then enable the previously-disabled `ReadonlyDataSourceConfigTest.beanExistsUnderNonTestProfile()`: remove the `@Disabled("Wave 2: ...")` annotation (or change to commented-out marker). The test asserts that under @ActiveProfiles("test"), the `readonlyDataSource` bean is absent (`ctx.containsBean("readonlyDataSource")` is false) — proving the @Profile guard works and ContextLoadsTest is unaffected.

    Live boot probe (executor runs after the edit): `cd backend/rectrace && mvn -q spring-boot:run -Dspring-boot.run.profiles=local &` then poll `curl -fsS http://localhost:6088/rectrace/actuator/health` until 200 (max 60s). Spring logs MUST include "Read-only DataSource initialized (pool=5, user=rectrace_readonly)" (or whatever info log statement was used). Kill the app via `pkill -f rectrace` after probe.
  </action>
  <verify>
    <automated>cd backend/rectrace && mvn -q -DskipTests compile && mvn -q test -Dtest='ReadonlyDataSourceConfigTest' -DfailIfNoTests=false && grep -q "@Profile(\"!test\")" src/main/java/com/citi/gru/rectrace/config/ReadonlyDataSourceConfig.java && grep -q "setQueryTimeout\\|setFetchSize\\|setMaxRows" src/main/java/com/citi/gru/rectrace/config/ReadonlyDataSourceConfig.java && { echo "FAIL: template setters present"; exit 1; } || echo "OK no template setters"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `backend/rectrace/src/main/java/com/citi/gru/rectrace/config/ReadonlyDataSourceConfig.java`.
    - `grep -q "@Profile(\"!test\")" src/main/java/com/citi/gru/rectrace/config/ReadonlyDataSourceConfig.java` matches.
    - `grep -c "@Bean(name = \"readonly" src/main/java/com/citi/gru/rectrace/config/ReadonlyDataSourceConfig.java` returns 2 (two beans named with prefix `readonly`).
    - Critical negative gate: `grep -E "readonlyJdbcTemplate.*\\.(setQueryTimeout|setFetchSize|setMaxRows)|\\.setQueryTimeout\\(|\\.setFetchSize\\(|\\.setMaxRows\\(" src/main/java/com/citi/gru/rectrace/config/ReadonlyDataSourceConfig.java | grep -v "^\\s*//" | grep -v "^\\s*\\*"` returns NOTHING (no setter calls outside comments). If this fails the implementation has violated SQL-04 contract.
    - `grep -q "new ScriptExecutor()" src/main/java/com/citi/gru/rectrace/config/ReadonlyDataSourceConfig.java` matches (BOOT-08 dual-source pattern preserved).
    - `grep -q "setReadOnly(true)" src/main/java/com/citi/gru/rectrace/config/ReadonlyDataSourceConfig.java` matches (Hikari read-only hint per Research Pattern 4).
    - `cd backend/rectrace && mvn -q test -Dtest='ReadonlyDataSourceConfigTest'` exits 0; the previously-Disabled test is now enabled and green.
    - `cd backend/rectrace && mvn -q test -Dtest='ContextLoadsTest'` exits 0 — proves the @Profile("!test") guard keeps the new bean out of the test context (otherwise the test would fail trying to reach Oracle).
    - Live probe (executor runs): with Docker stack up, `mvn spring-boot:run -Dspring-boot.run.profiles=local` boots, `/actuator/health` returns 200, log line `Read-only DataSource initialized (pool=5, user=rectrace_readonly)` (or equivalent info log) present in output. Capture the log line in the SUMMARY.md.
  </acceptance_criteria>
  <done>
    Two beans (`readonlyDataSource`, `readonlyJdbcTemplate`) land under `@Profile("!test")`. No setter calls on the template. ContextLoadsTest still green. App boots with profile=local against the rectrace_readonly user.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spring bean → Oracle | New connection pool authenticates as rectrace_readonly; HikariCP setReadOnly(true) is a hint, not a hard wall (the structural defense is the DB-side grant). |
| Property resolution → password literal | Local-dev plaintext, production ScriptExecutor — same dual-source as primary DataSource. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | T (Tampering) | Mutating readonlyJdbcTemplate singleton with setQueryTimeout/setFetchSize/setMaxRows | mitigate | Acceptance criteria includes a negative grep that fails the task if any setter is called on the template bean. SQL-04 is the contract; this is its first defense layer. |
| T-05-07 | S (Spoofing) | Wrong credentials accidentally bound from property defaults | mitigate | Empty default on password only (`${datasource.readonly.password:}`); url/username/driver/service-name have NO defaults — boot fails with `Could not resolve placeholder` if Plan 02 didn't ship the props. |
| T-05-08 | D (Denial of Service) | Connection pool exhaustion | accept | maxPoolSize=5 is intentionally small; SQL tabs are dev/admin-authored low-volume. Phase 7 will revisit pool tuning under load. |
</threat_model>

<verification>
- `mvn -q dependency:tree -DincludeArtifactIds=jsqlparser` shows exactly `jsqlparser:jar:5.3:compile`.
- `mvn -q -DskipTests compile` green.
- `mvn -q test -Dtest='ContextLoadsTest,ReadonlyDataSourceConfigTest'` green.
- Live boot probe: app starts under `spring-boot.run.profiles=local`, `/actuator/health` returns 200, log line confirming readonlyDataSource init present.
- Negative grep: zero setter calls on readonlyJdbcTemplate (SQL-04 contract).
</verification>

<success_criteria>
- pom.xml carries `com.github.jsqlparser:jsqlparser:5.3` exactly once.
- ReadonlyDataSourceConfig.java exists, is `@Profile("!test")`, exports `readonlyDataSource` + `readonlyJdbcTemplate`.
- No `setQueryTimeout/setFetchSize/setMaxRows` call on the template bean in any production code (grep verifies).
- App boots with `spring-boot.run.profiles=local` and connects as rectrace_readonly.
- ContextLoadsTest still green (proves @Profile guard).
- ReadonlyDataSourceConfigTest now green (test class enabled — Plan 01's `@Disabled("Wave 2: ...")` removed).
</success_criteria>

<output>
After completion, create `.planning/phases/05-config-driven-select/05-03-SUMMARY.md` recording: the pom delta (diff), the new config class summary (bean names + profile guard), the live boot probe log snippet showing successful Oracle connection, and the exact `grep -c` count of `setQueryTimeout|setFetchSize|setMaxRows` in the new file (must be 0 outside comments).
</output>
