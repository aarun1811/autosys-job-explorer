---
phase: 03-react-search-vertical-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-react/src/search/types.ts
  - frontend-react/src/search/hooks/useSearchConfig.ts
  - frontend-react/src/search/__tests__/types.test.ts
  - frontend-react/src/search/__tests__/useSearchConfig.test.ts
autonomous: true
requirements: [SEARCH-01]
tags: [react, zod, tanstack-query, types, config-driven]
must_haves:
  truths:
    - "Zod schemas mirror the backend SSRMRequestV4 / SearchConfigurationV4 / InitialSearchResponseV4 DTO shapes"
    - "useSearchConfig() returns a TanStack Query result against /rectrace/api/v4/search/config with staleTime: Infinity"
    - "Zod parse fails fast on malformed config (test covers this)"
  artifacts:
    - path: "frontend-react/src/search/types.ts"
      provides: "ColumnDefinitionV4Schema, CategoryConfigV4Schema, SearchConfigurationV4Schema, InitialFilterSchema, InitialSearchResponseV4Schema, SSRMRequestV4Schema, SortModelItemSchema, plus z.infer types"
      contains: "z.object"
    - path: "frontend-react/src/search/hooks/useSearchConfig.ts"
      provides: "useSearchConfig() hook"
      exports: ["useSearchConfig"]
    - path: "frontend-react/src/search/__tests__/types.test.ts"
      provides: "Zod parse tests for fileName JSON sample"
    - path: "frontend-react/src/search/__tests__/useSearchConfig.test.ts"
      provides: "TanStack Query hook test (mocked apiFetch)"
  key_links:
    - from: "frontend-react/src/search/hooks/useSearchConfig.ts"
      to: "frontend-react/src/search/types.ts"
      via: "SearchConfigurationV4Schema.parse() in queryFn"
      pattern: "SearchConfigurationV4Schema\\.parse"
    - from: "frontend-react/src/search/hooks/useSearchConfig.ts"
      to: "frontend-react/src/lib/queryClient.ts"
      via: "apiFetch + TanStack Query queryCache onError (auto Sonner)"
      pattern: "apiFetch\\('/rectrace/api/v4/search/config'\\)"
---

<objective>
Establish the typed foundation for Phase 3: Zod schemas mirroring backend V4 DTOs, and the `useSearchConfig` TanStack Query hook that drives all config-driven grid behavior. Every downstream Phase 3 file imports types from here.

Purpose: SEARCH-01 (config-driven search) requires a single source of truth for the JSON contract between backend `/api/v4/search/config` and the React app. Zod gives runtime parse safety; `z.infer` gives TypeScript types — one definition, two guarantees.

Output: `frontend-react/src/search/types.ts`, `frontend-react/src/search/hooks/useSearchConfig.ts`, two Vitest test files.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-react-search-vertical-slice/03-CONTEXT.md
@.planning/phases/03-react-search-vertical-slice/03-RESEARCH.md
@.planning/phases/03-react-search-vertical-slice/03-PATTERNS.md

# Backend DTO source of truth
@backend/rectrace/src/main/java/com/citi/gru/rectrace/dto/v4/SSRMRequestV4.java
@backend/rectrace/src/main/resources/search-config-v4.json

# Angular reference for interface shapes
@frontend/rectrace/src/app/services/search-v5.service.ts

# Phase 2 idioms to reuse
@frontend-react/src/lib/queryClient.ts
@frontend-react/src/lib/queryClient.test.ts

<interfaces>
<!-- The exact identifiers Phase 3 downstream plans will import from types.ts -->
<!-- Names MUST match these — downstream plans hard-reference them. -->

From frontend-react/src/search/types.ts (this plan creates):
- Schema exports:
  - ColumnDefinitionV4Schema
  - CategoryConfigV4Schema
  - SearchConfigurationV4Schema
  - InitialFilterSchema (shape: { column: string, values: string[] })
  - CategoryResultV4Schema (shape: { category: string, initialFilter: InitialFilter })
  - InitialSearchResponseV4Schema (shape: { categoryResults: Record<string, CategoryResultV4> } — mirror search-v5.service.ts lines 37-50)
  - SortModelItemSchema (shape: { colId: string, sort: 'asc' | 'desc' })
  - SSRMRequestV4Schema (shape per SSRMRequestV4.java)
- Inferred type exports (via z.infer):
  - ColumnDefinitionV4
  - CategoryConfigV4
  - SearchConfigurationV4
  - InitialFilter
  - InitialSearchResponseV4
  - SSRMRequestV4

From frontend-react/src/search/hooks/useSearchConfig.ts (this plan creates):
- export function useSearchConfig(): UseQueryResult<SearchConfigurationV4>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zod schemas for V4 search DTOs in search/types.ts</name>
  <files>frontend-react/src/search/types.ts, frontend-react/src/search/__tests__/types.test.ts</files>
  <read_first>
    - frontend-react/src/search/types.ts (verify it does not yet exist; if exists, read before editing)
    - backend/rectrace/src/main/resources/search-config-v4.json (lines 1-50 — fileName entry is the canonical schema instance)
    - backend/rectrace/src/main/java/com/citi/gru/rectrace/dto/v4/SSRMRequestV4.java (full file — DTO fields)
    - frontend/rectrace/src/app/services/search-v5.service.ts (lines 6-92 — TypeScript interfaces to translate to Zod)
    - .planning/phases/03-react-search-vertical-slice/03-PATTERNS.md (§"search/types.ts" — exact Zod shapes to author)
    - .planning/phases/03-react-search-vertical-slice/03-RESEARCH.md (§1 lines 263-293 — canonical Zod port)
    - frontend-react/src/lib/queryClient.test.ts (Vitest idiom — mock fetch, describe/test blocks)
  </read_first>
  <behavior>
    - Test: ColumnDefinitionV4Schema.parse() accepts the `execution_order` column object from search-config-v4.json (includes cellStyle with kebab-case keys, pinned: 'right', sortable: false, filter: false, cellRendererParams: { jobNameField: 'load_job' })
    - Test: ColumnDefinitionV4Schema rejects missing required field (e.g., absent `field` or `headerName`)
    - Test: CategoryConfigV4Schema.parse() accepts the full `fileName` entry from search-config-v4.json (with `key: 'fileName'`, columns array, searchColumn, elasticsearch, oracle subtrees)
    - Test: SearchConfigurationV4Schema.parse() accepts `{ categories: [<fileName entry>] }` and yields `result.categories[0].key === 'fileName'`
    - Test: SSRMRequestV4Schema.parse() accepts a body with category, initialFilter ({ column, values }), rowGroupCols (string[]), groupKeys (string[]), startRow, endRow, sortModel (SortModelItem[]), filterModel (Record<string, unknown>), visibleColumns (string[])
    - Test: InitialSearchResponseV4Schema.parse() accepts `{ categoryResults: { fileName: { category: 'fileName', initialFilter: { column: 'file_name_pattern', values: ['x.csv'] } } } }`
    - Test: z.infer types are exported (compile-time assertion via `const _t: SearchConfigurationV4 = result` in test fixture).
  </behavior>
  <action>
    Create frontend-react/src/search/types.ts exporting the 8 Zod schemas listed in the &lt;interfaces&gt; block above. Use the canonical shapes from 03-PATTERNS.md §"search/types.ts" (lines 800-833) verbatim — they were derived from Angular search-v5.service.ts (lines 22-92) which mirrors the backend DTOs. ColumnDefinitionV4Schema fields: field (string), headerName (string), rowGroup (boolean optional), hide (boolean optional), sortable (boolean optional), filter (boolean optional), resizable (boolean optional), width (number optional), cellRenderer (string optional), cellRendererParams (z.record(z.unknown()) optional), cellStyle (z.record(z.string()) optional — kebab-case keys allowed at parse time; adapter converts in Plan 04), pinned (z.enum(['left','right']).nullable().optional()). CategoryConfigV4Schema: key (string), label (string optional), searchColumn (string optional), elasticsearch (z.record(z.unknown())), oracle (z.record(z.unknown())), columns (z.array(ColumnDefinitionV4Schema)). SearchConfigurationV4Schema: categories (z.array(CategoryConfigV4Schema)). InitialFilterSchema: column (string), values (z.array(z.string())). CategoryResultV4Schema: category (string), initialFilter (InitialFilterSchema). InitialSearchResponseV4Schema: categoryResults (z.record(CategoryResultV4Schema)). SortModelItemSchema: colId (string), sort (z.enum(['asc','desc'])). SSRMRequestV4Schema: category (string), initialFilter (InitialFilterSchema.nullable()), rowGroupCols (z.array(z.string())), groupKeys (z.array(z.string())), startRow (z.number()), endRow (z.number()), sortModel (z.array(SortModelItemSchema)), filterModel (z.record(z.unknown())), visibleColumns (z.array(z.string())). Export inferred types via `export type X = z.infer&lt;typeof XSchema&gt;`. Co-locate Zod schemas in search/types.ts per Claude's discretion in CONTEXT.md (avoid src/lib/schemas.ts split). NO React, NO AG-Grid imports in this file.

    Create frontend-react/src/search/__tests__/types.test.ts. Import every schema. Use describe('search/types Zod schemas', ...) blocks with the behavior assertions above. Embed the fileName fixture inline as a JSON literal copied verbatim from search-config-v4.json (escape JSON keys correctly — particularly `align-items` in cellStyle). Use Vitest's `test`/`expect` idiom from queryClient.test.ts. NO mocks needed — Zod is pure.
  </action>
  <verify>
    <automated>cd frontend-react && npx vitest run src/search/__tests__/types.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File frontend-react/src/search/types.ts exists.
    - Source assertion: `grep -c "^export const.*Schema = z\." frontend-react/src/search/types.ts` returns 8.
    - Source assertion: `grep -c "^export type" frontend-react/src/search/types.ts` returns at least 6.
    - Source assertion: `grep -v '^//' frontend-react/src/search/types.ts | grep -c "from 'react'"` returns 0 (no React import).
    - Source assertion: `grep -v '^//' frontend-react/src/search/types.ts | grep -c "from 'ag-grid"` returns 0 (no AG-Grid import).
    - Behavior: All Zod parse tests pass under `npx vitest run src/search/__tests__/types.test.ts`.
    - Behavior: `npx vitest run src/search/__tests__/types.test.ts` reports >= 6 passing tests.
    - Behavior: `npx tsc --noEmit -p frontend-react/tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>
    types.ts exports the 8 Zod schemas + inferred types listed in &lt;interfaces&gt;; all Zod parse tests pass; downstream plans can import `import type { CategoryConfigV4, SearchConfigurationV4, ColumnDefinitionV4, InitialSearchResponseV4, SSRMRequestV4 } from '@/search/types'`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useSearchConfig TanStack Query hook</name>
  <files>frontend-react/src/search/hooks/useSearchConfig.ts, frontend-react/src/search/__tests__/useSearchConfig.test.ts</files>
  <read_first>
    - frontend-react/src/lib/queryClient.ts (apiFetch + QueryCache onError wiring)
    - frontend-react/src/lib/queryClient.test.ts (mock-fetch pattern lines 7-15 — vi.stubGlobal('fetch', mockFetch))
    - frontend-react/src/search/types.ts (just created in Task 1 — imports SearchConfigurationV4Schema)
    - frontend/rectrace/src/app/services/search-v5.service.ts (lines 118-122 — Angular getConfiguration())
    - .planning/phases/03-react-search-vertical-slice/03-PATTERNS.md (§"useSearchConfig.ts" lines 557-592)
    - .planning/phases/03-react-search-vertical-slice/03-RESEARCH.md (§1 lines 193-210)
  </read_first>
  <behavior>
    - Test: useSearchConfig() calls apiFetch with path `/rectrace/api/v4/search/config` (mock apiFetch, assert path)
    - Test: useSearchConfig() returns parsed SearchConfigurationV4 on success (mock fetch returns the fileName JSON fixture)
    - Test: useSearchConfig() Zod-rejects malformed response (mock returns `{ wrong: true }` → hook surfaces ZodError; QueryCache.onError → reportRequestFailure auto-fires Sonner — assert via mocked toast)
    - Test: queryKey is `['search-config']`
    - Test: staleTime is Infinity (assert option passed)
  </behavior>
  <action>
    Create frontend-react/src/search/hooks/useSearchConfig.ts. Export `useSearchConfig()` that calls `useQuery&lt;SearchConfigurationV4&gt;({ queryKey: ['search-config'], queryFn, staleTime: Infinity, gcTime: Infinity })`. The queryFn does: `const res = await apiFetch('/rectrace/api/v4/search/config'); const json = await res.json(); return SearchConfigurationV4Schema.parse(json);`. Import `useQuery` from `@tanstack/react-query`, `apiFetch` from `@/lib/queryClient`, and `SearchConfigurationV4Schema, SearchConfigurationV4` from `@/search/types`. No try/catch — TanStack Query's QueryCache.onError handler (wired in queryClient.ts line 53 per Phase 2) auto-routes failures through reportRequestFailure → Sonner.

    Create frontend-react/src/search/__tests__/useSearchConfig.test.ts. Wrap renderHook in a QueryClientProvider (use a fresh QueryClient per test; `retry: false` so failures surface synchronously). Mock `apiFetch` via `vi.mock('@/lib/queryClient', () =&gt; ({ apiFetch: vi.fn(), reportRequestFailure: vi.fn() }))`. Use @testing-library/react `renderHook` + `waitFor` for query state. Tests per &lt;behavior&gt; above. Reuse the fileName JSON fixture pattern from types.test.ts (inline literal) for the success-case mock response.
  </action>
  <verify>
    <automated>cd frontend-react && npx vitest run src/search/__tests__/useSearchConfig.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File frontend-react/src/search/hooks/useSearchConfig.ts exists.
    - Source assertion: `grep -c "queryKey: \['search-config'\]" frontend-react/src/search/hooks/useSearchConfig.ts` returns 1.
    - Source assertion: `grep -c "staleTime: Infinity" frontend-react/src/search/hooks/useSearchConfig.ts` returns 1.
    - Source assertion: `grep -c "SearchConfigurationV4Schema\.parse" frontend-react/src/search/hooks/useSearchConfig.ts` returns 1.
    - Source assertion: `grep -c "/rectrace/api/v4/search/config" frontend-react/src/search/hooks/useSearchConfig.ts` returns 1.
    - Source assertion: `grep -c "try\|catch" frontend-react/src/search/hooks/useSearchConfig.ts` returns 0 (rely on QueryCache.onError).
    - Behavior: `npx vitest run src/search/__tests__/useSearchConfig.test.ts` reports >= 4 passing tests.
    - Behavior: `npx tsc --noEmit -p frontend-react/tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>
    useSearchConfig() hook ships; SearchGrid (Plan 05) can call `useSearchConfig().data?.categories.find(c =&gt; c.key === cat)` to get config-driven columns. Errors auto-surface via Sonner toast with correlation ID.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → React (JSON response from /api/v4/search/config) | Untrusted data crosses here; Zod parse is the validation gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Tampering | search-config response | mitigate | SearchConfigurationV4Schema.parse() rejects malformed payloads at the trust boundary; failure surfaces via TanStack Query QueryCache.onError → reportRequestFailure → Sonner toast with correlation ID. |
| T-03.1-02 | Information disclosure | error path | mitigate | apiFetch attaches 32-hex correlationId to thrown Error (Phase 2 invariant); reportRequestFailure renders the ID in Sonner. No PII or stack-traces leaked to UI. |
| T-03.1-03 | Spoofing | n/a | accept | Phase 9 SEC-01 owns auth filter; Phase 3 inherits Phase 1 D-1.8 permit-all SecurityFilterChain. |
</threat_model>

<verification>
- `cd frontend-react && npx vitest run src/search/__tests__/` — all tests pass
- `cd frontend-react && npx tsc --noEmit` — no TS errors
- `cd frontend-react && npx eslint src/search/` — no lint errors
- Source grep gates from each task's `<acceptance_criteria>` block return expected counts
</verification>

<success_criteria>
- search/types.ts ships 8 Zod schemas + 6+ inferred types
- search/hooks/useSearchConfig.ts ships useSearchConfig() hook
- Two Vitest test files green
- TypeScript clean, ESLint clean
- Foundation ready for Plans 02-08 to import from
</success_criteria>

<output>
After completion, create `.planning/phases/03-react-search-vertical-slice/03-01-SUMMARY.md` per template, listing the 8 exported schemas + 6+ inferred types as the public surface area downstream plans depend on.
</output>
