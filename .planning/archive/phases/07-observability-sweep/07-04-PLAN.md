---
phase: 07-observability-sweep
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - rectrace-tlm-stats/src/main/resources/logback-spring.xml
  - rectrace-tlm-stats/src/main/resources/application.properties
  - rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/observability/filter/UserIdMdcFilter.java
  - rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/observability/filter/AccessLogFilter.java
  - rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/observability/ActuatorSecurityConfig.java
  - rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/observability/health/HealthIndicatorDataSourceConfig.java
  - rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/observability/health/OracleHealthIndicator.java
  - rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/config/AsyncConfig.java
  - rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/AccessLogJsonShapeTest.java
  - rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/ActuatorHealthIntegrationTest.java
  - rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/ActuatorExposureTest.java
  - rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/PrometheusEndpointTest.java
  - rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/AsyncMdcPropagationTest.java
  - rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/health/OracleHealthIndicatorTest.java
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03, OBS-05, OBS-06, OBS-07]
tags: [observability, tlm-stats, mirror, logback, actuator, health-indicator]

must_haves:
  truths:
    - "rectrace-tlm-stats emits JSON logs in `prod` profile via the same logback-spring.xml template as backend/rectrace (parameterised by spring.application.name)."
    - "rectrace-tlm-stats `/actuator/` exposes EXACTLY the same five endpoints — health, info, prometheus, loggers, metrics."
    - "rectrace-tlm-stats `/actuator/health` aggregates ONE custom indicator: `oracle` (against `reconmgmt.datasource` per Open Q9). No ES, no loaderRunAge, no searchConfig (these subsystems don't exist in tlm-stats)."
    - "rectrace-tlm-stats has UserIdMdcFilter + AccessLogFilter mirroring backend/rectrace (same regex, same ordering)."
    - "rectrace-tlm-stats AsyncConfig wires ContextPropagatingTaskDecorator on its taskExecutor; if no AsyncConfig exists today, one is created with a minimal ThreadPoolTaskExecutor matching backend/rectrace's shape."
    - "rectrace-tlm-stats ActuatorSecurityConfig stub matches backend/rectrace's pattern (anonymous permit-all for Phase 7; Phase 9 hardens)."
    - "Six Wave-0 tests in tlm-stats are un-disabled and PASS."
  artifacts:
    - path: rectrace-tlm-stats/src/main/resources/logback-spring.xml
      provides: "profile-aware JSON encoder (copy of backend/rectrace version)"
      contains: "LogstashEncoder"
    - path: rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/observability/health/OracleHealthIndicator.java
      provides: "Oracle health probe against reconmgmt.datasource (the static one; per-TLM-instance datasources excluded)"
      contains: "AbstractHealthIndicator"
    - path: rectrace-tlm-stats/src/main/resources/application.properties
      provides: "actuator exposure allow-list mirroring backend/rectrace"
      contains: "management.endpoints.web.exposure.include=health,info,prometheus,loggers,metrics"
    - path: rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/config/AsyncConfig.java
      provides: "taskExecutor with ContextPropagatingTaskDecorator"
      contains: "ContextPropagatingTaskDecorator"
  key_links:
    - from: rectrace-tlm-stats/src/main/resources/logback-spring.xml
      to: spring.application.name parameter
      via: "springProperty rendering allows the same file in both modules"
      pattern: "springProperty.*spring\\.application\\.name"
    - from: rectrace-tlm-stats Oracle health indicator
      to: reconmgmt DataSource (existing bean from DatabaseConfig)
      via: "@Qualifier('reconmgmtDataSource') JdbcTemplate"
      pattern: "reconmgmt"
---

<objective>
Mirror the Phase 7 surface from `backend/rectrace` to `rectrace-tlm-stats` as the subset that makes sense for the second module. The mirror module has NO loader subsystem, NO Elasticsearch client, NO search-config services — so the OracleHealthIndicator is the ONLY custom health indicator; the logback config, filters, actuator lockdown, ActuatorSecurityConfig, AsyncConfig + ContextPropagatingTaskDecorator are ported verbatim (package prefix `com.citi.gru.rectrace.tlmstats.observability.*`). Enable six Wave-0 tests from Plan 01.

Per CONTEXT.md: "Both `backend/rectrace` AND `rectrace-tlm-stats` get the treatment (mirror module gets subset — no loader/no SQL search)." Per A3: slow-query aspect is intentionally NOT ported to tlm-stats in this plan — research flagged the parity question as a planner decision and we choose to ship the lean version. If TLM queries prove slow under prod load, a follow-up plan adds it; current decision is "don't ship parity for a non-evidence-based hypothetical."

Output: Eight new files in tlm-stats main + one edited config, six tests un-disabled.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-observability-sweep/07-CONTEXT.md
@.planning/phases/07-observability-sweep/07-RESEARCH.md
@.planning/phases/07-observability-sweep/07-01-SUMMARY.md
@.planning/phases/07-observability-sweep/07-02-SUMMARY.md
@.planning/phases/07-observability-sweep/07-03-SUMMARY.md
@rectrace-tlm-stats/src/main/resources/logback-spring.xml
@rectrace-tlm-stats/src/main/resources/application.properties
@backend/rectrace/src/main/resources/logback-spring.xml
@backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/filter/UserIdMdcFilter.java
@backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/filter/AccessLogFilter.java
@backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/ActuatorSecurityConfig.java
@backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/health/OracleHealthIndicator.java
@backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/health/HealthIndicatorDataSourceConfig.java
@backend/rectrace/src/main/java/com/citi/gru/rectrace/config/AsyncConfig.java

<interfaces>
<!-- rectrace-tlm-stats does NOT currently have an AsyncConfig.java (verified via grep). Plan 04 creates one. -->
<!-- rectrace-tlm-stats DatabaseConfig provides multiple DataSource beans: reconmgmtDataSource, recportalDataSource, plus dynamic per-TLM-instance datasources from tlm-instances.json. Only reconmgmtDataSource is used for the health indicator per Open Q9. -->
<!-- rectrace-tlm-stats logback-spring.xml today (verified): 10-line ConsoleAppender, identical structure to backend/rectrace pre-Phase-7. Plan 04 REPLACES it with the Plan 02 template, parameterised so the same file works for both modules. -->
<!-- TLM-stats package prefix: com.citi.gru.rectrace.tlmstats.*. The observability sub-package is com.citi.gru.rectrace.tlmstats.observability.*. -->
<!-- TLM-stats Spring Boot version: 3.5.14 (same as backend/rectrace per Phase 1). starter-aop dep added by Plan 01 (but slow-query aspect deliberately not ported per A3). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port logback-spring.xml + application.properties + filters + ActuatorSecurityConfig to tlm-stats</name>
  <files>rectrace-tlm-stats/src/main/resources/logback-spring.xml, rectrace-tlm-stats/src/main/resources/application.properties, rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/observability/filter/UserIdMdcFilter.java, rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/observability/filter/AccessLogFilter.java, rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/observability/ActuatorSecurityConfig.java, rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/AccessLogJsonShapeTest.java, rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/ActuatorExposureTest.java</files>
  <behavior>
- TLM-stats logback-spring.xml is byte-identical to backend/rectrace's Plan 02 output (since the template parameterises by `spring.application.name` via `springProperty`, both modules can use the same file content).
- TLM-stats application.properties has the same actuator exposure lockdown.
- TLM-stats GET `/actuator/health` produces an access log line with the same MDC keys (traceId, userId, path, method, status, durationMs).
- TLM-stats GET `/actuator/` lists exactly five endpoints, none excluded.
- TLM-stats ActuatorSecurityConfig grants anonymous access to all actuator endpoints (Phase 7 stub; matches Plan 02).
  </behavior>
  <action>
**File 1 — `rectrace-tlm-stats/src/main/resources/logback-spring.xml` (REPLACE):**
Copy the EXACT file content produced by Plan 02 (Task 1 File 1). The `springProperty` for `appName` reads `spring.application.name`, which differs between modules — that's the only per-module variance and it's handled by the spring property resolver at boot time. DO NOT duplicate the file with module-specific edits.

**File 2 — `rectrace-tlm-stats/src/main/resources/application.properties` (EDIT, append):**
Append the SAME actuator/observability block as Plan 02 (minus the loader-run-age group reference — tlm-stats has no loaderRunAge indicator):
```
management.endpoints.web.exposure.include=health,info,prometheus,loggers,metrics
management.endpoints.web.exposure.exclude=env,heapdump,shutdown,beans,configprops,threaddump,scheduledtasks
management.endpoint.health.show-details=when-authorized
management.endpoint.health.show-components=when-authorized
management.observations.annotations.enabled=true
management.endpoint.health.probes.enabled=true
```
Note: NO `management.endpoint.health.group.loader.include` line in tlm-stats — that's loader-specific. NO `observability.slow-query-threshold-ms` because the slow-query aspect is not being ported (A3 decision).

Also add `spring.application.name=rectrace-tlm-stats` if not already present (verify before adding — search Phase 0/1 properties).

**File 3 — `UserIdMdcFilter.java` (NEW):**
Copy the Plan 02 file VERBATIM but change the package declaration to `com.citi.gru.rectrace.tlmstats.observability.filter` and update any import that references `com.citi.gru.rectrace.*` (none — the Plan 02 file uses only Spring/SLF4J imports). The filter body is identical: same regex `^[A-Za-z0-9._-]{1,64}$`, same `@Order(Ordered.HIGHEST_PRECEDENCE + 100)`, same MDC.remove in finally.

**File 4 — `AccessLogFilter.java` (NEW):**
Same approach as File 3: package change only. Body verbatim. Same `@Order(Ordered.LOWEST_PRECEDENCE)`, same logger name `"access"`, same MDC keys (path/method/status/durationMs).

**File 5 — `ActuatorSecurityConfig.java` (NEW):**
Same approach: package `com.citi.gru.rectrace.tlmstats.observability`. Body verbatim — `@Order(Ordered.HIGHEST_PRECEDENCE)` `SecurityFilterChain` matching `EndpointRequest.toAnyEndpoint()` permitting all.

**Files 6-7 — Un-disable two Wave-0 tests:**
Remove `@Disabled` from `AccessLogJsonShapeTest.java` and `ActuatorExposureTest.java`.

Per `<scope_reduction_prohibition>`: tlm-stats gets the FULL filter+logback+actuator surface. No "tlm-stats can be a thinner version of the access log" — the access log shape is identical because Splunk ingest expects identical schemas across modules.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/autosys-job-explorer && mvn -pl rectrace-tlm-stats test -Dtest='AccessLogJsonShapeTest,ActuatorExposureTest' 2>&1 | tee /tmp/p7-04-t1.log | grep -E "Tests run: ([2-9]|[1-9][0-9]+).*Failures: 0.*Errors: 0" && diff <(grep -v "<!--" backend/rectrace/src/main/resources/logback-spring.xml) <(grep -v "<!--" rectrace-tlm-stats/src/main/resources/logback-spring.xml) | grep -v '^#' | wc -l | grep -qE "^[[:space:]]*[0-3]$"</automated>
  </verify>
  <done>logback-spring.xml in both modules is structurally identical (diff returns 0-3 differences max, all comments); both filters extend OncePerRequestFilter with mirror semantics; ActuatorSecurityConfig stub permits anonymous; both tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OracleHealthIndicator + HealthIndicatorDataSourceConfig + AsyncConfig + remaining tests</name>
  <files>rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/observability/health/HealthIndicatorDataSourceConfig.java, rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/observability/health/OracleHealthIndicator.java, rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/config/AsyncConfig.java, rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/ActuatorHealthIntegrationTest.java, rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/PrometheusEndpointTest.java, rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/AsyncMdcPropagationTest.java, rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/health/OracleHealthIndicatorTest.java</files>
  <behavior>
- `/actuator/health` on tlm-stats returns 200 with `$.components.oracle.status` present (anonymous summary returns only `{"status":"UP"}` — full components only when authorized per Plan 02 props).
- OracleHealthIndicator probes the `reconmgmt` Oracle datasource (NOT recportal, NOT dynamic per-TLM instances) — Open Q9.
- The healthCheckJdbcTemplate is a NEW JdbcTemplate wrapping `reconmgmtDataSource` with `setQueryTimeout(2)` — never mutates the existing reconmgmt template if one exists (Pitfall P-7).
- `@Async("taskExecutor")` invocation on tlm-stats propagates MDC.traceId via ContextPropagatingTaskDecorator. If tlm-stats had no taskExecutor bean today, Plan 04 creates one with the same shape as backend/rectrace.
- `/actuator/prometheus` on tlm-stats returns text/plain with `http_server_requests_seconds_count` + `jvm_memory_used_bytes`.
- Mocked-JdbcTemplate UP/DOWN paths in OracleHealthIndicatorTest behave identically to backend/rectrace's.
  </behavior>
  <action>
**File 1 — `HealthIndicatorDataSourceConfig.java` (NEW):**
`package com.citi.gru.rectrace.tlmstats.observability.health;`
`@Configuration @Profile("!test")` declaring `@Bean("healthCheckJdbcTemplate") JdbcTemplate healthCheckJdbcTemplate(@Qualifier("reconmgmtDataSource") DataSource ds)` — returns a NEW JdbcTemplate with `setQueryTimeout(2)`. The exact qualifier name must match the existing DatabaseConfig bean — `grep -n "@Bean.*DataSource" rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/config/DatabaseConfig.java` to confirm the bean name; adapt the qualifier accordingly.

**File 2 — `OracleHealthIndicator.java` (NEW):**
`package com.citi.gru.rectrace.tlmstats.observability.health;`
Identical body to backend/rectrace's OracleHealthIndicator: extends `AbstractHealthIndicator`, `@Component("oracle")`, constructor takes `@Qualifier("healthCheckJdbcTemplate") JdbcTemplate t`, `doHealthCheck` runs `SELECT 1 FROM DUAL` returning UP/DOWN per result.

**File 3 — `AsyncConfig.java` (NEW):**
`package com.citi.gru.rectrace.tlmstats.config;`
Verify first whether tlm-stats already has an AsyncConfig — `grep -rn "@EnableAsync\|ThreadPoolTaskExecutor" rectrace-tlm-stats/src/main/java`. If absent (expected), create one mirroring backend/rectrace's structure:
```
@Configuration
@EnableAsync
public class AsyncConfig {
    @Bean(name = "taskExecutor")
    public Executor taskExecutor() {
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        executor.setCorePoolSize(5);
        executor.setMaxPoolSize(10);
        executor.setQueueCapacity(50);
        executor.setThreadNamePrefix("TlmStatsAsync-");
        executor.setTaskDecorator(new ContextPropagatingTaskDecorator());
        executor.initialize();
        return executor;
    }
}
```
Pool sizes halved relative to backend/rectrace (TLM-stats has lighter load — no SSRM, no parallel ES). If an AsyncConfig already exists, ADD `setTaskDecorator(new ContextPropagatingTaskDecorator())` to the existing bean instead of creating a new file.

**Files 4-7 — Un-disable four Wave-0 tests:**
Remove `@Disabled` annotation from:
- `ActuatorHealthIntegrationTest.java`
- `PrometheusEndpointTest.java`
- `AsyncMdcPropagationTest.java`
- `OracleHealthIndicatorTest.java`

If a test scaffold from Plan 01 assumed the wrong datasource bean name (`reconmgmtDataSource` vs actual), update the test mock to match the actual bean name discovered during Task 2 — not the other way around.

Per `<scope_reduction_prohibition>`: deliver the OracleHealthIndicator against the REAL reconmgmt datasource (per Open Q9). Not "a stub indicator that returns UP" — the bean must actually probe Oracle.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/autosys-job-explorer && mvn -pl rectrace-tlm-stats test -Dtest='ActuatorHealthIntegrationTest,PrometheusEndpointTest,AsyncMdcPropagationTest,OracleHealthIndicatorTest' 2>&1 | tee /tmp/p7-04-t2.log | grep -E "Tests run: ([4-9]|[1-9][0-9]+).*Failures: 0.*Errors: 0" && grep -q "AbstractHealthIndicator" rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/observability/health/OracleHealthIndicator.java && grep -q "ContextPropagatingTaskDecorator" rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/config/AsyncConfig.java && grep -q "setQueryTimeout(2)" rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/observability/health/HealthIndicatorDataSourceConfig.java</automated>
  </verify>
  <done>OracleHealthIndicator extends AbstractHealthIndicator and uses dedicated 2s-timeout JdbcTemplate; AsyncConfig wires ContextPropagatingTaskDecorator (creating bean if absent, editing if present); four tests un-disabled and green; full `mvn -pl rectrace-tlm-stats test` runs all six Phase-7 tlm-stats tests with Failures:0 Errors:0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tlm-stats client → /actuator/** | mirror of backend/rectrace boundary; same exposure rules apply |
| tlm-stats Oracle health probe → reconmgmt DataSource | dedicated JdbcTemplate avoids singleton mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-20 | Tampering | logback-spring.xml drift between modules — Splunk ingest could see two different JSON schemas | mitigate | Both modules use byte-identical logback-spring.xml; only `spring.application.name` (resolved at boot) differs |
| T-07-21 | Information Disclosure | tlm-stats also leaks `/actuator/env`/`/heapdump` if exposure not locked | mitigate | Same `exposure.include` allow-list + `exposure.exclude` list as backend/rectrace |
| T-07-22 | Denial of Service | Probing all 9 dynamic per-TLM-instance datasources from /actuator/health would amplify load to 9x and flap if any one TLM down | mitigate (by exclusion) | Per Open Q9, OracleHealthIndicator probes ONLY `reconmgmtDataSource`; per-TLM probes belong on a dedicated endpoint (out of scope for Phase 7) |
| T-07-23 | Information Disclosure | tlm-stats logs may now include reconmgmt loginIds in JSON shipped to Splunk | accept | LoginIds are internal-Citi user IDs (not PII); same trust as backend/rectrace (D-7.11) |
</threat_model>

<verification>
- `mvn -pl rectrace-tlm-stats test -Dtest='com.citi.gru.rectrace.tlmstats.observability.*'` → all 6 tests pass.
- `mvn -pl rectrace-tlm-stats -DskipTests package` → build succeeds.
- `diff backend/rectrace/src/main/resources/logback-spring.xml rectrace-tlm-stats/src/main/resources/logback-spring.xml` → only differences are comment lines or absent (target: structurally identical).
- Post-execute manual smoke: `curl -s http://localhost:8080/actuator/ | jq '._links | keys'` → exactly 5 endpoints + self.
- `grep -c "OracleHealthIndicator\|HealthIndicatorDataSourceConfig\|UserIdMdcFilter\|AccessLogFilter\|ActuatorSecurityConfig" rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/observability/*.java rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/observability/**/*.java | grep -vc ":0"` ≥ 5.
- Full multi-module suite green: `mvn test` from project root (both modules together) → 0 failures.
</verification>

<success_criteria>
- tlm-stats logback-spring.xml mirrors backend/rectrace's (parameterised by spring.application.name).
- tlm-stats application.properties has same exposure allow-list (sans loader group).
- tlm-stats has UserIdMdcFilter + AccessLogFilter + ActuatorSecurityConfig (subset mirror).
- tlm-stats has OracleHealthIndicator + HealthIndicatorDataSourceConfig (only this indicator, only reconmgmt DS).
- tlm-stats has AsyncConfig with ContextPropagatingTaskDecorator (created if absent).
- NO slow-query aspect / scheduled-trace aspect / ScriptExecutor edits in tlm-stats (per A3 + module's actual surface).
- Six Wave-0 tlm-stats tests un-disabled and green.
- OBS-01/02/03/05/06/07 fully covered across BOTH modules.
</success_criteria>

<output>
After completion, create `.planning/phases/07-observability-sweep/07-04-SUMMARY.md` recording: byte-diff between the two modules' logback-spring.xml (target: zero structural difference), the exact `reconmgmtDataSource` qualifier name discovered, evidence that the existing tlm-stats endpoints `/api/tlm-stats/*` + `/api/quickrec-stats/*` still respond 200 (regression check), and confirmation that NO ScriptExecutor / loader / SearchConfig artifacts were created in tlm-stats (lean-mirror integrity).
</output>
