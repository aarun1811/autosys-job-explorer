---
phase: 07-observability-sweep
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - backend/rectrace/src/main/java/com/citi/gru/rectrace/config/AsyncConfig.java
  - backend/rectrace/src/main/java/com/citi/gru/rectrace/util/ScriptExecutor.java
  - backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/aop/SlowLog.java
  - backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/aop/SlowQueryLoggerAspect.java
  - backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/aop/ScheduledTraceIdAspect.java
  - backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/health/OracleHealthIndicator.java
  - backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/health/ElasticsearchHealthIndicator.java
  - backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/health/LoaderRunAgeHealthIndicator.java
  - backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/health/SearchConfigHealthIndicator.java
  - backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/health/HealthIndicatorDataSourceConfig.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/ActuatorHealthIntegrationTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/PrometheusEndpointTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/AsyncMdcPropagationTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/aop/SlowQueryLoggerAspectTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/aop/ScheduledTraceIdAspectTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/health/OracleHealthIndicatorTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/health/ElasticsearchHealthIndicatorTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/health/LoaderRunAgeHealthIndicatorTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/health/SearchConfigHealthIndicatorTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/util/ScriptExecutorEnvVarTest.java
autonomous: true
requirements: [OBS-02, OBS-04, OBS-05, OBS-06]
tags: [observability, health-indicators, aop, slow-query, micrometer-prometheus, mdc-propagation, scheduler-trace]

must_haves:
  truths:
    - "`/actuator/health` aggregates four indicators — oracle, elasticsearch, searchConfig in the default group; loaderRunAge in a separate `/actuator/health/loader` group (per Pitfall P-11 / D-7.12)."
    - "OracleHealthIndicator uses a dedicated `healthCheckJdbcTemplate` bean with `queryTimeout=2s` — NEVER mutates the shared primaryJdbcTemplate (Pitfall P-7)."
    - "`/actuator/prometheus` returns text/plain with `http_server_requests_seconds_count` and `jvm_memory_used_bytes` after one HTTP request."
    - "SlowQueryLoggerAspect WARNs when JdbcTemplate.query*/update*/execute*/batchUpdate* call exceeds `observability.slow-query-threshold-ms` (default 500ms); also fires on `@SlowLog`-annotated methods. Excluded in `test` profile via `@Profile(\"!test\")` (Pitfall P-9)."
    - "Pointcut targets concrete `org.springframework.jdbc.core.JdbcTemplate` (NOT JdbcOperations interface) — does not double-fire through NamedParameterJdbcTemplate (Pitfall P-6)."
    - "AsyncConfig wires `ContextPropagatingTaskDecorator` into the `taskExecutor` bean — MDC.traceId/userId propagate through `@Async` methods (Boot 3.2+ built-in, NOT hand-rolled per D-7.6)."
    - "ScheduledTraceIdAspect calls `tracer.nextSpan().start()` on every `@Scheduled` invocation — produces a fresh 32-hex `traceId` in MDC per fire."
    - "ScriptExecutor.executeScript injects `RECTRACE_CORRELATION_ID` env var into the subprocess when MDC.traceId is present; omits it when MDC is empty."
    - "Wave-0 tests for OBS-02/04/05/06 (nine test classes) are un-disabled and PASS."
  artifacts:
    - path: backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/health/HealthIndicatorDataSourceConfig.java
      provides: "Dedicated healthCheckJdbcTemplate bean with 2s query timeout"
      contains: "setQueryTimeout"
    - path: backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/health/LoaderRunAgeHealthIndicator.java
      provides: "Per-job last-success age check; DOWN when age > 2× cron"
      contains: "AbstractHealthIndicator"
    - path: backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/aop/SlowQueryLoggerAspect.java
      provides: "JdbcTemplate concrete-class pointcut + @SlowLog pointcut; WARN at threshold"
      contains: "JdbcTemplate"
    - path: backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/aop/ScheduledTraceIdAspect.java
      provides: "Fresh-trace-per-fire aspect on @Scheduled"
      contains: "tracer.nextSpan"
    - path: backend/rectrace/src/main/java/com/citi/gru/rectrace/config/AsyncConfig.java
      provides: "taskExecutor with ContextPropagatingTaskDecorator"
      contains: "ContextPropagatingTaskDecorator"
    - path: backend/rectrace/src/main/java/com/citi/gru/rectrace/util/ScriptExecutor.java
      provides: "RECTRACE_CORRELATION_ID env var injection"
      contains: "RECTRACE_CORRELATION_ID"
  key_links:
    - from: AsyncConfig.taskExecutor()
      to: ContextPropagatingTaskDecorator
      via: "executor.setTaskDecorator(new ContextPropagatingTaskDecorator())"
      pattern: "setTaskDecorator.*ContextPropagatingTaskDecorator"
    - from: SlowQueryLoggerAspect pointcut
      to: org.springframework.jdbc.core.JdbcTemplate
      via: "execution(* org.springframework.jdbc.core.JdbcTemplate.query*(..)) etc"
      pattern: "JdbcTemplate\\.query\\*"
    - from: ScheduledTraceIdAspect
      to: io.micrometer.tracing.Tracer.nextSpan()
      via: "@Around @annotation(Scheduled) opens a new Brave span via injected Tracer"
      pattern: "tracer\\.nextSpan"
    - from: ScriptExecutor.executeScript
      to: ProcessBuilder.environment()
      via: "pb.environment().put('RECTRACE_CORRELATION_ID', MDC.get('traceId'))"
      pattern: "RECTRACE_CORRELATION_ID"
    - from: LoaderRunAgeHealthIndicator
      to: LoaderRunHistoryService + LoaderJobRegistry
      via: "constructor injection; per-job age computed against 2× CronExpression.next(now).minus(now)"
      pattern: "LoaderRunHistoryService"
---

<objective>
Land the health/AOP/propagation half of Phase 7 in `backend/rectrace`: four custom `HealthIndicator` beans (Oracle, Elasticsearch, LoaderRunAge in its own /actuator/health/loader group, SearchConfig), the slow-query AOP aspect with `@SlowLog` annotation, the `@Scheduled` fresh-trace aspect, the `ContextPropagatingTaskDecorator` wiring in `AsyncConfig`, and the `RECTRACE_CORRELATION_ID` env-var injection in `ScriptExecutor`. Enable nine Wave-0 tests from Plan 01.

Purpose: OBS-02 (four health indicators), OBS-04 (slow-query AOP), OBS-05 (`@Timed`/`@Observed` wiring — surfaced via starter-aop + `management.observations.annotations.enabled=true` set by Plan 02), OBS-06 (three thread-boundary holes closed).

Output: Ten new/edited files in main; nine test files un-disabled. Disjoint from Plan 02's file set so Plans 02 and 03 run in parallel within Wave 2.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-observability-sweep/07-CONTEXT.md
@.planning/phases/07-observability-sweep/07-RESEARCH.md
@.planning/phases/07-observability-sweep/07-01-SUMMARY.md
@backend/rectrace/src/main/java/com/citi/gru/rectrace/config/AsyncConfig.java
@backend/rectrace/src/main/java/com/citi/gru/rectrace/util/ScriptExecutor.java
@backend/rectrace/src/main/java/com/citi/gru/rectrace/loader/LoaderJobRegistry.java
@backend/rectrace/src/main/java/com/citi/gru/rectrace/loader/LoaderTicker.java
@backend/rectrace/src/main/java/com/citi/gru/rectrace/loader/LoaderRunHistoryService.java

<interfaces>
<!-- AsyncConfig today (verified): single @Bean(name="taskExecutor") ThreadPoolTaskExecutor, corePoolSize=10, maxPoolSize=20, queueCapacity=100. Plan 03 adds .setTaskDecorator(new ContextPropagatingTaskDecorator()) before .initialize(). -->
<!-- ScriptExecutor today (verified): public String executeScript(String scriptPath, String serviceName, String dbSchema). Plan 03 adds env-var injection inside the ProcessBuilder construction. -->
<!-- LoaderRunHistoryService (Phase 6) exposes per-job last successful run via `getLastSuccess(String jobKey)` returning Instant or null — verify exact signature during impl. -->
<!-- LoaderJobRegistry (Phase 6) exposes `getJobKeys()` and `getCronExpression(String jobKey)` returning CronExpression — verify exact signatures during impl. -->
<!-- SearchConfigServiceV4 + SqlSearchConfigServiceV4 each expose `getCategories()` / `getQueries()` returning a non-empty list when boot-time validation passed — verify exact signature during impl. -->
<!-- Brave Tracer bean: io.micrometer.tracing.Tracer (NOT brave.Tracer). Autowired into ScheduledTraceIdAspect. -->
<!-- ContextPropagatingTaskDecorator: org.springframework.core.task.support.ContextPropagatingTaskDecorator (Boot 3.2+ built-in). -->
<!-- AbstractHealthIndicator: org.springframework.boot.actuate.health.AbstractHealthIndicator. -->
<!-- ElasticsearchClient: co.elastic.clients.elasticsearch.ElasticsearchClient (already on classpath per Phase 1 BOOT-06). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Four HealthIndicator beans + dedicated health-check datasource config</name>
  <files>backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/health/HealthIndicatorDataSourceConfig.java, backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/health/OracleHealthIndicator.java, backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/health/ElasticsearchHealthIndicator.java, backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/health/LoaderRunAgeHealthIndicator.java, backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/health/SearchConfigHealthIndicator.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/ActuatorHealthIntegrationTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/health/OracleHealthIndicatorTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/health/ElasticsearchHealthIndicatorTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/health/LoaderRunAgeHealthIndicatorTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/health/SearchConfigHealthIndicatorTest.java</files>
  <behavior>
- `/actuator/health` returns 200 with `$.components.oracle.status`, `$.components.elasticsearch.status`, `$.components.searchConfig.status` present; loaderRunAge is OMITTED from the default group.
- `/actuator/health/loader` returns 200 with `$.components.loaderRunAge.status` and per-job detail map.
- OracleHealthIndicator with mocked JdbcTemplate returning `Integer.valueOf(1)` returns UP with `withDetail("query","SELECT 1 FROM DUAL")`; mock that throws DataAccessException returns DOWN with `withDetail("error", "...")`.
- ElasticsearchHealthIndicator with mocked ElasticsearchClient.ping() returning `BooleanResponse(true)` returns UP; mock that throws IOException returns DOWN.
- LoaderRunAgeHealthIndicator with one job "core_load" (cron every 5 min) and mocked lastSuccess at `now - 4 min` returns UP with `ageMs ~ 240_000`; lastSuccess at `now - 11 min` returns DOWN with `ageMs ~ 660_000` and per-job detail `{lastSuccess, ageMs, status:DOWN}`.
- SearchConfigHealthIndicator with both services returning non-empty returns UP; either returning empty returns DOWN with `withDetail("v4Loaded",bool).withDetail("sqlLoaded",bool)`.
  </behavior>
  <action>
**File 1 — `HealthIndicatorDataSourceConfig.java` (NEW):**
`package com.citi.gru.rectrace.observability.health;`

Declare a `@Bean("healthCheckJdbcTemplate")` returning a NEW `JdbcTemplate` wrapping the primary `DataSource` (`@Qualifier` the existing primary DS bean) with `setQueryTimeout(2)` set ONLY on this dedicated instance. Pitfall P-7: never mutate the shared singleton JdbcTemplate. The bean is `@Profile("!test")` to avoid test-context interference, but use `@ConditionalOnBean(name="dataSource")` so it activates whenever the primary DS is wired.

**File 2 — `OracleHealthIndicator.java` (NEW):**
`package com.citi.gru.rectrace.observability.health;`
Extend `AbstractHealthIndicator`. Constructor takes `@Qualifier("healthCheckJdbcTemplate") JdbcTemplate t`. `doHealthCheck(Health.Builder b)`:
- `Integer one = t.queryForObject("SELECT 1 FROM DUAL", Integer.class);`
- If `one != null && one == 1` → `b.up().withDetail("query","SELECT 1 FROM DUAL")`
- Else → `b.down().withDetail("reason","unexpected result")`
- `AbstractHealthIndicator` catches thrown exceptions and maps to DOWN with `error` detail (Pitfall P-4 — use abstract base, not raw interface).
- `@Component("oracle")` so it appears as `$.components.oracle` in the JSON.

**File 3 — `ElasticsearchHealthIndicator.java` (NEW):**
Constructor injects `co.elastic.clients.elasticsearch.ElasticsearchClient esClient`. `doHealthCheck`:
- `boolean pong = esClient.ping().value();`
- UP if true with `withDetail("cluster", <hosts from spring.elasticsearch.uris>)`; else DOWN with `withDetail("reason","ping returned false")`.
- `@Component("elasticsearch")`.
- Wrap inner call in try/catch IOException → AbstractHealthIndicator's exception handling lifts it to DOWN automatically; ensure the indicator surface throws what AbstractHealthIndicator expects (i.e. let exceptions propagate from `doHealthCheck`).

**File 4 — `LoaderRunAgeHealthIndicator.java` (NEW):**
Constructor injects `LoaderJobRegistry registry`, `LoaderRunHistoryService runHistoryService`, `Clock clock` (Spring auto-wires `Clock.systemUTC()` if no other bean; declare a `@Bean Clock clock() { return Clock.systemUTC(); }` in the same config file if absent).
- `@Component("loaderRunAge")` — name MUST match `management.endpoint.health.group.loader.include=loaderRunAge` from Plan 02.
- For each jobKey in registry: compute `Instant lastSuccess = runHistoryService.getLastSuccess(jobKey)` (verify exact API during impl — if signature differs, adapt); compute `Duration cronInterval = nextFire(cronExpression, now).minus(now)`; if `lastSuccess == null || Duration.between(lastSuccess, now).compareTo(cronInterval.multipliedBy(2)) > 0` → that job is DOWN.
- Builder pattern: collect per-job details into a map `{jobKey: {lastSuccess, ageMs, status}}`; if ANY job DOWN → overall DOWN; else UP.
- For empty-registry case (no jobs configured) → UP with `withDetail("jobs","none configured")`.

**File 5 — `SearchConfigHealthIndicator.java` (NEW):**
Constructor injects `SearchConfigServiceV4 v4` and `SqlSearchConfigServiceV4 sql`.
- `boolean v4Loaded = !v4.getCategories().isEmpty();` (adapt to actual API)
- `boolean sqlLoaded = !sql.getQueries().isEmpty();` (adapt)
- UP if both true; else DOWN.
- `@Component("searchConfig")`.

**Files 6-10 — Un-disable five Wave-0 tests:**
Remove `@Disabled` annotation from:
- `ActuatorHealthIntegrationTest.java`
- `OracleHealthIndicatorTest.java`
- `ElasticsearchHealthIndicatorTest.java`
- `LoaderRunAgeHealthIndicatorTest.java`
- `SearchConfigHealthIndicatorTest.java`

If any test assertion needs adjustment because Plan 01's scaffold made an assumption that doesn't match the actual service API (e.g. `runHistoryService.getLastSuccess` actually named `findLastSuccessfulRun`), update the TEST to match the ACTUAL service signature — DO NOT add adapter methods to the service. The four health-indicator implementations adapt to whatever signature exists.

Per `<scope_reduction_prohibition>`: deliver ALL FOUR indicators in this task. Do not defer SearchConfigHealthIndicator or LoaderRunAgeHealthIndicator to "later" — D-7.2 requires all four.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/autosys-job-explorer && mvn -pl backend/rectrace test -Dtest='ActuatorHealthIntegrationTest,OracleHealthIndicatorTest,ElasticsearchHealthIndicatorTest,LoaderRunAgeHealthIndicatorTest,SearchConfigHealthIndicatorTest' 2>&1 | tee /tmp/p7-03-t1.log | grep -E "Tests run: ([5-9]|[1-9][0-9]+).*Failures: 0.*Errors: 0" && grep -l "AbstractHealthIndicator" backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/health/*.java | wc -l | grep -qE "^[[:space:]]*4"</automated>
  </verify>
  <done>Five health-related Wave-0 tests un-disabled and green; four indicator classes extend AbstractHealthIndicator; HealthIndicatorDataSourceConfig declares a dedicated JdbcTemplate bean with 2s query timeout; primary JdbcTemplate is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Slow-query AOP + @SlowLog annotation + Prometheus endpoint test</name>
  <files>backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/aop/SlowLog.java, backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/aop/SlowQueryLoggerAspect.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/aop/SlowQueryLoggerAspectTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/PrometheusEndpointTest.java</files>
  <behavior>
- SlowQueryLoggerAspect fires WARN on a JdbcTemplate.query call exceeding `observability.slow-query-threshold-ms` (default 500).
- Aspect does NOT fire on calls under threshold.
- Aspect does NOT double-fire when the same logical query goes through `NamedParameterJdbcTemplate.query(...) → JdbcTemplate.query(...)` — pointcut targets concrete JdbcTemplate ONLY (Pitfall P-6).
- Aspect is `@Profile("!test")` — does NOT run during test context boot (Pitfall P-9); SlowQueryLoggerAspectTest registers the aspect in a `@TestConfiguration` explicitly to exercise it.
- `@SlowLog` annotation, applied to a service method that sleeps 600ms, produces a WARN log line.
- `/actuator/prometheus` returns text/plain; body contains `http_server_requests_seconds_count` (server-request metric) and `jvm_memory_used_bytes` (JVM metric), proving `micrometer-registry-prometheus` from Plan 01 is wired and the exposure list from Plan 02 includes `prometheus`.
  </behavior>
  <action>
**File 1 — `SlowLog.java` (NEW):**
`package com.citi.gru.rectrace.observability.aop;`
```
@Target({ElementType.METHOD, ElementType.TYPE})
@Retention(RetentionPolicy.RUNTIME)
public @interface SlowLog { }
```
Marker annotation only.

**File 2 — `SlowQueryLoggerAspect.java` (NEW):**
`package com.citi.gru.rectrace.observability.aop;`
- `@Aspect @Component @Profile("!test") @Slf4j`
- `@Value("${observability.slow-query-threshold-ms:500}") private long thresholdMs;`
- Two `@Around` advices:
  1. `@Around("execution(* org.springframework.jdbc.core.JdbcTemplate.query*(..)) || execution(* org.springframework.jdbc.core.JdbcTemplate.update*(..)) || execution(* org.springframework.jdbc.core.JdbcTemplate.execute*(..)) || execution(* org.springframework.jdbc.core.JdbcTemplate.batchUpdate*(..))")` — calls private helper `timeAndMaybeWarn(pjp, "jdbc")`.
  2. `@Around("@annotation(com.citi.gru.rectrace.observability.aop.SlowLog)")` — calls helper with tag `"slowlog"`.
- Helper computes `(System.nanoTime() - start) / 1_000_000` and logs `log.warn("slow-{} method={} durationMs={} arg0={}", tag, pjp.getSignature().toShortString(), ms, truncate(args[0].toString(), 200));` when threshold exceeded.
- `truncate(String s, int max)` private static method per RESEARCH §3.5.

Pointcut MUST target concrete `JdbcTemplate` (NOT the `JdbcOperations` interface) — verified Pitfall P-6 fix.

**File 3 — Un-disable `SlowQueryLoggerAspectTest`:**
Remove `@Disabled` annotation. The test registers the aspect explicitly via `@TestConfiguration` and exercises both pointcuts. If the test scaffold from Plan 01 assumed an aspect signature that doesn't match the actual implementation here, update the test to match the actual aspect — but do not weaken the assertions.

**File 4 — Un-disable `PrometheusEndpointTest`:**
Remove `@Disabled`. Test exercises `/actuator/prometheus` and asserts the body contains the two metric names. The endpoint is wired by `micrometer-registry-prometheus` (Plan 01) + exposure list (Plan 02) — no additional code needed here.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/autosys-job-explorer && mvn -pl backend/rectrace test -Dtest='SlowQueryLoggerAspectTest,PrometheusEndpointTest' 2>&1 | tee /tmp/p7-03-t2.log | grep -E "Tests run: ([2-9]|[1-9][0-9]+).*Failures: 0.*Errors: 0" && grep -q "@Profile(\"!test\")" backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/aop/SlowQueryLoggerAspect.java && grep -q "org.springframework.jdbc.core.JdbcTemplate" backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/aop/SlowQueryLoggerAspect.java && ! grep -q "JdbcOperations" backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/aop/SlowQueryLoggerAspect.java</automated>
  </verify>
  <done>Both tests un-disabled and green; SlowQueryLoggerAspect is `@Profile("!test")` (Pitfall P-9); pointcut references concrete `org.springframework.jdbc.core.JdbcTemplate` and does NOT reference `JdbcOperations` (Pitfall P-6); SlowLog annotation defined with METHOD+TYPE targets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: @Async MDC propagation + @Scheduled fresh-trace aspect + ScriptExecutor env-var injection</name>
  <files>backend/rectrace/src/main/java/com/citi/gru/rectrace/config/AsyncConfig.java, backend/rectrace/src/main/java/com/citi/gru/rectrace/util/ScriptExecutor.java, backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/aop/ScheduledTraceIdAspect.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/AsyncMdcPropagationTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/aop/ScheduledTraceIdAspectTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/util/ScriptExecutorEnvVarTest.java</files>
  <behavior>
- An `@Async("taskExecutor")` method invoked with `MDC.put("traceId","abc123def4567890")` returns `MDC.get("traceId")` equal to `"abc123def4567890"` from the async thread.
- An `@Scheduled` method invoked twice (directly via Spring proxy) produces two DIFFERENT non-empty 32-hex traceIds; aspect uses `tracer.nextSpan().start()` then `tracer.withSpan(span)` then `span.end()` in finally.
- `ScriptExecutor.executeScript` with `MDC.put("traceId","abc123def4567890")` injects `RECTRACE_CORRELATION_ID=abc123def4567890` into the subprocess environment; with no MDC value, the env var is ABSENT from the ProcessBuilder environment.
  </behavior>
  <action>
**File 1 — `AsyncConfig.java` (EDIT):**
Existing file declares a single `@Bean(name="taskExecutor")`. Modify the bean method:
- Add import `org.springframework.core.task.support.ContextPropagatingTaskDecorator;`
- Insert `executor.setTaskDecorator(new ContextPropagatingTaskDecorator());` AFTER `executor.setThreadNamePrefix("AsyncSearch-")` and BEFORE `executor.initialize();`.
- Add Javadoc citing D-7.6 and Pitfall A4 verification target.

DO NOT hand-roll an MDC-copying decorator. The built-in `ContextPropagatingTaskDecorator` propagates Brave TraceContext + MDC + Security + RequestContext (verified against Boot 3.5 docs).

**File 2 — `ScriptExecutor.java` (EDIT):**
Current code constructs `ProcessBuilder pb = new ProcessBuilder(scriptPath, "@" + serviceName, dbSchema);`. After construction, AFTER `pb.redirectErrorStream(true)` (or wherever existing setup ends, before `pb.start()`):
```
String traceId = org.slf4j.MDC.get("traceId");
if (traceId != null && !traceId.isEmpty()) {
    pb.environment().put("RECTRACE_CORRELATION_ID", traceId);
}
```
Add Javadoc paragraph on the method: "Subprocesses can correlate stdout/stderr with backend logs by reading `RECTRACE_CORRELATION_ID` env var. The variable is present iff MDC.traceId is set at invocation time. Boot-time DB-password retrieval has no active trace and will not see the variable."

**File 3 — `ScheduledTraceIdAspect.java` (NEW):**
`package com.citi.gru.rectrace.observability.aop;`
- `@Aspect @Component`
- Constructor takes `io.micrometer.tracing.Tracer tracer`.
- `@Around("@annotation(org.springframework.scheduling.annotation.Scheduled)")` method:
  ```
  Span span = tracer.nextSpan().name(pjp.getSignature().toShortString()).start();
  try (Tracer.SpanInScope ws = tracer.withSpan(span)) {
      return pjp.proceed();
  } finally {
      span.end();
  }
  ```
- Brave's MDC scope decorator (auto-wired by `BraveAutoConfiguration` in Phase 1/2) populates `MDC.traceId` from the active span. No manual MDC.put needed.
- Pitfall P-10: ShedLock `LockingTaskExecutor.executeWithLock` runs on the caller thread; the aspect on the OUTER `LoaderTicker.tick()` method propagates MDC into the lock-protected lambda automatically. No additional work for the LoaderTicker.

**Files 4-6 — Un-disable three Wave-0 tests:**
Remove `@Disabled` annotation from:
- `AsyncMdcPropagationTest.java`
- `ScheduledTraceIdAspectTest.java`
- `ScriptExecutorEnvVarTest.java`

If any test scaffold from Plan 01 referenced a method signature that doesn't match the actual code (e.g. `executeScript` returning `String` vs `void`), update the test — DO NOT change the ScriptExecutor signature beyond the env-var injection.

Per `<scope_reduction_prohibition>`: all three thread-boundary holes MUST be closed in this task. Not "subprocess can come later" — D-7.6 explicitly enumerates all three.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/autosys-job-explorer && mvn -pl backend/rectrace test -Dtest='AsyncMdcPropagationTest,ScheduledTraceIdAspectTest,ScriptExecutorEnvVarTest' 2>&1 | tee /tmp/p7-03-t3.log | grep -E "Tests run: ([3-9]|[1-9][0-9]+).*Failures: 0.*Errors: 0" && grep -q "ContextPropagatingTaskDecorator" backend/rectrace/src/main/java/com/citi/gru/rectrace/config/AsyncConfig.java && grep -q "RECTRACE_CORRELATION_ID" backend/rectrace/src/main/java/com/citi/gru/rectrace/util/ScriptExecutor.java && grep -q "tracer.nextSpan" backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/aop/ScheduledTraceIdAspect.java</automated>
  </verify>
  <done>Three Wave-0 tests un-disabled and green; AsyncConfig wires ContextPropagatingTaskDecorator (not a hand-rolled MDC decorator); ScriptExecutor injects RECTRACE_CORRELATION_ID conditionally; ScheduledTraceIdAspect opens a fresh Brave span via tracer.nextSpan() per fire.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request thread → @Async thread | MDC + TraceContext must propagate cleanly without leaking across pool reuse |
| Scheduler thread → loader job logic | Fresh traceId per fire prevents trace-collisions in run-history |
| backend JVM → subprocess (get_password.sh) | Env var RECTRACE_CORRELATION_ID flows out; must not contain injection chars |
| /actuator/health → operator/k8s probe | LoaderRunAge in default group would flap readiness; isolated in /loader group |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Information Disclosure | OracleHealthIndicator with shared JdbcTemplate setQueryTimeout would break ALL Oracle callers (Pitfall P-7) | mitigate | Dedicated `healthCheckJdbcTemplate` bean in `HealthIndicatorDataSourceConfig` — never touches singleton |
| T-07-13 | Denial of Service | /actuator/health hangs when Oracle network slow | mitigate | 2s `queryTimeout` on the dedicated healthCheckJdbcTemplate (Pitfall P-5) |
| T-07-14 | Denial of Service | LoaderRunAge DOWN flaps k8s/ops readiness probe | mitigate | Separate `/actuator/health/loader` group per Pitfall P-11; default `/actuator/health` excludes loaderRunAge |
| T-07-15 | Information Disclosure | Slow-query log line includes bind args — could leak passwords if SQL has plaintext password | mitigate | Truncate to 200 chars/arg (D-7.4); also code review of @SlowLog-annotated methods to confirm no secrets in args (Phase 9 cleanup) |
| T-07-16 | Tampering | Aspect on `@Scheduled` could be bypassed if methods are private | accept | Spring AOP only proxies public methods; @Scheduled methods are public by convention; documented in aspect's Javadoc |
| T-07-17 | Information Disclosure | RECTRACE_CORRELATION_ID env var inherited by deeper child processes | accept | TraceId is non-secret by design (it's already in HTTP headers); inheritance is desired for correlation |
| T-07-18 | Denial of Service | AOP double-fire via NamedParameterJdbcTemplate→JdbcTemplate delegation | mitigate | Pointcut targets concrete `JdbcTemplate` (Pitfall P-6) — verified via `grep -q -v JdbcOperations` |
| T-07-19 | Tampering | ContextPropagatingTaskDecorator could leak MDC across pool threads if thread holds context after task ends | mitigate | Boot 3.2+ built-in handles cleanup via ContextSnapshot.Scope.close(); avoided hand-rolled bug (D-7.6) |
</threat_model>

<verification>
- `mvn -pl backend/rectrace test -Dtest='com.citi.gru.rectrace.observability.*'` → 13 tests run (12 from Plan 01 + 1 Actuator integration), Failures: 0, Errors: 0.
- `mvn -pl backend/rectrace -DskipTests package && (cd /Users/aarun/Workspace/Projects/autosys-job-explorer && ops/rectrace-ops.sh start backend 2>/dev/null || mvn -pl backend/rectrace spring-boot:run -Dspring-boot.run.profiles=local &)` — manual local smoke (per CLAUDE.md ops surface) gated behind separate UAT step; this plan's gate is mvn test alone.
- `curl -s http://localhost:6088/rectrace/actuator/health | jq '.components | keys'` (post-execute-phase): includes `oracle`, `elasticsearch`, `searchConfig`; OMITS `loaderRunAge`.
- `curl -s http://localhost:6088/rectrace/actuator/health/loader | jq '.components.loaderRunAge.status'` returns UP/DOWN.
- `curl -s http://localhost:6088/rectrace/actuator/prometheus | grep -c "^http_server_requests_seconds_count\|^jvm_memory_used_bytes"` ≥ 2.
- `grep -c "ContextPropagatingTaskDecorator" backend/rectrace/src/main/java/com/citi/gru/rectrace/config/AsyncConfig.java | grep -v '^0$'` → ≥ 1.
- `grep -c "JdbcOperations" backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/aop/SlowQueryLoggerAspect.java | grep -qE '^[[:space:]]*0$'` (must be ZERO).
</verification>

<success_criteria>
- All four HealthIndicator beans exist, each `@Component(<name>)` matching the JSON-path key.
- LoaderRunAgeHealthIndicator placed in `/actuator/health/loader` group (Pitfall P-11) — no readiness flap risk.
- HealthIndicatorDataSourceConfig declares dedicated 2s-timeout JdbcTemplate; shared singleton untouched.
- SlowQueryLoggerAspect pointcut targets concrete `JdbcTemplate` only; `@Profile("!test")` gating; @SlowLog annotation defined.
- ScheduledTraceIdAspect uses `tracer.nextSpan()` per fire.
- AsyncConfig wires `ContextPropagatingTaskDecorator`.
- ScriptExecutor injects `RECTRACE_CORRELATION_ID` env var conditionally on MDC.traceId presence.
- Nine Wave-0 tests (5 health + 1 prometheus + 1 slow-query + 1 async + 1 scheduled + 1 script-executor = 10 actually) un-disabled and green.
- OBS-02, OBS-04, OBS-05, OBS-06 marked complete in phase summary.
</success_criteria>

<output>
After completion, create `.planning/phases/07-observability-sweep/07-03-SUMMARY.md` recording: full list of health indicator JSON-path keys observed in /actuator/health response, prometheus metric sample, evidence that AsyncMdcPropagationTest's hand-off works (test log fragment), and any deviation from the Plan 01 scaffold's assumed service signatures (e.g. if `LoaderRunHistoryService.getLastSuccess` was actually `findLastSuccessfulRun`, document the adapter applied in the test).
</output>
