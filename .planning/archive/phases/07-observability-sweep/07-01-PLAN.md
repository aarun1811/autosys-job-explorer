---
phase: 07-observability-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/rectrace/pom.xml
  - rectrace-tlm-stats/pom.xml
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/AccessLogJsonShapeTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/LogbackProdProfileTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/ActuatorHealthIntegrationTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/ActuatorExposureTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/PrometheusEndpointTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/AsyncMdcPropagationTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/aop/SlowQueryLoggerAspectTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/aop/ScheduledTraceIdAspectTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/health/OracleHealthIndicatorTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/health/ElasticsearchHealthIndicatorTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/health/LoaderRunAgeHealthIndicatorTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/health/SearchConfigHealthIndicatorTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/util/ScriptExecutorEnvVarTest.java
  - rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/AccessLogJsonShapeTest.java
  - rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/ActuatorHealthIntegrationTest.java
  - rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/ActuatorExposureTest.java
  - rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/PrometheusEndpointTest.java
  - rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/AsyncMdcPropagationTest.java
  - rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/health/OracleHealthIndicatorTest.java
autonomous: true
requirements: [OBS-01, OBS-04, OBS-05]
tags: [observability, spring-boot, logback, micrometer, prometheus, aop, test-scaffold]

must_haves:
  truths:
    - "Both modules' POMs declare logstash-logback-encoder 8.0 (explicit pin), micrometer-registry-prometheus (BOM-managed), and spring-boot-starter-aop (BOM-managed)."
    - "Both modules' POMs still build (mvn -DskipTests package succeeds) — dependency resolution converges, no Jackson 3 collision."
    - "Backend Wave-0 test scaffolds compile under -DskipTests=false and are @Disabled (no assertions yet) — Plans 02/03 enable them by removing the @Disabled annotation."
    - "TLM-stats Wave-0 test scaffolds compile and are @Disabled — Plan 04 enables them."
    - "mvn -pl backend/rectrace test -Dtest='com.citi.gru.rectrace.observability.*' reports 0 failures (all @Disabled)."
    - "mvn -pl rectrace-tlm-stats test -Dtest='com.citi.gru.rectrace.tlmstats.observability.*' reports 0 failures (all @Disabled)."
  artifacts:
    - path: backend/rectrace/pom.xml
      provides: "logstash-logback-encoder:8.0 + micrometer-registry-prometheus + spring-boot-starter-aop deps"
      contains: "logstash-logback-encoder"
    - path: rectrace-tlm-stats/pom.xml
      provides: "same three deps for second module"
      contains: "logstash-logback-encoder"
    - path: backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/AccessLogJsonShapeTest.java
      provides: "Wave-0 OBS-01 JSON-shape contract — @Disabled scaffold"
      contains: "@Disabled"
    - path: backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/aop/SlowQueryLoggerAspectTest.java
      provides: "Wave-0 OBS-04 slow-query contract — @Disabled scaffold"
      contains: "@Disabled"
    - path: backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/PrometheusEndpointTest.java
      provides: "Wave-0 OBS-05 prometheus endpoint contract — @Disabled scaffold"
      contains: "@Disabled"
  key_links:
    - from: backend/rectrace/pom.xml
      to: logstash-logback-encoder:8.0
      via: "explicit <version>8.0</version> (NOT BOM-managed; must pin per D-7.1)"
      pattern: "logstash-logback-encoder.*8\\.0"
    - from: backend/rectrace/pom.xml
      to: spring-boot-starter-aop
      via: "BOM-managed (no version)"
      pattern: "spring-boot-starter-aop"
    - from: backend/rectrace/pom.xml
      to: micrometer-registry-prometheus
      via: "BOM-managed (no version)"
      pattern: "micrometer-registry-prometheus"
---

<objective>
Land the dependency floor and the full Wave-0 test scaffold for Phase 7 across BOTH backend modules. This plan adds the three required deps (logstash-logback-encoder 8.0 explicit, micrometer-registry-prometheus + spring-boot-starter-aop BOM-managed) and writes thirteen `@Disabled` JUnit 5 test classes whose specs are the executable contract that Plans 02/03/04 will satisfy by removing one annotation per file.

Purpose: Per D-8 (Nyquist) the test scaffold MUST exist before implementation so each follow-up plan has an automated verify gate. Per D-7.1 the encoder is explicitly pinned at 8.0 because 9.0 migrated to Jackson 3 and would shred the Boot 3.5.14 dependency tree.

Output: Two updated POMs (deps added, no behavior change yet), thirteen new `@Disabled` test files (backend/rectrace gets ten; rectrace-tlm-stats gets a six-test mirror subset).
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-observability-sweep/07-CONTEXT.md
@.planning/phases/07-observability-sweep/07-RESEARCH.md
@backend/rectrace/pom.xml
@rectrace-tlm-stats/pom.xml

<interfaces>
<!-- Existing deps confirmed via grep — both modules already have starter-actuator + micrometer-tracing-bridge-brave + starter-security + starter-test. -->
<!-- backend/rectrace/pom.xml already declares: starter-data-jpa, starter-web, starter-data-elasticsearch, starter-security, starter-test, micrometer-tracing-bridge-brave, starter-actuator. -->
<!-- rectrace-tlm-stats/pom.xml already declares: starter-web, starter-test, starter-actuator, starter-security, starter-data-jpa, micrometer-tracing-bridge-brave. -->
<!-- NEITHER POM declares: logstash-logback-encoder, micrometer-registry-prometheus, spring-boot-starter-aop — Plan 01 adds all three. -->
<!-- Spring Boot version pinned at 3.5.14 (backend/rectrace/pom.xml line 7-9 parent); Java 21 (line 30). Same in rectrace-tlm-stats. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the three Phase-7 dependencies to both backend POMs</name>
  <files>backend/rectrace/pom.xml, rectrace-tlm-stats/pom.xml</files>
  <action>
Edit BOTH `backend/rectrace/pom.xml` and `rectrace-tlm-stats/pom.xml`. In each file's `<dependencies>` block, add the three new dependencies grouped together with a comment header `<!-- Phase 7 (OBS-01/04/05) — observability stack -->`:

1. `net.logstash.logback:logstash-logback-encoder` with explicit `<version>8.0</version>` — per D-7.1, NOT BOM-managed. DO NOT use 9.0 (Jackson 3 collision with Boot 3.5.14 BOM).
2. `io.micrometer:micrometer-registry-prometheus` — NO version tag (BOM-managed via Boot 3.5.14 parent — pulls 1.14.x line).
3. `org.springframework.boot:spring-boot-starter-aop` — NO version tag (BOM-managed). Required for both `@Timed`/`@Observed` annotation interception AND the slow-query/scheduled-trace aspects planned in 07-03.

Place the new block AFTER the existing `spring-boot-starter-actuator` dependency in each file (keep semantic locality).

DO NOT add `micrometer-tracing-bridge-brave` — already present in both POMs (verified). DO NOT alter the parent version, Java version, or any existing pin. DO NOT add maven-enforcer-plugin in this plan — that lands in Plan 05 (OBS-08).

Per D-7.1, the explicit 8.0 pin sits OUTSIDE `<dependencyManagement>` because logstash-logback-encoder is not part of the Spring Boot BOM. Adding it under `<dependencies>` with `<version>8.0</version>` is correct.

Per `<scope_reduction_prohibition>`: deliver ALL THREE deps in BOTH POMs in this task. No "tlm-stats can come later" — D-7.1 explicitly says "Both `backend/rectrace` AND `rectrace-tlm-stats` get it."
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/autosys-job-explorer && mvn -pl backend/rectrace -DskipTests dependency:tree | grep -E "logstash-logback-encoder|micrometer-registry-prometheus|spring-boot-starter-aop" | grep -v '^#' | wc -l | grep -qE "^[[:space:]]*[3-9]" && mvn -pl rectrace-tlm-stats -DskipTests dependency:tree | grep -E "logstash-logback-encoder|micrometer-registry-prometheus|spring-boot-starter-aop" | grep -v '^#' | wc -l | grep -qE "^[[:space:]]*[3-9]"</automated>
  </verify>
  <done>Both modules' `mvn dependency:tree` lists all three new artifacts; `mvn -DskipTests package` succeeds for both modules with no convergence warnings about Jackson, Logback, or Micrometer; logstash-logback-encoder resolves to exactly 8.0 (not 9.0).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write backend/rectrace Wave-0 test scaffolds (ten @Disabled JUnit 5 classes)</name>
  <files>backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/AccessLogJsonShapeTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/LogbackProdProfileTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/ActuatorHealthIntegrationTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/ActuatorExposureTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/PrometheusEndpointTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/AsyncMdcPropagationTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/aop/SlowQueryLoggerAspectTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/aop/ScheduledTraceIdAspectTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/health/OracleHealthIndicatorTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/health/ElasticsearchHealthIndicatorTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/health/LoaderRunAgeHealthIndicatorTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/health/SearchConfigHealthIndicatorTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/util/ScriptExecutorEnvVarTest.java</files>
  <behavior>
Each test class is class-level `@Disabled("Wave 0 scaffold — enabled by Plan 07-0X")` so the suite compiles and passes today. Each declares the test methods with full bodies AS IF the implementation existed; Plan 02/03 will flip `@Disabled` off in one mechanical step.

- `AccessLogJsonShapeTest`: `@SpringBootTest(webEnvironment=RANDOM_PORT)` + `MockMvc` GET `/actuator/health`; Logback `ListAppender<ILoggingEvent>` attached to logger named "access"; assert the captured event's `getMDCPropertyMap()` contains keys `traceId`, `path`, `method`, `status`, `durationMs` (userId may be empty when no header sent).
- `LogbackProdProfileTest`: `@SpringBootTest(properties={"spring.profiles.active=prod","splunk.hec.host=127.0.0.1","splunk.hec.port=9997"})`; obtain `LoggerContext`, iterate root logger's appenders, assert at least one is `net.logstash.logback.appender.LogstashTcpSocketAppender`.
- `ActuatorHealthIntegrationTest`: MockMvc GET `/actuator/health`; assert `status=200`; jsonPath `$.components.oracle.status` exists; jsonPath `$.components.elasticsearch.status` exists; jsonPath `$.components.searchConfig.status` exists. Loader-run-age sits in a separate group per Pitfall P-11 / D-7.12 → assert `GET /actuator/health/loader` returns 200 with `$.components.loaderRunAge.status`.
- `ActuatorExposureTest`: MockMvc GET `/actuator`; assert response includes `_links.health`, `_links.info`, `_links.prometheus`, `_links.loggers`, `_links.metrics`; assert response does NOT include `_links.env`, `_links.heapdump`, `_links.shutdown`, `_links.beans`, `_links.configprops`, `_links.threaddump`.
- `PrometheusEndpointTest`: MockMvc GET `/actuator/health` first (to register one request meter), then GET `/actuator/prometheus`; assert content-type `text/plain;version=0.0.4`; assert body contains `http_server_requests_seconds_count` and `jvm_memory_used_bytes` (Hikari metrics also auto-registered: `hikaricp_connections_active`).
- `AsyncMdcPropagationTest`: declare a `@TestConfiguration` with a `@Service` bean having `@Async("taskExecutor")` `CompletableFuture<String> echoTrace()` that returns `MDC.get("traceId")`; in test, `MDC.put("traceId", "abc123def4567890")`, call the async method, assert the future resolves to `"abc123def4567890"` within 5s. Pitfall A4: ContextPropagatingTaskDecorator MUST carry MDC.
- `SlowQueryLoggerAspectTest`: `@SpringBootTest(properties="observability.slow-query-threshold-ms=10")` + autowired `JdbcTemplate` (mocked via `@MockBean` of `DataSource`) — call `jdbc.queryForObject("SELECT 1 FROM DUAL", Integer.class)` after stubbing `Thread.sleep(50)`; `ListAppender` on `SlowQueryLoggerAspect`'s logger asserts ONE warn-level event with message containing `slow-jdbc` and `durationMs=`; then call again with under-threshold latency and assert NO additional warn event. Pitfall P-6 explicitly: pointcut targets concrete `JdbcTemplate`, not `JdbcOperations`.
- `ScheduledTraceIdAspectTest`: `@SpringBootTest` with a `@TestConfiguration` declaring a `@Component` with `@Scheduled(fixedDelay=Long.MAX_VALUE, initialDelay=Long.MAX_VALUE)` method NEVER auto-fired; invoke method directly through Spring context (proxied) — assert during invocation `MDC.get("traceId")` returns a 32-hex-char value AND that two consecutive direct invocations produce DIFFERENT traceIds (fresh per fire per D-7.10).
- `OracleHealthIndicatorTest`: unit test with Mockito-mocked `JdbcTemplate`; UP path returns `Integer.valueOf(1)`; DOWN path throws `org.springframework.dao.DataAccessResourceFailureException("oracle down")` — assert `health.getStatus()` is `Status.DOWN` and `health.getDetails()` contains `error`.
- `ElasticsearchHealthIndicatorTest`: Mockito-mocked `co.elastic.clients.elasticsearch.ElasticsearchClient`; UP path `client.ping()` returns `BooleanResponse(true)`; DOWN path throws `IOException`; assert UP/DOWN respectively.
- `LoaderRunAgeHealthIndicatorTest`: Mockito-mocked `LoaderRunHistoryService` + `LoaderJobRegistry` (returns one job "core_load" with cron expression for every 5 min) + injected `java.time.Clock` fixed. UP when last-success age < 10 min; DOWN when last-success age > 10 min (2× cron interval per D-7.12); per-job detail map contains `{lastSuccess, ageMs, status}`.
- `SearchConfigHealthIndicatorTest`: Mockito-mocked `SearchConfigServiceV4` + `SqlSearchConfigServiceV4`; UP when both return non-empty categories; DOWN when either is empty; details contain `v4Loaded`/`sqlLoaded` booleans.
- `ScriptExecutorEnvVarTest`: spies `ProcessBuilder` (or uses a test subclass that captures the constructed PB); set `MDC.put("traceId","abc123def4567890")` then call `executeScript("/bin/true", "svc", "schema")`; assert captured ProcessBuilder's `environment().get("RECTRACE_CORRELATION_ID")` equals `"abc123def4567890"`. Also assert that with NO MDC value (cleared first), the env var is ABSENT (not blank-string).
  </behavior>
  <action>
Create all thirteen files (twelve under `backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/...` plus one under `.../util/`). Every class:

1. Package `com.citi.gru.rectrace.observability` (or `.aop` / `.health` sub-package or `com.citi.gru.rectrace.util` for ScriptExecutorEnvVarTest).
2. Class-level `@Disabled("Wave 0 scaffold — enabled by Plan 07-0X")` where 0X is 02 or 03 depending on responsibility (filters/logback/exposure → 02; health/aop/async/script → 03).
3. Full imports for JUnit 5 (`org.junit.jupiter.api.*`), Spring test (`@SpringBootTest`, `@AutoConfigureMockMvc`, `MockMvc` where needed), Mockito where needed, `org.slf4j.MDC`, Logback `ch.qos.logback.classic.Logger`/`ch.qos.logback.core.read.ListAppender`, AssertJ (`org.assertj.core.api.Assertions`).
4. Spring-context tests use `@ActiveProfiles("test")` (matches Phase 0 convention) and `application-test.properties` which already exists in both modules.
5. The test bodies are COMPLETE — when Plan 02/03 removes `@Disabled`, the test runs verbatim. Do not stub `Assertions.fail()`.
6. Use `org.springframework.test.context.bean.override.mockito.MockitoBean` (Spring Boot 3.4+) NOT the deprecated `@MockBean`. Per Boot 3.5.14, the modern API is `@MockitoBean`.

Profile-gate signaling for Plan 03: tests that exercise beans gated by `@Profile("!test")` (per Pitfall P-9 the slow-query aspect IS test-profile-excluded) MUST use a nested `@TestConfiguration` that registers the aspect explicitly in the test ApplicationContext, NOT rely on it being scanned in.

Naming: subdirectories `observability/`, `observability/aop/`, `observability/health/` per the package map in 07-RESEARCH.md §"Recommended Project Structure".

Per CLAUDE.md: package prefix is `com.citi.gru.rectrace.*`. No code is required outside test sources — Plan 01 produces NO main-source changes (only POMs + test scaffolds).
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/autosys-job-explorer && mvn -pl backend/rectrace test -Dtest='com.citi.gru.rectrace.observability.*Test,com.citi.gru.rectrace.observability.aop.*Test,com.citi.gru.rectrace.observability.health.*Test,com.citi.gru.rectrace.util.ScriptExecutorEnvVarTest' 2>&1 | tee /tmp/p7-01-t2.log | grep -E "Tests run:.*Failures: 0.*Errors: 0.*Skipped: ([0-9]+)" && grep -c "@Disabled" /tmp/p7-01-t2.log || (cd /Users/aarun/Workspace/Projects/autosys-job-explorer && find backend/rectrace/src/test/java/com/citi/gru/rectrace/observability backend/rectrace/src/test/java/com/citi/gru/rectrace/util/ScriptExecutorEnvVarTest.java -name '*.java' | xargs grep -l '@Disabled' | grep -v '^#' | wc -l | grep -qE "^[[:space:]]*1[3-9]")</automated>
  </verify>
  <done>All 13 backend test files compile; `mvn -pl backend/rectrace test` runs them and reports `Failures: 0, Errors: 0, Skipped: 13+` (all class-level disabled); each file has a class-level `@Disabled` annotation referencing its enabling plan (02 or 03); test bodies are complete (no `fail("TODO")` placeholders).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write rectrace-tlm-stats Wave-0 test scaffolds (six @Disabled JUnit 5 classes — mirror subset)</name>
  <files>rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/AccessLogJsonShapeTest.java, rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/ActuatorHealthIntegrationTest.java, rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/ActuatorExposureTest.java, rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/PrometheusEndpointTest.java, rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/AsyncMdcPropagationTest.java, rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/health/OracleHealthIndicatorTest.java</files>
  <behavior>
Same shapes as Task 2 but stripped to the tlm-stats surface: NO loader-run-age (no loader subsystem), NO ES indicator (no ES client wired), NO search-config indicator (no search-config service), NO slow-query aspect test (per A3 the aspect MIGHT not be ported — Plan 04 decides; if Plan 04 adds it, a follow-up scaffold lands then). The six tests cover:

- `AccessLogJsonShapeTest`: same shape, but MockMvc target is `/api/tlm-stats/health` or `/actuator/health` (TLM-stats has no custom context-path).
- `ActuatorHealthIntegrationTest`: assert ONLY `$.components.oracle.status` (reconmgmt DS per Open Q9).
- `ActuatorExposureTest`: same allow-list assertion as backend/rectrace.
- `PrometheusEndpointTest`: same shape; assert prometheus body has `http_server_requests_seconds_count` and `jvm_memory_used_bytes`.
- `AsyncMdcPropagationTest`: same shape — tlm-stats' default async executor (no custom AsyncConfig present today; Plan 04 may add one). Assert MDC traceId propagation through any `@Async` invocation; if tlm-stats has no `@Async` beans, declare a `@TestConfiguration` with a synthetic one (mirrors backend/rectrace's approach).
- `OracleHealthIndicatorTest`: unit shape mirroring the backend/rectrace OracleHealthIndicatorTest; mocked JdbcTemplate; UP/DOWN paths.
  </behavior>
  <action>
Create the six files under `rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability/...`. Package prefix is `com.citi.gru.rectrace.tlmstats.observability` (mirroring tlm-stats' main-source package convention `com.citi.gru.rectrace.tlmstats.*`).

Every class is class-level `@Disabled("Wave 0 scaffold — enabled by Plan 07-04")`. Test bodies are complete (same convention as Task 2). Use `@MockitoBean` not `@MockBean`. Use `@ActiveProfiles("test")` and rely on `application-test.properties` (already exists per Phase 0 — verified).

Do NOT write tests for capabilities tlm-stats does NOT ship (loader-run-age, ES, search-config, slow-query). If Plan 04 adds slow-query parity to tlm-stats (per A3 recommendation), a new scaffold is created in that plan — NOT here. This task's six-test footprint is fixed by the actual tlm-stats surface.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/autosys-job-explorer && mvn -pl rectrace-tlm-stats test -Dtest='com.citi.gru.rectrace.tlmstats.observability.*Test,com.citi.gru.rectrace.tlmstats.observability.health.*Test' 2>&1 | tee /tmp/p7-01-t3.log | grep -E "Tests run:.*Failures: 0.*Errors: 0" && find rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability -name '*.java' | xargs grep -l '@Disabled' | grep -v '^#' | wc -l | grep -qE "^[[:space:]]*[6-9]"</automated>
  </verify>
  <done>All 6 tlm-stats test files compile; `mvn -pl rectrace-tlm-stats test` runs them and reports 0 failures / 0 errors with 6 skipped (class-level disabled); each file has a class-level `@Disabled("Wave 0 scaffold — enabled by Plan 07-04")`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → developer | POM consumes resolved dep tree; malicious transitive could ship a compromised encoder |
| test source → main source | Test scaffolds don't run in production but are committed to the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | logstash-logback-encoder 8.0 (explicit pin) | mitigate | Pin to a published Maven Central artifact; Plan 05 adds maven-enforcer-plugin convergence check that fails on transitive override |
| T-07-02 | Information Disclosure | Test scaffolds contain MDC keys (traceId/userId) — could leak into commit history if seeded with real data | mitigate | Scaffolds use synthetic 32-hex string "abc123def4567890" — no real correlation IDs, no real loginIds |
| T-07-03 | Denial of Service | Adding starter-aop pulls aspectjweaver — runtime weave overhead | accept | Aspect surface limited to JdbcTemplate + @SlowLog (Plan 03); proxy-based AOP only, no compile-time weaving |
| T-07-04 | Elevation of Privilege | Test scaffolds use @ActiveProfiles("test") which has script-executor stubbed — must not regress to production-profile | mitigate | All @SpringBootTest tests declare @ActiveProfiles("test"); reuse application-test.properties from Phase 0 |
</threat_model>

<verification>
- `mvn -pl backend/rectrace -DskipTests dependency:tree | grep logstash-logback-encoder` → exact version `8.0`.
- `mvn -pl rectrace-tlm-stats -DskipTests dependency:tree | grep logstash-logback-encoder` → exact version `8.0`.
- `mvn -pl backend/rectrace -DskipTests dependency:tree | grep -E "micrometer-registry-prometheus|spring-boot-starter-aop"` → both present, BOM-managed.
- `mvn -pl backend/rectrace test -Dtest='com.citi.gru.rectrace.observability.*'` → Tests run: ≥13, Failures: 0, Errors: 0 (all skipped via @Disabled).
- `mvn -pl rectrace-tlm-stats test -Dtest='com.citi.gru.rectrace.tlmstats.observability.*'` → Tests run: ≥6, Failures: 0, Errors: 0.
- `grep -rln "@Disabled" backend/rectrace/src/test/java/com/citi/gru/rectrace/observability | wc -l` ≥ 12.
- `grep -rln "@Disabled" rectrace-tlm-stats/src/test/java/com/citi/gru/rectrace/tlmstats/observability | wc -l` ≥ 6.
</verification>

<success_criteria>
- Both POMs list logstash-logback-encoder:8.0 (explicit), micrometer-registry-prometheus (BOM), spring-boot-starter-aop (BOM).
- `mvn -pl backend/rectrace -DskipTests package` passes; same for tlm-stats.
- 13 backend + 6 tlm-stats Wave-0 test files compile, run @Disabled (0 failures), and have complete test bodies ready for Plan 02/03/04 to enable.
- No Java 9.0 / Jackson 3 transitive leak (verified via `mvn dependency:tree | grep -c "jackson-core:3"` → 0).
</success_criteria>

<output>
After completion, create `.planning/phases/07-observability-sweep/07-01-SUMMARY.md` recording: exact resolved versions for all three added artifacts, full list of scaffold files created with the plan-number each `@Disabled` cites, any unexpected convergence warnings from `mvn dependency:tree`.
</output>
