---
phase: 00.1
plan: 07
type: execute
wave: 6
depends_on: ["00.1-01", "00.1-02", "00.1-03", "00.1-04", "00.1-05", "00.1-06"]
files_modified:
  - backend/rectrace/src/main/resources/application-local.properties
  - rectrace-tlm-stats/src/main/resources/application-local.properties
  - ../rectrace-local-dev/README.md
autonomous: false
requirements:
  - LOCAL-DEV-06
  - LOCAL-DEV-04
threat_model_ref: T-00.1-14

must_haves:
  truths:
    - "`backend/rectrace/src/main/resources/application-local.properties` exists in THIS repo, points at localhost:1521 Oracle (FREEPDB1 service) and localhost:9200 ES (no auth), uses the four schema-user passwords from the sibling repo's .env.example."
    - "`rectrace-tlm-stats/src/main/resources/application-local.properties` exists in THIS repo, points reconmgmt + recportal datasources at localhost:1521 Oracle with the matching schema-user credentials."
    - "Sibling repo's README.md is finalized with full Quick start, Reset, Verify, Troubleshooting sections."
    - "8-item D-0.1.23 smoke checklist PASSES against the existing Boot 2.7.16 / Java 17 codebase using the new `local` profile."
    - "Smoke command fails LOUD when the app does not reach 'Started RectraceApplication' — `grep -q` + `|| exit 1` with `set -o pipefail` (WARNING-4 fix)."
  artifacts:
    - path: "backend/rectrace/src/main/resources/application-local.properties"
      provides: "Local Spring profile pointing at the sibling repo's docker stack"
      contains: "jdbc:oracle:thin:@localhost:1521/FREEPDB1"
    - path: "rectrace-tlm-stats/src/main/resources/application-local.properties"
      provides: "Local Spring profile for rectrace-tlm-stats service"
      contains: "reconmgmt"
    - path: "../rectrace-local-dev/README.md"
      provides: "Final operator-facing documentation"
      contains: "docker compose up -d"
  key_links:
    - from: "backend/rectrace/src/main/resources/application-local.properties"
      to: "../rectrace-local-dev/.env.example"
      via: "schema-user passwords match (rectrace_pwd, autosys_pwd)"
      pattern: "rectrace_pwd"
    - from: "rectrace-tlm-stats/src/main/resources/application-local.properties"
      to: "../rectrace-local-dev/init/01-create-schema-users.sql"
      via: "reconmgmt + recportal schema users created in init script"
      pattern: "reconmgmt|recportal"
---

<objective>
Finalize Phase 0.1 by (1) creating the two `application-local.properties` files inside THIS repo so the existing Boot 2.7.16 codebase can connect to the sibling repo's Docker stack (the scope concession to D-0.1.24 documented in the planning_context), (2) expanding the sibling repo's `README.md` to operator-quality, and (3) running the D-0.1.23 8-item manual smoke checklist as the phase exit gate.

This plan now runs in wave 6 (was wave 5) — WARNING-1 cascading shift after plan 06 moved to wave 5. All dependencies (`00.1-01..00.1-06`) must complete before this plan starts.

Purpose: Per the planning_context item 5, creating the two `application-local.properties` files in THIS repo is the chosen approach (over a "user temporarily creates them manually" alternative) because Phase 1 D-1.14 inherits them already-working, avoiding manual user steps for a critical verification gate. This is an explicit deviation from D-0.1.24 — documented in the plan and in commit messages so Phase 1 knows the files already exist.

Output: Two committed properties files in THIS repo, a finalized README in the sibling repo, and a recorded PASS/FAIL on the 8-item D-0.1.23 smoke.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/00.1-local-dev-seed-bootstrap/00.1-CONTEXT.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/00.1-local-dev-seed-bootstrap/00.1-RESEARCH.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/00.1-local-dev-seed-bootstrap/00.1-VALIDATION.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/01-backend-platform-upgrade/01-CONTEXT.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/backend/rectrace/src/main/resources/application.properties
@/Users/aarun/Workspace/Projects/autosys-job-explorer/rectrace-tlm-stats/src/main/resources/application.properties
@/Users/aarun/Workspace/Projects/autosys-job-explorer/rectrace-tlm-stats/src/main/resources/application-test.properties
@/Users/aarun/Workspace/Projects/autosys-job-explorer/rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/config/DatabaseConfig.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend/rectrace and rectrace-tlm-stats application-local.properties (scope concession to D-0.1.24)</name>
  <files>
    /Users/aarun/Workspace/Projects/autosys-job-explorer/backend/rectrace/src/main/resources/application-local.properties
    /Users/aarun/Workspace/Projects/autosys-job-explorer/rectrace-tlm-stats/src/main/resources/application-local.properties
  </files>
  <read_first>
    /Users/aarun/Workspace/Projects/autosys-job-explorer/backend/rectrace/src/main/resources/application.properties (mirror property names exactly)
    /Users/aarun/Workspace/Projects/autosys-job-explorer/rectrace-tlm-stats/src/main/resources/application.properties (mirror property names exactly)
    /Users/aarun/Workspace/Projects/autosys-job-explorer/rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/config/DatabaseConfig.java lines 60-200 — the `passwordScriptPath` bean is read with `@Value("${password.script.path:/opt/rectify/control/scripts/get_password.sh}")` and ScriptExecutor.executeScript is called UNCONDITIONALLY at bean construction. The local profile MUST provide a working substitute path or the smoke fails. See WARNING-4 fix below.
    /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/01-backend-platform-upgrade/01-CONTEXT.md (D-1.14 — what Phase 1 expects these files to contain)
    /Users/aarun/Workspace/Projects/rectrace-local-dev/.env.example (passwords must match the schema-user passwords)
    /Users/aarun/Workspace/Projects/rectrace-local-dev/init/01-create-schema-users.sql (schema users + passwords)
  </read_first>
  <action>
    **Scope-concession disclosure**: Per planning_context item 5, creating these files is the chosen path (deviation from D-0.1.24's literal text). Phase 1 D-1.14 inherits them as-is.

    **WARNING-4 context**: The rectrace-tlm-stats `DatabaseConfig.java` calls `scriptExecutor.executeScript(passwordScriptPath, ...)` unconditionally (lines 80, 108, 190) and uses the script's stdout as the datasource password. The hardcoded default is `/opt/rectify/control/scripts/get_password.sh`. We CANNOT bypass this with an empty `password.script.path=` value alone — the script invocation still happens. Two viable approaches:

      (a) **Ship a tiny local-dev shim script** alongside the properties files (out of scope for THIS task — would be plan 07 Task 1b material).
      (b) **Accept that the rectrace-tlm-stats smoke needs the script + password.script.path pointing at it**, and strengthen the smoke command to FAIL LOUD when the app doesn't start (WARNING-4 fix-2). Choose (b) for now; document the gap so Phase 1 can add the shim. Until then, the `application-local.properties` file points `password.script.path` at a placeholder and the smoke will assert the app does NOT start cleanly without the shim — making the gap explicit instead of silently passing.

    For the BACKEND module (`backend/rectrace`), the situation is different: the primary `datasource.password` bean is read directly from properties (see `application.properties` line `datasource.password=…`). NO ScriptExecutor wraps it in the rectrace backend. So the backend smoke (step 1 of D-0.1.23) is fully clean and the strengthened smoke applies.

    **Write `backend/rectrace/src/main/resources/application-local.properties`**. Mirror every property key from `application.properties` but replace values to point at the sibling repo's Docker stack. The primary Oracle datasource maps to the RECTRACE schema user; the secondary `autosys.db.*` maps to the AUTOSYS schema user. Exact content:

    ```properties
    # Local development profile for the sibling repo (../rectrace-local-dev/) Docker stack.
    # Activated with: mvn spring-boot:run -Dspring.profiles.active=local
    #
    # Sources:
    # - sibling repo's .env.example for connection defaults
    # - schema users created by ../rectrace-local-dev/init/01-create-schema-users.sql
    #
    # Per Phase 0.1 planning, this file was created in Phase 0.1 (scope concession to D-0.1.24)
    # so the existing Boot 2.7.16 codebase can be verified against the seed before Phase 1 ships.
    # Phase 1 D-1.14 inherits this file; it may be refined post-Boot-upgrade if 3.3.x property
    # names change.

    spring.application.name=rectrace

    # Suppress SQL noise locally (matches Phase 1 BOOT-08 cleanup intent)
    spring.jpa.show-sql=false
    spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.Oracle12cDialect
    spring.web.resources.static-locations=classpath:/static/
    spring.mvc.static-path-pattern=/**
    spring.servlet.context-path=/rectrace

    # Primary Oracle (RECTRACE schema user in local FREEPDB1)
    datasource.url=jdbc:oracle:thin:@localhost:1521/FREEPDB1
    datasource.username=rectrace
    datasource.password=rectrace_pwd
    datasource.driver-class-name=oracle.jdbc.OracleDriver
    datasource.service-name=FREEPDB1
    datasource.db-schema=RECTRACE

    # Elasticsearch (security disabled per D-0.1.16)
    spring.elasticsearch.uris=http://localhost:9200

    # Search Configuration
    search-config.location=file:src/main/resources/search-config-v4.json
    search-config.location-old=file:src/main/resources/search-config.json

    server.port=6088

    # Secondary Oracle (AUTOSYS schema user)
    autosys.db.url=jdbc:oracle:thin:@localhost:1521/FREEPDB1
    autosys.db.username=autosys
    autosys.db.password=autosys_pwd
    autosys.db.driver-class-name=oracle.jdbc.OracleDriver
    autosys.db.schema=AUTOSYS
    autosys.db.hikari.minimum-idle=1
    autosys.db.hikari.maximum-pool-size=5
    autosys.db.hikari.connection-timeout=20000
    ```

    Notes:
    - `spring.elasticsearch.username` / `spring.elasticsearch.password` are intentionally OMITTED — D-0.1.16 disables ES security locally; the existing Boot 2.7.16 `ElasticsearchDevConfiguration` (per Phase 0 work) handles the no-auth path.
    - Hikari pool sizes are reduced for local dev (`minimum-idle=1`, `maximum-pool-size=5`) — no need for the prod sizes.

    **Write `rectrace-tlm-stats/src/main/resources/application-local.properties`** mirroring `rectrace-tlm-stats/src/main/resources/application.properties`. Per WARNING-4 the `password.script.path` property is set to a placeholder and the smoke step 1 will (correctly) fail loud if the local-dev shim script is missing. Exact content:

    ```properties
    # Local development profile for the sibling repo's Docker stack.
    # Activated with: mvn spring-boot:run -Dspring.profiles.active=local
    # See backend/rectrace/.../application-local.properties for the broader rationale.

    server.port=8080
    spring.application.name=rectrace-tlm-stats

    logging.level.com.citi.gru.rectrace.tlmstats=INFO
    logging.level.org.springframework.web=INFO
    logging.level.org.springframework.jdbc=INFO

    management.endpoints.web.exposure.include=health,info
    management.endpoint.health.show-details=always

    # Reconmgmt datasource (RECONMGMT schema user in local FREEPDB1)
    reconmgmt.datasource.url=jdbc:oracle:thin:@localhost:1521/FREEPDB1
    reconmgmt.datasource.username=reconmgmt
    reconmgmt.datasource.password=reconmgmt_pwd
    reconmgmt.datasource.service-name=FREEPDB1
    reconmgmt.datasource.db-schema=RECONMGMT
    reconmgmt.datasource.driver-class-name=oracle.jdbc.OracleDriver

    # Recportal datasource (RECPORTAL schema user in local FREEPDB1)
    recportal.datasource.url=jdbc:oracle:thin:@localhost:1521/FREEPDB1
    recportal.datasource.username=recportal
    recportal.datasource.password=recportal_pwd
    recportal.datasource.service-name=FREEPDB1
    recportal.datasource.db-schema=RECPORTAL
    recportal.datasource.driver-class-name=oracle.jdbc.OracleDriver

    # WARNING-4 note: DatabaseConfig.java calls scriptExecutor.executeScript(passwordScriptPath, ...)
    # UNCONDITIONALLY at bean construction (see lines 80, 108, 190). The local profile cannot
    # bypass it by setting password.script.path empty — the executeScript call still fires.
    # Until Phase 1 adds a local-dev shim script, point this at a placeholder so the failure
    # mode is LOUD (smoke step 1 will fail with a clear "script not found" error) rather than
    # silently passing on stale state.
    password.script.path=/tmp/rectrace-local-dev-password.sh

    # Connection Pool (small for local dev)
    spring.datasource.hikari.maximum-pool-size=5
    spring.datasource.hikari.minimum-idle=1
    spring.datasource.hikari.connection-timeout=20000
    spring.datasource.hikari.idle-timeout=600000
    spring.datasource.hikari.max-lifetime=1800000
    ```

    **Verify both modules compile** with the new profile by running a fast compile (NOT a full test run — Phase 0 already proves compile is green):
    ```
    cd /Users/aarun/Workspace/Projects/autosys-job-explorer
    mvn -pl backend/rectrace compile -q
    mvn -pl rectrace-tlm-stats compile -q
    ```

    **Sanity-check the BACKEND profile loads** with a STRICT smoke (WARNING-4 fix-2): `set -o pipefail`, `grep -q` for the literal "Started RectraceApplication" line, `|| exit 1` on failure. The rectrace-tlm-stats smoke is intentionally NOT run here (it would fail without a password shim; that's expected and documented above).

    ```bash
    set -o pipefail
    cd /Users/aarun/Workspace/Projects/autosys-job-explorer
    rm -f /tmp/rectrace-local-smoke.log
    timeout 90 mvn -pl backend/rectrace spring-boot:run \
      -Dspring-boot.run.profiles=local \
      -Dspring-boot.run.arguments="--spring.main.web-application-type=none --spring.main.banner-mode=off" \
      > /tmp/rectrace-local-smoke.log 2>&1 &
    PID=$!
    # Poll for the success line, max 60s
    end=$((SECONDS+60))
    STARTED=0
    while [ $SECONDS -lt $end ]; do
      if grep -q "Started RectraceApplication" /tmp/rectrace-local-smoke.log; then
        STARTED=1
        break
      fi
      # Fast-fail on common showstoppers so we don't burn the full 60s on dead boot
      if grep -qE "ORA-|ElasticsearchException|UnknownHostException|APPLICATION FAILED TO START" /tmp/rectrace-local-smoke.log; then
        break
      fi
      sleep 2
    done
    kill $PID 2>/dev/null || true
    wait $PID 2>/dev/null || true
    if [ $STARTED -ne 1 ]; then
      echo "FAIL: backend rectrace did not reach 'Started RectraceApplication' within 60s. Last 50 lines:"
      tail -n 50 /tmp/rectrace-local-smoke.log
      exit 1
    fi
    echo "OK: backend rectrace smoke passed (Started + no ORA-/ElasticsearchException in window)"
    # Also assert no ORA- or ElasticsearchException appeared during the window
    if grep -qE "ORA-|ElasticsearchException|UnknownHostException" /tmp/rectrace-local-smoke.log; then
      echo "FAIL: error markers found in smoke log:"
      grep -E "ORA-|ElasticsearchException|UnknownHostException" /tmp/rectrace-local-smoke.log
      exit 1
    fi
    echo "OK: no error markers in smoke log"
    ```

    (The `web-application-type=none` lets it boot to the data-access layer and exit quickly without binding port 6088 — sufficient to validate the connection strings parse.)

    **Commit to THIS repo (autosys-job-explorer)** on `milestone/modernization` branch (per Phase 0 D-15):
    ```
    cd /Users/aarun/Workspace/Projects/autosys-job-explorer
    git add backend/rectrace/src/main/resources/application-local.properties rectrace-tlm-stats/src/main/resources/application-local.properties
    git commit -m "$(cat <<'EOF'
docs(00.1): add application-local.properties for both modules

Per Phase 0.1 plan 07: local-profile properties files committed in THIS
repo as a scope concession to D-0.1.24 so the existing Boot 2.7.16
codebase can verify the sibling repo's seed before Phase 1 starts.

- backend/rectrace: primary RECTRACE + secondary AUTOSYS datasources +
  Elasticsearch on http://localhost:9200 (no auth, per D-0.1.16).
- rectrace-tlm-stats: RECONMGMT + RECPORTAL datasources on local Oracle.
  password.script.path points at a placeholder; smoke will fail loud
  until Phase 1 adds a local-dev shim script (WARNING-4 gap documented
  in the file header).

Phase 1 D-1.14 inherits these files as-is. Connection strings match the
sibling repo's docker-compose.yml service ports and init script's
schema users.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
    ```
  </action>
  <verify>
    <automated>test -f /Users/aarun/Workspace/Projects/autosys-job-explorer/backend/rectrace/src/main/resources/application-local.properties \
      &amp;&amp; test -f /Users/aarun/Workspace/Projects/autosys-job-explorer/rectrace-tlm-stats/src/main/resources/application-local.properties \
      &amp;&amp; grep -q 'jdbc:oracle:thin:@localhost:1521/FREEPDB1' /Users/aarun/Workspace/Projects/autosys-job-explorer/backend/rectrace/src/main/resources/application-local.properties \
      &amp;&amp; grep -q '^datasource.username=rectrace$' /Users/aarun/Workspace/Projects/autosys-job-explorer/backend/rectrace/src/main/resources/application-local.properties \
      &amp;&amp; grep -q '^autosys.db.username=autosys$' /Users/aarun/Workspace/Projects/autosys-job-explorer/backend/rectrace/src/main/resources/application-local.properties \
      &amp;&amp; grep -q '^spring.elasticsearch.uris=http://localhost:9200$' /Users/aarun/Workspace/Projects/autosys-job-explorer/backend/rectrace/src/main/resources/application-local.properties \
      &amp;&amp; grep -q '^reconmgmt.datasource.username=reconmgmt$' /Users/aarun/Workspace/Projects/autosys-job-explorer/rectrace-tlm-stats/src/main/resources/application-local.properties \
      &amp;&amp; grep -q '^recportal.datasource.username=recportal$' /Users/aarun/Workspace/Projects/autosys-job-explorer/rectrace-tlm-stats/src/main/resources/application-local.properties \
      &amp;&amp; grep -q '^password.script.path=' /Users/aarun/Workspace/Projects/autosys-job-explorer/rectrace-tlm-stats/src/main/resources/application-local.properties \
      &amp;&amp; cd /Users/aarun/Workspace/Projects/autosys-job-explorer \
      &amp;&amp; mvn -pl backend/rectrace compile -q \
      &amp;&amp; mvn -pl rectrace-tlm-stats compile -q \
      &amp;&amp; bash -c 'set -o pipefail; rm -f /tmp/rectrace-local-smoke.log; timeout 90 mvn -pl backend/rectrace spring-boot:run -Dspring-boot.run.profiles=local -Dspring-boot.run.arguments="--spring.main.web-application-type=none --spring.main.banner-mode=off" > /tmp/rectrace-local-smoke.log 2>&amp;1 &amp; PID=$!; end=$((SECONDS+60)); STARTED=0; while [ $SECONDS -lt $end ]; do if grep -q "Started RectraceApplication" /tmp/rectrace-local-smoke.log; then STARTED=1; break; fi; if grep -qE "ORA-|ElasticsearchException|UnknownHostException|APPLICATION FAILED TO START" /tmp/rectrace-local-smoke.log; then break; fi; sleep 2; done; kill $PID 2>/dev/null || true; wait $PID 2>/dev/null || true; if [ $STARTED -ne 1 ]; then echo "FAIL: not started"; tail -n 50 /tmp/rectrace-local-smoke.log; exit 1; fi; if grep -qE "ORA-|ElasticsearchException|UnknownHostException" /tmp/rectrace-local-smoke.log; then echo "FAIL: error markers"; grep -E "ORA-|ElasticsearchException|UnknownHostException" /tmp/rectrace-local-smoke.log; exit 1; fi; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - Both `application-local.properties` files exist at the documented paths.
    - Both files reference `jdbc:oracle:thin:@localhost:1521/FREEPDB1`.
    - Backend file uses `rectrace`/`rectrace_pwd` for the primary datasource and `autosys`/`autosys_pwd` for the secondary `autosys.db.*` datasource.
    - Backend file points ES at `http://localhost:9200` with no username/password (D-0.1.16).
    - TLM-stats file uses `reconmgmt`/`reconmgmt_pwd` for `reconmgmt.datasource.*` and `recportal`/`recportal_pwd` for `recportal.datasource.*`.
    - TLM-stats file sets `password.script.path` explicitly (WARNING-4: surfaces the gap visibly rather than relying on the global default).
    - `mvn compile` succeeds for both modules.
    - **WARNING-4 strict smoke**: the backend smoke command uses `set -o pipefail`, `grep -q "Started RectraceApplication"`, asserts STARTED==1 with `exit 1` on failure, and rejects any `ORA-|ElasticsearchException|UnknownHostException` line in the log. Exits 1 on any failure, not 0.
    - Both files committed to `milestone/modernization` branch in THIS repo.
    - No file under `backend/rectrace/src/main/java/` or `rectrace-tlm-stats/src/main/java/` modified (only the `.properties` files).
  </acceptance_criteria>
  <done>Two `application-local.properties` files committed; both modules compile; backend `local` profile passes strict smoke (WARNING-4).</done>
</task>

<task type="auto">
  <name>Task 2: Finalize sibling repo README.md with full operator documentation</name>
  <files>
    /Users/aarun/Workspace/Projects/rectrace-local-dev/README.md
  </files>
  <read_first>
    /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/00.1-local-dev-seed-bootstrap/00.1-CONTEXT.md decisions D-0.1.3, D-0.1.13, D-0.1.14, D-0.1.18, D-0.1.20, D-0.1.21
    /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/00.1-local-dev-seed-bootstrap/00.1-RESEARCH.md §"Pitfall 1: Apple Silicon", §"Pitfall 2: gvenzl Init Scripts Only Run on First Boot", §"Pitfall 7: docker compose down Doesn't Wipe Data"
    /Users/aarun/Workspace/Projects/rectrace-local-dev/README.md (current skeleton from plan 00.1-01 Task 2)
    /Users/aarun/Workspace/Projects/rectrace-local-dev/apply.py (the operator-facing entry point)
    /Users/aarun/Workspace/Projects/rectrace-local-dev/docker-compose.yml
  </read_first>
  <action>
    Expand the README to operator quality. Keep the existing skeleton headings; fill in each section.

    **`# rectrace-local-dev`** (existing H1; keep)

    **`## What this is`** — refine to: "Standalone, Citi-disconnected local Oracle 23c + Elasticsearch 8.13.4 stack for developing against the `rectrace` project. All seed data is fabricated and generic; no Citi schemas, no Citi credentials, no Citi terminology — safe to back up to any personal git remote. Configured for macOS (Apple Silicon and Intel) but multi-arch Docker images work on Linux/amd64 too."

    **`## Prerequisites`** — bulleted:
    - Docker Engine 29.x + Docker Compose v2 (verified: 29.4.3 / v5.1.3).
    - Python 3.12+ — recommended via pyenv. The repo includes `.venv/` (gitignored); activate or use `./.venv/bin/python`.
    - No Oracle Instant Client required — Python `oracledb` thin mode talks to Oracle directly.
    - Optional: Oracle Instant Client (Basic) at `/Users/aarun/oracle/instantclient/` enables `oracledb` thick mode. To opt in, set `RECTRACE_LOCAL_THICK_MODE=1` and modify `apply.py` to call `oracledb.init_oracle_client(lib_dir='/Users/aarun/oracle/instantclient')`. Thin mode is the documented default per D-0.1.20.
    - Optional: SQL*Plus from Oracle's Instant Client SQL*Plus add-on — drops into the same Instant Client directory; only needed for ad-hoc DB inspection.

    **`## Quick start`** — fenced block:
    ```bash
    # 1. Copy env defaults (.env is gitignored)
    cp .env.example .env

    # 2. Bootstrap a Python venv with pinned deps
    python3 -m venv .venv
    ./.venv/bin/pip install -r requirements.txt

    # 3. Start the stack (Oracle takes 60-120s on first boot)
    docker compose up -d
    # Poll docker inspect on the pinned container_name keys
    docker inspect --format='{{.State.Health.Status}}' rectrace-oracle
    docker inspect --format='{{.State.Health.Status}}' rectrace-es
    # Wait until both report "healthy"

    # 4. Apply schema + seed data + ES index
    ./.venv/bin/python apply.py
    # Or: ./.venv/bin/python apply.py --oracle-only  (skip ES)
    # Or: ./.venv/bin/python apply.py --es-only      (skip Oracle)

    # 5. Verify
    ./.venv/bin/python apply.py --verify
    ```

    **`## Reset`** — fenced block:
    ```bash
    # Fast reset (~5s) — drops + recreates schemas + ES index, container stays up.
    ./.venv/bin/python apply.py --reset

    # Nuclear reset (~2-3 min) — wipes Oracle datafiles, re-runs init script,
    # then re-applies everything. Use when init/01-create-schema-users.sql changes.
    docker compose down -v
    docker compose up -d
    # wait for both healthy
    ./.venv/bin/python apply.py
    ```
    Add a note explaining Pitfall 2: schema users are created ONLY on first volume init; adding a new schema user means `docker compose down -v` is mandatory.

    **`## Verify`** — what `apply.py --verify` does + the 8-item D-0.1.23 smoke pointer:
    ```
    ./.venv/bin/python apply.py --verify
    # Checks:
    #   - Oracle: 11 expected tables have expected row counts
    #     (rectrace_core, ujo_job, ujo_job_status, autosys_tlm_recon_sequences,
    #      autosys_all_jobs_data, recon_bank, mr_csum_man_match_stats_hist,
    #      mr_csum_man_match_details, mr_csum_netting_hist, quickrec_stats_table,
    #      recportal_manual_match_table)
    #   - Elasticsearch: rectrace_core_index has 5 docs
    #   - Elasticsearch: term query set_id.keyword=SET-ABC-123 returns 1 hit
    #     (validates the Phase 8 hyphen-bug fix prerequisite)
    # Exit 0 = all checks pass.
    ```
    Plus: "To verify the existing Spring Boot 2.7.16 application against this stack, run from the parent `autosys-job-explorer/` repo:"
    ```
    cd ../autosys-job-explorer
    mvn -pl backend/rectrace spring-boot:run -Dspring-boot.run.profiles=local

    # In a second terminal:
    mvn -pl rectrace-tlm-stats spring-boot:run -Dspring-boot.run.profiles=local
    # NOTE: rectrace-tlm-stats currently fails to start under the `local` profile
    # because DatabaseConfig unconditionally invokes scriptExecutor.executeScript
    # against password.script.path. Until Phase 1 adds a local-dev shim, only the
    # backend/rectrace + frontend tier are exercised in this smoke.

    # In a third terminal (Angular UI):
    cd frontend/rectrace && npm start
    # Open http://localhost:4200
    ```

    **`## Troubleshooting`** — table:
    - ES container exits ~10s after start with SIGILL → Apple Silicon variant; check `docker-compose.yml` has `-XX:UseSVE=0` in `ES_JAVA_OPTS` (Pitfall 1).
    - `ORA-01017: invalid username/password` → added a new schema user without `docker compose down -v` (Pitfall 2).
    - `docker compose down` left old data around → run `docker compose down -v` (volumes flag) instead (Pitfall 7).
    - `oracledb.DatabaseError: DPY-6005: cannot connect` for ~2 minutes after `docker compose up -d` → Oracle cold boot; `apply.py` waits with backoff, so just re-run it.
    - rectrace-tlm-stats startup fails with "script not found" on `/tmp/rectrace-local-dev-password.sh` → expected gap until Phase 1 ships the local-dev shim. For now, run only `backend/rectrace` + Angular for the smoke.
    - Elasticsearch returns dynamic-mapping warnings → the mapping in `es/rectrace_core_index.mapping.json` must be applied BEFORE any docs are indexed. `apply.py --reset` does this correctly.
    - ES bulk insert fails with version-mismatch warning → upgrade `elasticsearch` PyPI package only within the `>=8.13,<8.14` pin (Pitfall 5).

    Commit:
    ```
    git -C /Users/aarun/Workspace/Projects/rectrace-local-dev/ add README.md
    git -C /Users/aarun/Workspace/Projects/rectrace-local-dev/ commit -m "Finalize README: quick start, reset, verify, troubleshooting"
    ```
  </action>
  <verify>
    <automated>test -f /Users/aarun/Workspace/Projects/rectrace-local-dev/README.md \
      &amp;&amp; grep -q '^## Quick start$' /Users/aarun/Workspace/Projects/rectrace-local-dev/README.md \
      &amp;&amp; grep -q '^## Reset$' /Users/aarun/Workspace/Projects/rectrace-local-dev/README.md \
      &amp;&amp; grep -q '^## Verify$' /Users/aarun/Workspace/Projects/rectrace-local-dev/README.md \
      &amp;&amp; grep -q '^## Troubleshooting$' /Users/aarun/Workspace/Projects/rectrace-local-dev/README.md \
      &amp;&amp; grep -q 'docker compose up -d' /Users/aarun/Workspace/Projects/rectrace-local-dev/README.md \
      &amp;&amp; grep -q 'apply.py --reset' /Users/aarun/Workspace/Projects/rectrace-local-dev/README.md \
      &amp;&amp; grep -q 'apply.py --verify' /Users/aarun/Workspace/Projects/rectrace-local-dev/README.md \
      &amp;&amp; grep -q '\-XX:UseSVE=0' /Users/aarun/Workspace/Projects/rectrace-local-dev/README.md \
      &amp;&amp; grep -q 'docker compose down -v' /Users/aarun/Workspace/Projects/rectrace-local-dev/README.md \
      &amp;&amp; wc -l /Users/aarun/Workspace/Projects/rectrace-local-dev/README.md | awk '$1 >= 60 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - README has all six required sections (existing What this is, Prerequisites; new full content in Quick start, Reset, Verify, Troubleshooting).
    - Contains fenced code blocks (```bash) for: `docker compose up -d`, `apply.py --reset`, `apply.py --verify`, `mvn -pl backend/rectrace spring-boot:run`.
    - Mentions Pitfall references (UseSVE=0, docker compose down -v).
    - References the rectrace-tlm-stats password-script gap so the operator isn't surprised.
    - File length ≥ 60 lines.
    - Sibling repo's `git log --oneline | wc -l` reflects this additional commit.
  </acceptance_criteria>
  <done>Operator-quality README committed to sibling repo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User runs D-0.1.23 8-item smoke checklist against Boot 2.7.16 codebase</name>
  <files>
    (no files modified — manual verification step)
  </files>
  <action>
    See the &lt;how-to-verify&gt; block below. This is a checkpoint:human-verify task; the actual work is performed by the user running the 8-item smoke checklist documented in 00.1-VALIDATION.md §"Manual-Only Verifications". Claude pauses and waits for the resume signal.
  </action>
  <verify>
    <automated>echo "Manual checkpoint — user must record 8/8 PASS via resume signal."</automated>
  </verify>
  <done>User types "approved — 8/8 PASS" (or reports a blocker).</done>
  <what-built>
    A fully seeded local stack (Oracle + ES via Docker Compose), an idempotent `apply.py` driver, two `application-local.properties` files inside this repo for the existing Boot 2.7.16 / Java 17 codebase, and a finalized README in the sibling repo.

    The D-0.1.23 smoke is the phase exit gate. Per planning_context this MUST be run against the EXISTING pre-Phase-1 Boot 2.7.16 / Java 17 stack, NOT post-upgrade.
  </what-built>
  <how-to-verify>
    Run the 8-item D-0.1.23 smoke checklist exactly as authored in `00.1-VALIDATION.md` §"Manual-Only Verifications" + RESEARCH.md §"D-0.1.23 Smoke Verification Checklist". Pre-flight from the sibling repo's directory:

    ```bash
    cd /Users/aarun/Workspace/Projects/rectrace-local-dev/
    docker inspect --format='{{.State.Health.Status}}' rectrace-oracle  # healthy
    docker inspect --format='{{.State.Health.Status}}' rectrace-es      # healthy
    ./.venv/bin/python apply.py --verify   # exit 0
    ```

    Then start the backend (terminal A):
    ```bash
    cd /Users/aarun/Workspace/Projects/autosys-job-explorer
    mvn -pl backend/rectrace spring-boot:run -Dspring-boot.run.profiles=local
    # Wait for "Started RectraceApplication in NN seconds" log line.
    # Check 1: No ORA-* or ElasticsearchException in the logs.
    ```

    Terminal B — rectrace-tlm-stats (KNOWN GAP — see WARNING-4 in plan 07 Task 1; expected to fail with "script not found" until Phase 1 adds the shim):
    ```bash
    cd /Users/aarun/Workspace/Projects/autosys-job-explorer
    mvn -pl rectrace-tlm-stats spring-boot:run -Dspring-boot.run.profiles=local
    # Expected: APPLICATION FAILED TO START referencing /tmp/rectrace-local-dev-password.sh.
    # Record as "KNOWN GAP" in the smoke results; do NOT count as a smoke failure
    # for Phase 0.1 exit (the gap is documented + handed off to Phase 1).
    ```

    Terminal C — Angular UI:
    ```bash
    cd /Users/aarun/Workspace/Projects/autosys-job-explorer/frontend/rectrace
    npm install   # if not already
    npm start
    # Browser: http://localhost:4200
    ```

    Run each of the 8 checks and record the result:

    1. **App starts on `local` profile** — Terminal A shows `Started RectraceApplication in NN seconds`; no ORA-* / ElasticsearchException lines.
    2. **V4 keyword search returns rows** — from a 4th terminal:
       ```bash
       curl -X POST http://localhost:6088/rectrace/api/v4/search/initial \
         -H 'Content-Type: application/json' \
         -H 'x-citiportal-loginid: testuser' \
         -d '{"searchTerm":"SAMPLE","category":"jobName"}'
       ```
       Returns rows referencing seed jobs (`SAMPLE_TRADE_RECON_001`, etc.).
    3. **Suggestion endpoint works**:
       ```bash
       curl 'http://localhost:6088/rectrace/api/search/suggest?term=SAM&field=job_name'
       ```
       Returns at least 1 suggestion.
    4. **Execution-order graph renders multi-node** — Angular UI, click execution-order button on a `SAMPLE_TRADE_RECON_001` row; modal opens; Cytoscape shows 3 nodes (PRE/MAIN/POST).
    5. **TLM-stats modal opens with charts** — Angular UI, click set_id renderer on a `SETID_001` row; modal opens; charts have 10 days of data (SYSDATE-relative — WARNING-5).
       NOTE: This step depends on rectrace-tlm-stats running. If terminal B is failing per the known gap, mark as "KNOWN GAP — covered by Phase 1 shim".
    6. **QuickRec modal opens with data** — Angular UI, click recon_id renderer on a `RID-001-NA` row; QuickRec modal opens; shows 2 rows.
       Same KNOWN GAP caveat as step 5.
    7. **Hyphenated identifier search via `.keyword`**:
       ```bash
       curl -X POST 'http://localhost:9200/rectrace_core_index/_search' \
         -H 'Content-Type: application/json' \
         -d '{"query":{"term":{"set_id.keyword":"SET-ABC-123"}}}'
       ```
       Returns exactly 1 hit.
    8. **Reset is idempotent**:
       ```bash
       cd /Users/aarun/Workspace/Projects/rectrace-local-dev/
       ./.venv/bin/python apply.py --reset
       ./.venv/bin/python apply.py --verify   # exit 0
       ./.venv/bin/python apply.py --reset
       ./.venv/bin/python apply.py --verify   # exit 0 again
       ```

    Record the result of each step (PASS/FAIL/KNOWN-GAP plus any anomalies) in the per-plan summary file. **Any unexpected FAIL (i.e. not a documented KNOWN GAP) blocks phase completion** — re-open the relevant plan to debug.

    Step 7's NDJSON hyphen scenario (`SET-ABC-123`) returning exactly 1 hit is the Phase 8 dry-run prerequisite — its PASS proves the `.keyword` multi-field plumbing is wired correctly and Phase 8 can dry-run the fix locally.
  </how-to-verify>
  <resume-signal>Type "approved — 8/8 PASS (with N KNOWN GAPS documented)" once every check returns PASS or a documented KNOWN GAP. If any check FAILs unexpectedly, type "blocked: step N failed — <details>" so the orchestrator can flag the failing plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `application-local.properties` (in THIS repo) → committed to milestone/modernization branch | The file contains plaintext local-dev passwords matching the sibling repo's .env.example. These are non-secret (documented in CONTEXT.md D-0.1.13 as "local-dev-only strings"). |
| Boot 2.7.16 JVM → Oracle/ES on localhost | Service auth is the plaintext local passwords; ES has no auth. Acceptable for local dev only — the `local` profile MUST NOT be activated in any deployed environment. Documented in the properties file's leading comment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00.1-14 | Information Disclosure | The `local` profile is activated in a non-laptop environment (UAT, prod) by mistake | mitigate | The properties file's header comment explicitly states "Activated with: mvn spring-boot:run -Dspring.profiles.active=local" and "DO NOT enable in any deployed environment." Phase 9 (Domain Security) will add a startup guard that fails if `local` profile is active outside `Local-Dev` mode. Out of Phase 0.1 scope to add the guard; deferred. |
| T-00.1-15 | Information Disclosure | Plaintext credentials committed to milestone/modernization branch | accept | Credentials match the sibling repo's `.env.example` — local-only, fabricated, will never authenticate against any real Citi datasource. Already documented in commit message. |
| T-00.1-16 | Tampering | A future PR modifies `application-local.properties` to point at a non-local host, then merges — opening cross-environment data leakage | accept | Risk mitigated by the file header's explicit guidance and PR review discipline. Hard guard would require Phase 9 work (startup-time profile vs. hostname check) — deferred per project schedule. |
</threat_model>

<verification>
- Both `.properties` files exist in their respective `src/main/resources/` directories.
- Sibling repo's README has all required sections.
- Backend strict smoke (WARNING-4): app reaches `Started RectraceApplication` AND no `ORA-/Elasticsearch/UnknownHost` error markers — exits 0 only on full pass.
- D-0.1.23 8-item smoke returns PASS (or documented KNOWN GAP for rectrace-tlm-stats steps 5+6) on each check (recorded in the SUMMARY).
</verification>

<success_criteria>
- This repo has two new `application-local.properties` files committed on `milestone/modernization`.
- Sibling repo's README is operator-complete.
- D-0.1.23 smoke checklist 8/8 PASS or documented KNOWN GAP, recorded by the user via the checkpoint resume signal.
- Phase 0.1 ready to mark complete in ROADMAP.md + STATE.md.
</success_criteria>

<output>
After completion, create `.planning/phases/00.1-local-dev-seed-bootstrap/00.1-07-SUMMARY.md` in THIS repo. Include the 8-item D-0.1.23 smoke results table (which checks PASSED, which FAILED with notes if any, which were marked KNOWN GAP). The summary is the phase exit artifact.
</output>
</content>
</invoke>