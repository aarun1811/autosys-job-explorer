---
phase: 02-react-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/rectrace/pom.xml
  - backend/rectrace/src/main/java/com/citi/gru/rectrace/config/CorrelationIdPropagationConfig.java
  - backend/rectrace/src/main/resources/logback-spring.xml
  - backend/rectrace/src/main/resources/application-local.properties
  - rectrace-tlm-stats/pom.xml
  - rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/config/CorrelationIdPropagationConfig.java
  - rectrace-tlm-stats/src/main/resources/logback-spring.xml
  - rectrace-tlm-stats/src/main/resources/application-local.properties
autonomous: true
requirements:
  - REACT-07

must_haves:
  truths:
    - "backend/rectrace/pom.xml and rectrace-tlm-stats/pom.xml both declare io.micrometer:micrometer-tracing-bridge-brave dependency (no version — BOM-managed)"
    - "CorrelationIdPropagationConfig.java exists in BOTH modules with a @Bean Propagation.Factory method, @Profile(\"!test\") annotation, and the 32-hex regex constant HEX32"
    - "logback-spring.xml exists in BOTH modules with pattern containing %X{traceId} (no %X{x-correlation-id} MDC key — under Option B traceId IS the correlation ID)"
    - "Both application-local.properties contain management.tracing.sampling.probability=1.0 and NEITHER contains management.tracing.baggage.remote-fields or management.tracing.baggage.correlation.fields"
    - "Behavioral round-trip (X-Correlation-Id header arrives at backend as the traceId) is verified in Plan 02-04 Wave 3 via scripts/smoke-correlation-id.sh"
  artifacts:
    - path: "backend/rectrace/pom.xml"
      provides: "Backend Maven manifest with tracing bridge"
      contains: "micrometer-tracing-bridge-brave"
    - path: "rectrace-tlm-stats/pom.xml"
      provides: "TLM Stats Maven manifest with tracing bridge"
      contains: "micrometer-tracing-bridge-brave"
    - path: "backend/rectrace/src/main/resources/logback-spring.xml"
      provides: "Logback config with traceId MDC pattern"
      contains: "%X{traceId"
    - path: "rectrace-tlm-stats/src/main/resources/logback-spring.xml"
      provides: "Same for TLM Stats module"
      contains: "%X{traceId"
    - path: "backend/rectrace/src/main/java/com/citi/gru/rectrace/config/CorrelationIdPropagationConfig.java"
      provides: "Custom Propagation.Factory bean with @Profile(!test) guard and 32-hex regex"
      contains: "HEX32"
    - path: "rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/config/CorrelationIdPropagationConfig.java"
      provides: "Duplicate for TLM Stats module"
      contains: "HEX32"
  key_links:
    - from: "backend/rectrace/src/main/resources/application-local.properties"
      to: "CorrelationIdPropagationConfig.java"
      via: "management.tracing.sampling.probability=1.0 enables full sampling; custom Propagation.Factory bean parses X-Correlation-Id header as the traceId"
      pattern: "management.tracing.sampling.probability"
    - from: "CorrelationIdPropagationConfig.java"
      to: "logback-spring.xml"
      via: "Brave bridge populates MDC traceId from the X-Correlation-Id header value; logback pattern reads %X{traceId}"
      pattern: "%X\\{traceId"
---

<objective>
Wire Micrometer Tracing (Brave bridge) into both Maven modules so that the `X-Correlation-Id` HTTP header sent by the React fetch wrapper is adopted as the backend's `traceId` via a custom Brave `Propagation.Factory` and written to every log line via `%X{traceId}` in MDC.

Purpose: This plan closes REACT-07 on the backend side. It implements D-2.10 (Option B — custom Propagation.Factory, NOT baggage config) so that X-Correlation-Id IS the traceId: single ID end-to-end, no two-IDs-for-one-request confusion. It is a pre-requisite for Plan 04's smoke-correlation-id.sh script and for Plan 03's end-to-end SSRM correlation validation.

Output: Both Maven modules build and boot cleanly; sending `X-Correlation-Id: <32-hex>` to any endpoint causes that exact hex value to appear as the `traceId` in the console log.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-react-foundation/02-CONTEXT.md
@.planning/phases/02-react-foundation/02-RESEARCH.md
@.planning/phases/02-react-foundation/02-PATTERNS.md

<interfaces>
<!-- Existing config class pattern to follow: -->
<!-- backend/rectrace/src/main/java/com/citi/gru/rectrace/config/SecurityConfig.java -->
<!--   @Profile("!test")         <- line 29 -->
<!--   @Configuration             <- line 30 -->
<!--   @EnableWebSecurity         <- NOT copied; remove for CorrelationIdPropagationConfig -->
<!--   public class SecurityConfig { -->
<!--     @Bean                    <- line 35 -->
<!--     public SecurityFilterChain securityFilterChain(...) { ... } -->
<!--   } -->

<!-- Existing pom.xml dependency block pattern (BOM-managed, no explicit version): -->
<!-- backend/rectrace/pom.xml lines 33-93 — existing deps follow this pattern: -->
<!--   <dependency> -->
<!--       <groupId>org.springframework.boot</groupId> -->
<!--       <artifactId>spring-boot-starter-web</artifactId> -->
<!--   </dependency> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add micrometer-tracing-bridge-brave to both Maven POMs and write CorrelationIdPropagationConfig (Option B — custom Propagation.Factory)</name>
  <files>
    backend/rectrace/pom.xml,
    rectrace-tlm-stats/pom.xml,
    backend/rectrace/src/main/java/com/citi/gru/rectrace/config/CorrelationIdPropagationConfig.java,
    rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/config/CorrelationIdPropagationConfig.java
  </files>

  <read_first>
    1. backend/rectrace/pom.xml — read existing dependency block structure to find the correct insertion point (after the last existing dependency, before &lt;/dependencies&gt;)
    2. rectrace-tlm-stats/pom.xml — same
    3. backend/rectrace/src/main/java/com/citi/gru/rectrace/config/SecurityConfig.java — copy @Profile("!test") + @Configuration annotation set (lines 29-32); do NOT copy @EnableWebSecurity
    4. .planning/phases/02-react-foundation/02-CONTEXT.md — D-2.9 (Brave bridge, BOM version 1.5.11, no exporter) and D-2.10 (custom Propagation.Factory — Option B, NOT baggage config)
  </read_first>

  <action>
    **backend/rectrace/pom.xml** — Add INSIDE the existing `<dependencies>` block after the last dependency entry (no version — BOM-managed):

        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-tracing-bridge-brave</artifactId>
        </dependency>

    BOM-managed version via Boot 3.5.14 parent: 1.5.11. Do NOT add an explicit `<version>` tag (violates D-1.3 cross-module alignment rule).

    **rectrace-tlm-stats/pom.xml** — Add the identical dependency block at the same position.

    **backend/rectrace/src/main/java/com/citi/gru/rectrace/config/CorrelationIdPropagationConfig.java** — Create new file implementing D-2.10 Option B (custom Propagation.Factory). Package: `com.citi.gru.rectrace.config`.

    Implement the following class EXACTLY (do not simplify or replace with baggage config — this IS the D-2.10 requirement):

        package com.citi.gru.rectrace.config;

        import brave.propagation.B3Propagation;
        import brave.propagation.Propagation;
        import brave.propagation.TraceContext;
        import brave.propagation.TraceContextOrSamplingFlags;
        import org.springframework.context.annotation.Bean;
        import org.springframework.context.annotation.Configuration;
        import org.springframework.context.annotation.Profile;

        import java.util.regex.Pattern;

        @Configuration
        @Profile("!test")
        public class CorrelationIdPropagationConfig {

            private static final String HEADER = "X-Correlation-Id";
            private static final Pattern HEX32 = Pattern.compile("^[a-fA-F0-9]{32}$");

            /**
             * Registers a custom Brave Propagation.Factory that reads the X-Correlation-Id
             * HTTP header and adopts it as the backend's 128-bit traceId (D-2.10).
             *
             * A UUID v4 with dashes stripped is exactly 32 hex chars — a valid W3C traceparent
             * 128-bit traceId. This means the X-Correlation-Id the user sees in error UI
             * IS the backend's traceId: single ID end-to-end, no two-IDs-for-one-request confusion.
             *
             * Security: the 32-hex regex in extractor() is the validation boundary for T-2-02.
             * Header values that fail the regex (including log-injection strings like "foo\n[...]")
             * are rejected — Brave generates a fresh traceId instead. No OncePerRequestFilter needed.
             *
             * inject(): echoes X-Correlation-Id on outbound calls (lowercase hex of traceId)
             * AND writes B3 downstream headers via the delegate.
             *
             * Phase 7 OBS-01 owns the full observability pipeline (exporters, structured logs).
             * Phase 2 scope: MDC population only, no Zipkin/Jaeger/OTel exporter.
             *
             * Decision refs: D-2.9, D-2.10, D-2.12
             */
            @Bean
            public Propagation.Factory correlationIdPropagationFactory() {
                return new Propagation.Factory() {
                    @Override
                    public <K> Propagation<K> create(Propagation.KeyFactory<K> keyFactory) {
                        final Propagation<K> delegate = B3Propagation.FACTORY.create(keyFactory);
                        final K headerKey = keyFactory.create(HEADER);
                        return new Propagation<K>() {
                            @Override public java.util.List<K> keys() {
                                java.util.List<K> all = new java.util.ArrayList<>(delegate.keys());
                                all.add(headerKey);
                                return all;
                            }
                            @Override public <R> TraceContext.Injector<R> injector(Propagation.Setter<R, K> setter) {
                                // Inject B3 downstream AND echo X-Correlation-Id (lowercase hex of traceId)
                                return (traceContext, carrier) -> {
                                    delegate.injector(setter).inject(traceContext, carrier);
                                    String hex = traceContext.traceIdString();
                                    if (hex != null && hex.length() == 32) {
                                        setter.put(carrier, headerKey, hex);
                                    }
                                };
                            }
                            @Override public <R> TraceContext.Extractor<R> extractor(Propagation.Getter<R, K> getter) {
                                return carrier -> {
                                    String raw = getter.get(carrier, headerKey);
                                    if (raw != null && HEX32.matcher(raw).matches()) {
                                        String hex = raw.toLowerCase();
                                        long hi = Long.parseUnsignedLong(hex.substring(0, 16), 16);
                                        long lo = Long.parseUnsignedLong(hex.substring(16), 16);
                                        TraceContext ctx = TraceContext.newBuilder()
                                            .traceIdHigh(hi).traceId(lo)
                                            .spanId(lo)  // best-effort root spanId
                                            .sampled(true)
                                            .build();
                                        return TraceContextOrSamplingFlags.create(ctx);
                                    }
                                    return delegate.extractor(getter).extract(carrier);
                                };
                            }
                        };
                    }
                };
            }
        }

    Note on HTTP header case: `keyFactory.create("X-Correlation-Id")` — case-insensitive HTTP header lookup is the framework's responsibility; Spring MVC HTTP carriers normalize header names automatically.

    **rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/config/CorrelationIdPropagationConfig.java** — Create identical file with package `com.citi.gru.rectrace.tlmstats.config`. All annotations, imports, logic, and the HEX32 constant are identical. This is an intentional module-mirror (a shared-ops lib would deduplicate but is deferred per CONTEXT.md).

    Verify both modules compile cleanly after adding the dependency:
    `mvn -pl backend/rectrace,rectrace-tlm-stats clean compile -q` should exit 0.
  </action>

  <verify>
    <automated>
      grep -c "micrometer-tracing-bridge-brave" /Users/aarun/Workspace/Projects/autosys-job-explorer/backend/rectrace/pom.xml
      grep -c "micrometer-tracing-bridge-brave" /Users/aarun/Workspace/Projects/autosys-job-explorer/rectrace-tlm-stats/pom.xml
      grep -c "HEX32" /Users/aarun/Workspace/Projects/autosys-job-explorer/backend/rectrace/src/main/java/com/citi/gru/rectrace/config/CorrelationIdPropagationConfig.java
      grep -c "HEX32" /Users/aarun/Workspace/Projects/autosys-job-explorer/rectrace-tlm-stats/src/main/java/com/citi/gru/rectrace/tlmstats/config/CorrelationIdPropagationConfig.java
      grep -c "@Profile" /Users/aarun/Workspace/Projects/autosys-job-explorer/backend/rectrace/src/main/java/com/citi/gru/rectrace/config/CorrelationIdPropagationConfig.java
      grep -c "Propagation.Factory" /Users/aarun/Workspace/Projects/autosys-job-explorer/backend/rectrace/src/main/java/com/citi/gru/rectrace/config/CorrelationIdPropagationConfig.java
      cd /Users/aarun/Workspace/Projects/autosys-job-explorer && mvn -pl backend/rectrace,rectrace-tlm-stats clean compile -q
    </automated>
  </verify>

  <done>
    - `micrometer-tracing-bridge-brave` appears in both pom.xml files with no explicit version tag (BOM-managed)
    - CorrelationIdPropagationConfig.java created in both modules with `@Profile("!test")`, `@Configuration`, `@Bean Propagation.Factory correlationIdPropagationFactory()`, and the `HEX32` constant
    - The custom factory's extractor() validates the 32-hex pattern; malformed/absent headers fall back to B3 upstream extraction
    - `mvn -pl backend/rectrace,rectrace-tlm-stats clean compile -q` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Add logback-spring.xml to both modules and append tracing properties (Option B — traceId only, no baggage keys)</name>
  <files>
    backend/rectrace/src/main/resources/logback-spring.xml,
    rectrace-tlm-stats/src/main/resources/logback-spring.xml,
    backend/rectrace/src/main/resources/application-local.properties,
    rectrace-tlm-stats/src/main/resources/application-local.properties
  </files>

  <read_first>
    1. backend/rectrace/src/main/resources/application-local.properties — read existing content; append only the sampling line (NOT baggage lines)
    2. rectrace-tlm-stats/src/main/resources/application-local.properties — same
    3. .planning/phases/02-react-foundation/02-CONTEXT.md — D-2.12 (logback pattern; %X{traceId} only, no spanId; Phase 7 replaces with JSON) and D-2.10 (Option B means traceId IS the X-Correlation-Id — no separate MDC key needed)
  </read_first>

  <action>
    **backend/rectrace/src/main/resources/logback-spring.xml** — Create new file (does not exist per PATTERNS.md note 2). Under Option B the traceId MDC key is populated by Brave from the X-Correlation-Id header value — there is no separate `x-correlation-id` MDC key. Pattern uses `%X{traceId}` ONLY:

        <?xml version="1.0" encoding="UTF-8"?>
        <configuration>
          <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
          <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
              <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [traceId=%X{traceId:-}] %logger{36} - %msg%n</pattern>
            </encoder>
          </appender>
          <root level="INFO">
            <appender-ref ref="CONSOLE"/>
          </root>
        </configuration>

    CRITICAL: File name MUST be `logback-spring.xml` (NOT `logback.xml`). Spring Boot loads `logback-spring.xml` after the context is ready, enabling Spring Environment property substitution. Phase 7 OBS-01 replaces this with `logstash-logback-encoder` JSON layout.

    Do NOT include `%X{x-correlation-id:-}` in the pattern — under Option B (custom Propagation.Factory) the X-Correlation-Id header value IS adopted as the traceId. A separate MDC key would duplicate the value and is not populated by the factory.

    **rectrace-tlm-stats/src/main/resources/logback-spring.xml** — Create identical file (same pattern, same CRITICAL note applies).

    **backend/rectrace/src/main/resources/application-local.properties** — Append ONLY the sampling line at the END of the existing file (preserving all existing content). Do NOT add baggage properties — they are incompatible with Option B and would cause two IDs per request:

        # Micrometer Tracing — Brave bridge (Phase 2 D-2.9, D-2.10, D-2.12)
        management.tracing.sampling.probability=1.0

    Do NOT add `management.tracing.baggage.remote-fields` or `management.tracing.baggage.correlation.fields` — these belong to Option A (baggage propagation) which is NOT what D-2.10 specifies. Option B's custom factory handles all propagation in Java code.

    **rectrace-tlm-stats/src/main/resources/application-local.properties** — Append the same 2 lines (comment + sampling probability only). Same prohibition on baggage lines applies.

    Rationale for `application-local.properties` only: production sampling defaults to 0.1 (Boot default); only local dev needs 1.0 for development visibility. This matches the planner discretion note in D-2.9.

    The smoke-correlation-id.sh script in Plan 04 will do the full round-trip test. For this plan's automated verify, confirm file existence and pattern string presence only.
  </action>

  <verify>
    <automated>
      grep -c "%X{traceId" /Users/aarun/Workspace/Projects/autosys-job-explorer/backend/rectrace/src/main/resources/logback-spring.xml
      grep -c "%X{traceId" /Users/aarun/Workspace/Projects/autosys-job-explorer/rectrace-tlm-stats/src/main/resources/logback-spring.xml
      grep -c "x-correlation-id" /Users/aarun/Workspace/Projects/autosys-job-explorer/backend/rectrace/src/main/resources/logback-spring.xml
      grep -c "management.tracing.sampling.probability" /Users/aarun/Workspace/Projects/autosys-job-explorer/backend/rectrace/src/main/resources/application-local.properties
      grep -c "management.tracing.sampling.probability" /Users/aarun/Workspace/Projects/autosys-job-explorer/rectrace-tlm-stats/src/main/resources/application-local.properties
      grep -c "management.tracing.baggage" /Users/aarun/Workspace/Projects/autosys-job-explorer/backend/rectrace/src/main/resources/application-local.properties
    </automated>
  </verify>

  <done>
    - `logback-spring.xml` created in both modules (NOT `logback.xml`); both contain `%X{traceId:-}` in the pattern; neither contains `%X{x-correlation-id:-}`
    - Both `application-local.properties` files contain `management.tracing.sampling.probability=1.0` as an appended line
    - Neither `application-local.properties` file contains `management.tracing.baggage.remote-fields` or `management.tracing.baggage.correlation.fields` (Option B handles propagation in Java)
    - Existing application-local.properties content is preserved unchanged
    - Third verify gate: `grep -c "x-correlation-id" logback-spring.xml` returns 0 (no spurious MDC key)
    - Fourth verify gate: `grep -c "management.tracing.baggage" application-local.properties` returns 0 (no baggage config)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → Spring filter chain | X-Correlation-Id header crosses trust boundary here; backend adopts the value as traceId only after regex validation |
| MDC → log output | traceId value from MDC is written to console log; crafted headers that pass the regex are 32 safe hex chars with no newlines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Information Disclosure | AG-Grid license key | N/A — out of scope for Plan 02 | Handled in Plan 01 |
| T-2-02 | Injection | `X-Correlation-Id` header → MDC → log | mitigate | The regex `^[a-fA-F0-9]{32}$` inside `CorrelationIdPropagationConfig.extractor()` is the validation boundary. Header values that fail this regex (including log-injection strings containing `\n`, `%`, or non-hex chars) are rejected — the `extractor()` falls back to upstream B3 extraction and Brave generates a fresh traceId. The 32-char hex constraint is structurally enforced: only `[a-fA-F0-9]` chars can appear, so newline injection and format-string injection vectors are defeated at the first character. No `OncePerRequestFilter` is needed; the Propagation.Factory IS the validation boundary. |
| T-2-03 | Information Disclosure | CORS over-permission (`@CrossOrigin(origins = "*")`) | accept | Pre-existing state from Phase 1; not changed in this plan. Phase 9 SEC-04 tightens CORS. No regression introduced. |
| T-2-04 | Elevation of Privilege | Dev proxy | N/A — out of scope for Plan 02 | Frontend concern only |
| T-2-05 | Tampering | `ops/build.sh` static cleanup | N/A — out of scope for Plan 02 | Handled in Plan 04 |
</threat_model>

<verification>
End-of-plan verification:
1. `grep -c "micrometer-tracing-bridge-brave" backend/rectrace/pom.xml rectrace-tlm-stats/pom.xml | grep -v ":0"` — both files contain the dependency
2. `grep -c "HEX32" backend/rectrace/src/main/java/com/citi/gru/rectrace/config/CorrelationIdPropagationConfig.java` → 1
3. `grep -c "%X{traceId" backend/rectrace/src/main/resources/logback-spring.xml rectrace-tlm-stats/src/main/resources/logback-spring.xml` — both return 1
4. `grep -c "x-correlation-id" backend/rectrace/src/main/resources/logback-spring.xml` → 0 (no spurious MDC key)
5. `grep -c "management.tracing.sampling.probability=1.0" backend/rectrace/src/main/resources/application-local.properties rectrace-tlm-stats/src/main/resources/application-local.properties` — both return 1
6. `grep -c "management.tracing.baggage" backend/rectrace/src/main/resources/application-local.properties` → 0 (no baggage config)
7. `mvn -pl backend/rectrace,rectrace-tlm-stats clean compile -q` — exits 0
8. `ls backend/rectrace/src/main/resources/logback-spring.xml rectrace-tlm-stats/src/main/resources/logback-spring.xml` — both files exist
</verification>

<success_criteria>
- `micrometer-tracing-bridge-brave` added to both Maven modules; no explicit version tag (BOM-managed at 1.5.11)
- `logback-spring.xml` created in both modules with `%X{traceId:-}` pattern ONLY (no `%X{x-correlation-id:-}` — that belongs to Option A/baggage which is NOT used)
- Both `application-local.properties` files have `sampling.probability=1.0` appended; NO `baggage.remote-fields` or `baggage.correlation.fields` lines (Option B handles propagation via custom Propagation.Factory)
- `CorrelationIdPropagationConfig.java` created in both modules with `@Profile("!test")`, `@Configuration`, `@Bean Propagation.Factory`, and `HEX32` regex constant
- The custom factory's `extractor()` reads X-Correlation-Id, validates against `^[a-fA-F0-9]{32}$`, and builds a TraceContext with hi/lo split; invalid/missing header falls back to B3 upstream extraction
- Both modules compile cleanly
- No Zipkin/Jaeger/OTel exporter added (deferred to Phase 7 per D-2.9)
</success_criteria>

<output>
After completion, create `.planning/phases/02-react-foundation/02-02-SUMMARY.md` with:
- Files modified (list with change type: new / append / modify)
- Tracing propagation mechanism chosen (Option B — custom Propagation.Factory per D-2.10) and rationale vs Option A
- T-2-02 mitigation confirmation: regex validation `^[a-fA-F0-9]{32}$` inside extractor() is the injection boundary
- Confirmation that logback-spring.xml uses %X{traceId} ONLY (no separate x-correlation-id MDC key)
- Confirmation that application-local.properties has NO baggage.remote-fields lines
- Deferred items confirmed (no exporter, no slow-query AOP, no HealthIndicator)
- Any compilation warnings or notes from the build
</output>
</output>
