---
phase: 00.1-local-dev-seed-bootstrap
plan: 03
subsystem: infra
tags: [oracle, ddl, schema, idempotent, plsql, sibling-repo]

# Dependency graph
requires:
  - phase: 00.1-local-dev-seed-bootstrap
    plan: 02
    provides: Two-service Docker stack with Oracle Free 23c healthy on localhost:1521/FREEPDB1; 4 schema users (rectrace, autosys, reconmgmt, recportal) authenticating via thin-mode oracledb
provides:
  - "Four idempotent DDL files at /Users/aarun/Workspace/Projects/rectrace-local-dev/schema/ — 01-rectrace.sql, 02-autosys.sql, 03-reconmgmt.sql, 04-recportal.sql"
  - "11 tables created across 4 Oracle schemas: RECTRACE owns rectrace_core (22 cols); AUTOSYS owns ujo_job + ujo_job_status (split) + autosys_tlm_recon_sequences + autosys_all_jobs_data; RECONMGMT owns recon_bank + 3 mr_csum_* tables; RECPORTAL owns quickrec_stats_table + recportal_manual_match_table"
  - "Idempotent drop-then-create pattern (PL/SQL block suppressing ORA-00942) on every table — re-applying produces no errors"
  - "ujo_job and ujo_job_status as SEPARATE tables joined on joid per JobStatusService.java:46-50; child dropped before parent for idempotency (BLOCKER-1+3 fix)"
affects: [00.1-05 seed scenarios INSERT into these tables, 00.1-06 apply.py replaces the inline one-shot script, 00.1-07 application-local.properties points at FREEPDB1 with these schemas live]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-schema DDL file naming: {NN}-{schema}.sql (ordered, one file per owning user) — apply.py iterates the list and connects AS each user"
    - "Idempotent drop block: BEGIN EXECUTE IMMEDIATE 'DROP TABLE x CASCADE CONSTRAINTS'; EXCEPTION WHEN OTHERS THEN IF SQLCODE != -942 THEN RAISE; END IF; END; / — survives both first-run (table absent) and re-run (table present)"
    - "Parent/child drop ordering: ujo_job_status dropped BEFORE ujo_job so even if a future PR adds an explicit FK, the drop still works (BLOCKER-3 fix)"
    - "Statement-split convention: files use '\\n/\\n' as the PL/SQL block + CREATE TABLE separator; apply driver splits on that delimiter and execs each chunk via oracledb.cursor.execute"
    - "Live-schema verification (WARNING-7 fix): RECTRACE_CORE column count asserted against user_tab_columns, not against whitespace-fragile grep of the .sql text"

key-files:
  created:
    - /Users/aarun/Workspace/Projects/rectrace-local-dev/schema/01-rectrace.sql
    - /Users/aarun/Workspace/Projects/rectrace-local-dev/schema/02-autosys.sql
    - /Users/aarun/Workspace/Projects/rectrace-local-dev/schema/03-reconmgmt.sql
    - /Users/aarun/Workspace/Projects/rectrace-local-dev/schema/04-recportal.sql
  modified: []

key-decisions:
  - "ujo_job and ujo_job_status declared as SEPARATE tables (joid NUMBER(10) FK target on both; ujo_job carries job_name; ujo_job_status carries status NUMBER(10) and next_start NUMBER(19)) — derived verbatim from JobStatusService.java:46-50 (LEFT JOIN ujo_job_status ON uj.joid = ujs.joid) and lines 64-65 (Integer/Long Java mappings)."
  - "Drop order is child-before-parent in 02-autosys.sql (ujo_job_status first, ujo_job second) so the idempotent drop pattern remains safe under a future explicit FK constraint."
  - "command and description columns on autosys_all_jobs_data declared as CLOB per CONTEXT.md D-0.1.8 (job command bodies can exceed VARCHAR2(4000))."
  - "DATE used for all time columns (stmt_date, load_date, cob, updated_date) per D-0.1.8 — supports the BETWEEN range filters in TlmStatsV2Service and QuickRecStatsService."
  - "rectrace_core has 22 VARCHAR2(4000) columns — the union of every searchColumn, columns[].field, and searchFields entry across all 13 categories in search-config-v4.json. Single wide-row design (no per-category sub-tables) matches the production codebase, which dispatches all 13 categories against the same physical table."
  - "Column-count verification done against the LIVE user_tab_columns view (live-schema oracle for count), not via grep of the .sql text (WARNING-7 fix — robust against whitespace/blank-line edits)."

requirements-completed: [LOCAL-DEV-02]

# Metrics
duration: ~5min
completed: 2026-05-12
---

# Phase 00.1 Plan 03: Oracle DDL Hand-Authored Across 4 Schemas Summary

**Four idempotent Oracle DDL files committed to the sibling repo and applied to the live containerised Oracle Free 23c; 11 tables now exist across 4 schemas with column types matching the source Java/JSON code verbatim — including the corrected ujo_job + ujo_job_status split (BLOCKER-1+3 fix) joined on joid per JobStatusService.java.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-12T14:00Z
- **Completed:** 2026-05-12T14:05Z
- **Tasks:** 2/2

## Tasks Completed

1. **Task 1 — Write schema/01-rectrace.sql and schema/02-autosys.sql** (no commit yet per plan; batched with Task 2)
   - `schema/01-rectrace.sql` — 1 table (`rectrace_core`, 22 VARCHAR2(4000) cols matching search-config-v4.json field union)
   - `schema/02-autosys.sql` — 4 tables: `ujo_job (joid, job_name)`, `ujo_job_status (joid, status NUMBER(10), next_start NUMBER(19))`, `autosys_tlm_recon_sequences`, `autosys_all_jobs_data (command CLOB, description CLOB)`
   - Drop order in 02-autosys.sql: `ujo_job_status` first (child), `ujo_job` second (parent) — BLOCKER-3 fix

2. **Task 2 — Write schema/03-reconmgmt.sql + schema/04-recportal.sql; apply all 4 DDL files** — sibling commit `c2d1083`
   - `schema/03-reconmgmt.sql` — 4 tables: `recon_bank`, `mr_csum_man_match_stats_hist`, `mr_csum_man_match_details`, `mr_csum_netting_hist`
   - `schema/04-recportal.sql` — 2 tables: `quickrec_stats_table`, `recportal_manual_match_table`
   - All 4 files applied to live Oracle (FREEPDB1) via inline oracledb thin-mode Python (apply.py proper lands in plan 00.1-06)
   - Re-applied a second time to prove idempotency — zero errors
   - Verified table presence via user_tables (all 11 tables, each owned by the correct schema user) and column count via user_tab_columns (rectrace_core = 22)

## Commits in Sibling Repo

- `c2d1083` Oracle DDL for 11 tables across 4 schemas (4 files, 209 insertions)

Sibling repo commit count: 3 → 4 (matches plan success_criteria).

## Live-Schema Verification

```text
[rectrace]  OK: ['RECTRACE_CORE']
[autosys]   OK: ['AUTOSYS_ALL_JOBS_DATA', 'AUTOSYS_TLM_RECON_SEQUENCES', 'UJO_JOB', 'UJO_JOB_STATUS']
[reconmgmt] OK: ['MR_CSUM_MAN_MATCH_DETAILS', 'MR_CSUM_MAN_MATCH_STATS_HIST', 'MR_CSUM_NETTING_HIST', 'RECON_BANK']
[recportal] OK: ['QUICKREC_STATS_TABLE', 'RECPORTAL_MANUAL_MATCH_TABLE']
rectrace_core live column count: 22
```

Column-type spot check on the AUTOSYS schema (BLOCKER-1+3 correctness gate):

```text
UJO_JOB        -> [('JOID','NUMBER',10), ('JOB_NAME','VARCHAR2',None)]
UJO_JOB_STATUS -> [('JOID','NUMBER',10), ('STATUS','NUMBER',10), ('NEXT_START','NUMBER',19)]
AUTOSYS_ALL_JOBS_DATA -> [..., ('COMMAND','CLOB',None), ('DESCRIPTION','CLOB',None)]
AUTOSYS_TLM_RECON_SEQUENCES -> [('JOB_NAME','VARCHAR2',None), ('LOAD_JOB','VARCHAR2',None), ('EXEC_ORDER','NUMBER',10)]
```

`status` is NUMBER(10) — matches Java `Integer` at JobStatusService.java line 64.
`next_start` is NUMBER(19) — matches Java `Long` at JobStatusService.java line 65.

## Idempotency Verification

Re-ran the inline apply Python a second time after initial application: zero exceptions, all DROP blocks correctly caught `ORA-00942` for the tables that didn't exist on the second pass (they do now), then CREATE TABLE ran on the cleared schema. Confirms the PL/SQL `IF SQLCODE != -942 THEN RAISE` pattern works end-to-end.

## Deviations from Plan

None — plan executed exactly as written.

The BLOCKER-1+3 corrections (ujo_job + ujo_job_status split, child-before-parent drop order) were already baked into the plan text by the planner; no in-flight deviations were needed.

## Known Stubs

None. All 11 tables ship with the column shapes the existing Boot 2.7.16 codebase queries against — no placeholder tables, no missing columns relative to the native SQL paths. Seed rows land in plan 00.1-05; that's deliberate, not a stub.

## Threat Flags

None. No new network endpoints, auth paths, or trust-boundary crossings introduced by hand-authored DDL applied AS the owning schema user (not SYS, so blast radius is each user's own schema — matches T-00.1-07 acceptance).

## Self-Check: PASSED

- FOUND: /Users/aarun/Workspace/Projects/rectrace-local-dev/schema/01-rectrace.sql
- FOUND: /Users/aarun/Workspace/Projects/rectrace-local-dev/schema/02-autosys.sql
- FOUND: /Users/aarun/Workspace/Projects/rectrace-local-dev/schema/03-reconmgmt.sql
- FOUND: /Users/aarun/Workspace/Projects/rectrace-local-dev/schema/04-recportal.sql
- FOUND: sibling commit c2d1083 (verified via `git log --oneline`)
- FOUND: 11 tables in live Oracle (verified via user_tables query AS each schema user)
- FOUND: rectrace_core live column count = 22 (verified via user_tab_columns)
- FOUND: ujo_job_status has status NUMBER(10) and next_start NUMBER(19) — matches JobStatusService.java Integer/Long types
