---
phase: 00.1
plan: 06
type: execute
wave: 4
depends_on: ["00.1-03", "00.1-04"]
files_modified:
  - ../rectrace-local-dev/apply.py
autonomous: true
requirements:
  - LOCAL-DEV-05
threat_model_ref: T-00.1-11

must_haves:
  truths:
    - "A single Python script `apply.py` at the sibling repo root orchestrates DDL apply, INSERT seed apply, ES mapping create, and ES bulk-load."
    - "Script supports flags: `--reset` (drop+recreate everything; same as default --all when applied to existing stack), `--verify` (row counts + .keyword exact-match probe; sub-5-second), `--oracle-only`, `--es-only`, and bare invocation (default `--all`)."
    - "Connection params loaded via python-dotenv from `.env` (falls back to `.env.example` if `.env` missing — with a warning printed)."
    - "Script polls `oracledb.connect()` with exponential backoff up to 5 minutes for cold-boot Oracle readiness (Pattern 2 from RESEARCH.md)."
    - "Re-running `apply.py` against an already-seeded stack produces identical row counts (TRUNCATE+INSERT pattern in the .sql files makes it idempotent)."
    - "Re-running `apply.py --reset` twice in a row yields identical row counts; second run completes without error (LOCAL-DEV-05 acceptance)."
    - "`apply.py --verify` exits 0 iff: every Oracle table row count matches the expected value AND the .keyword exact-match probe for `SET-ABC-123` returns exactly 1 hit."
  artifacts:
    - path: "../rectrace-local-dev/apply.py"
      provides: "Single entry point for all seed lifecycle operations"
      contains: "def main()"
  key_links:
    - from: "../rectrace-local-dev/apply.py"
      to: "../rectrace-local-dev/schema/*.sql + data/*.sql + es/*.json + data/05-*.ndjson"
      via: "reads files from sibling repo's well-known directories"
      pattern: "ROOT \\/ ['\"]schema['\"]"
---

<objective>
Build `apply.py` — the idempotent Python driver that orchestrates the full seed lifecycle: wait for Oracle, apply DDL per schema, apply INSERTs per schema, (re)create the ES index from mapping, bulk-load NDJSON docs, and run a fast `--verify` smoke. The script consolidates the inline Python helpers used in plans 00.1-03 and 00.1-05 into a single, reusable, flag-driven entry point.

Purpose: Per CONTEXT.md D-0.1.11/D-0.1.12 the seed lifecycle is the developer's daily ergonomic surface. `apply.py --reset` is the fast reset path (~5s) versus `docker compose down -v` (~120s, RESEARCH.md Anti-Patterns). Per VALIDATION.md `apply.py --verify` is the sub-5-second check that runs after every commit in the sibling repo and gates each plan-wave merge.

Output: A single executable Python file at `../rectrace-local-dev/apply.py` with the flag surface described in `must_haves.truths`.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/00.1-local-dev-seed-bootstrap/00.1-CONTEXT.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/00.1-local-dev-seed-bootstrap/00.1-RESEARCH.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/00.1-local-dev-seed-bootstrap/00.1-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author apply.py with --reset, --verify, --oracle-only, --es-only, default --all</name>
  <files>
    /Users/aarun/Workspace/Projects/rectrace-local-dev/apply.py
  </files>
  <read_first>
    /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/00.1-local-dev-seed-bootstrap/00.1-RESEARCH.md §"Code Examples > apply.py Skeleton"
    /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/00.1-local-dev-seed-bootstrap/00.1-RESEARCH.md §"Pattern 2: apply.py Retry-with-Backoff Connection Probe"
    /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/00.1-local-dev-seed-bootstrap/00.1-RESEARCH.md §"Pattern 3: ES Bulk Load via helpers.bulk from NDJSON"
    /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/00.1-local-dev-seed-bootstrap/00.1-RESEARCH.md §"Pitfall 6: oracledb 4.x Default Connection Pool Behavior" (use connect, not create_pool)
    /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/00.1-local-dev-seed-bootstrap/00.1-VALIDATION.md §"Per-Task Verification Map" + §"D-0.1.23 Smoke Verification Checklist"
    /Users/aarun/Workspace/Projects/rectrace-local-dev/.env.example (key names match)
    /Users/aarun/Workspace/Projects/rectrace-local-dev/schema/01-rectrace.sql (file naming pattern: `schema/0N-<schema>.sql`)
    /Users/aarun/Workspace/Projects/rectrace-local-dev/data/01-rectrace-inserts.sql (file naming pattern: `data/0N-<schema>-inserts.sql`)
  </read_first>
  <action>
    Write `/Users/aarun/Workspace/Projects/rectrace-local-dev/apply.py` — a single Python file based on RESEARCH.md "apply.py Skeleton", extended with `--verify` and refined for the 4-schema layout. The script must be runnable as `./.venv/bin/python apply.py [flags]` from the sibling repo root.

    Required behavior:

    1. **Argument parsing** via `argparse` with these flags:
       - `--reset`: equivalent to default `--all` (the DDL files TRUNCATE/DROP-CASCADE-then-CREATE, so re-running IS a reset). The flag exists for ergonomic clarity. Print "Resetting all data..." banner.
       - `--verify`: skip apply phase; only run row-count + .keyword probe checks; exit 0 on success, exit 1 (with per-table failure print) on any miss. Sub-5-second.
       - `--oracle-only`: skip ES step.
       - `--es-only`: skip Oracle step.
       - No flag → equivalent to `--all` (apply everything).

    2. **Environment loading**: `dotenv.load_dotenv()` at module top. If `ORACLE_DSN` not in env after load, fall back to loading from `.env.example` and print a warning ("[warn] no .env found, using .env.example defaults"). Required env vars: `ORACLE_DSN`, `ORACLE_PASSWORD` (system password), `RECTRACE_PWD`, `AUTOSYS_PWD`, `RECONMGMT_PWD`, `RECPORTAL_PWD`, `ES_URL`.

    3. **Oracle phase** (`apply_oracle()`):
       - Call `wait_for_oracle()` first — exponential backoff up to 300s, exactly per RESEARCH.md Pattern 2, probing connection as the `system` user with `ORACLE_PASSWORD`.
       - For each schema in `[('rectrace','RECTRACE_PWD','01-rectrace'), ('autosys','AUTOSYS_PWD','02-autosys'), ('reconmgmt','RECONMGMT_PWD','03-reconmgmt'), ('recportal','RECPORTAL_PWD','04-recportal')]`:
         - Connect as that schema user.
         - `apply_sql_file(conn, ROOT / 'schema' / f'{prefix}.sql')`.
         - `apply_sql_file(conn, ROOT / 'data' / f'{prefix}-inserts.sql')`.
         - Print row count for each table touched in that schema (use `user_tables` + `SELECT COUNT(*)` per table).

    4. **`apply_sql_file(conn, path)` helper**:
       - Read `path` as text.
       - Split on `\n/\n` (PL/SQL block delimiter); strip trailing `/` from each chunk.
       - Skip empty chunks.
       - `cursor.execute(stmt)` for each.
       - `conn.commit()` at end.
       - On error, print the statement (truncated to 100 chars) and re-raise.

    5. **ES phase** (`apply_elasticsearch()`):
       - `es = Elasticsearch(os.environ['ES_URL'])`.
       - Read `es/rectrace_core_index.mapping.json` as JSON.
       - If `es.indices.exists(index='rectrace_core_index')`, `es.indices.delete(index='rectrace_core_index')`.
       - `es.indices.create(index='rectrace_core_index', body=<mapping>)`.
       - Stream `data/05-elasticsearch-bulk.ndjson`; for each line, yield a bulk action with `_id = doc['job_name']` (deterministic — re-running upserts in place).
       - `bulk(es, gen(), raise_on_error=True, refresh='wait_for')` — `refresh='wait_for'` ensures the next query sees the docs.

    6. **`verify()` function** (the heart of `--verify`):
       - Expected row counts (extracted from plan 00.1-05 acceptance):
         ```python
         EXPECTED = {
             ('rectrace','rectrace_pwd'):  {'rectrace_core': 5},
             ('autosys','autosys_pwd'):    {'ujo_job': 5, 'autosys_tlm_recon_sequences': 15, 'autosys_all_jobs_data': 20},
             ('reconmgmt','reconmgmt_pwd'):{'recon_bank': 5, 'mr_csum_man_match_stats_hist': 50, 'mr_csum_man_match_details': 50, 'mr_csum_netting_hist': 50},
             ('recportal','recportal_pwd'):{'quickrec_stats_table': 10, 'recportal_manual_match_table': 5},
         }
         ```
       - For each (user, pwd) connect once; for each table run `SELECT COUNT(*) FROM <table>`; compare; record `(table, expected, actual, ok)`.
       - ES probes:
         - `es.count(index='rectrace_core_index')` — expect `count=5`.
         - `es.search(index='rectrace_core_index', body={'query':{'term':{'set_id.keyword':'SET-ABC-123'}}})` — expect `hits.total.value == 1`.
         - `es.search(index='rectrace_core_index', body={'query':{'term':{'job_name.keyword':'LOAD-ABC-123'}}})` — expect `hits.total.value == 1`.
       - Print a per-table pass/fail table; aggregate result.
       - `sys.exit(0)` if all pass; `sys.exit(1)` if any fail.
       - Total runtime target: <5s on a warm stack.

    7. **`main()`**:
       - Parse args.
       - If `--verify`: call `verify()`; return its exit code.
       - Else: if not `--es-only` → `apply_oracle()`; if not `--oracle-only` → `apply_elasticsearch()`; print "Done. Stack ready."

    Make the file executable: `chmod +x /Users/aarun/Workspace/Projects/rectrace-local-dev/apply.py` (and start with `#!/usr/bin/env python3` shebang for ergonomic `./apply.py` invocation, though `.venv/bin/python apply.py` is the documented path).

    Avoid the Pitfall 6 trap: use `oracledb.connect()`, NOT `oracledb.create_pool()`.

    The full script is ~150-180 lines. Build it out following the skeleton in RESEARCH.md, plus the `verify()` function and the per-schema mapping.

    After writing the file, run a smoke pass to ensure it executes:
    ```
    cd /Users/aarun/Workspace/Projects/rectrace-local-dev/
    ./.venv/bin/python apply.py --verify  # expect exit 0 (stack already seeded by plans 03+05)
    ./.venv/bin/python apply.py --reset   # full re-apply
    ./.venv/bin/python apply.py --verify  # expect exit 0 again
    ./.venv/bin/python apply.py --reset   # second consecutive reset
    ./.venv/bin/python apply.py --verify  # expect exit 0 — idempotency proof
    ```

    Commit:
    ```
    git -C /Users/aarun/Workspace/Projects/rectrace-local-dev/ add apply.py
    git -C /Users/aarun/Workspace/Projects/rectrace-local-dev/ commit -m "apply.py: idempotent driver with --reset and --verify flags"
    ```
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/rectrace-local-dev/ \
      &amp;&amp; test -f apply.py \
      &amp;&amp; ./.venv/bin/python -c 'import ast; ast.parse(open("apply.py").read()); print("AST parses")' \
      &amp;&amp; grep -q 'def main' apply.py \
      &amp;&amp; grep -q "argparse" apply.py \
      &amp;&amp; grep -q -- "--reset" apply.py \
      &amp;&amp; grep -q -- "--verify" apply.py \
      &amp;&amp; grep -q -- "--oracle-only" apply.py \
      &amp;&amp; grep -q -- "--es-only" apply.py \
      &amp;&amp; grep -q 'def wait_for_oracle' apply.py \
      &amp;&amp; grep -q 'from elasticsearch.helpers import bulk' apply.py \
      &amp;&amp; grep -q 'dotenv' apply.py \
      &amp;&amp; ! grep -q 'create_pool' apply.py \
      &amp;&amp; ./.venv/bin/python apply.py --verify \
      &amp;&amp; ./.venv/bin/python apply.py --reset \
      &amp;&amp; ./.venv/bin/python apply.py --verify \
      &amp;&amp; ./.venv/bin/python apply.py --reset \
      &amp;&amp; ./.venv/bin/python apply.py --verify</automated>
  </verify>
  <acceptance_criteria>
    - File `apply.py` exists and parses as valid Python (AST parse succeeds).
    - File defines `main`, `wait_for_oracle`, `apply_sql_file`, `apply_oracle`, `apply_elasticsearch`, `verify` functions.
    - Imports include `oracledb`, `elasticsearch`, `elasticsearch.helpers.bulk`, `dotenv`, `argparse`, `pathlib.Path`.
    - File does NOT contain `create_pool` (Pitfall 6).
    - All four argparse flags (`--reset`, `--verify`, `--oracle-only`, `--es-only`) recognized.
    - `python apply.py --verify` exits 0 after a successful apply (proves the verify probe works).
    - `python apply.py --reset` followed by `python apply.py --reset` (twice in succession) completes without errors and second `--verify` still exits 0 (LOCAL-DEV-05 idempotency proof).
    - `python apply.py --reset` total runtime under 30 seconds against a warm stack.
    - `python apply.py --verify` total runtime under 5 seconds.
    - `git -C /Users/aarun/Workspace/Projects/rectrace-local-dev/ ls-files apply.py` returns `apply.py`.
    - No file inside this rectrace repo modified.
  </acceptance_criteria>
  <done>apply.py committed; --verify and --reset idempotency proven by double-reset cycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` (gitignored) → apply.py runtime → Oracle/ES | apply.py reads passwords from .env (or .env.example fallback); passes them as `password=` to oracledb. No password printed to stdout. |
| schema/*.sql + data/*.sql files → Oracle | Files are authored in this workflow; trust boundary is the developer's git review of changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00.1-11 | Information Disclosure | apply.py logs might inadvertently print connection passwords | mitigate | Implement: when printing exception details from `oracledb.DatabaseError`, do not print the full connection params dict. Pass user/password via kwargs to `oracledb.connect`, not via DSN. Print only `[<schema>] <stmt-preview>...` lines; never echo the password. |
| T-00.1-12 | Tampering | If a future contributor adds dynamic SQL (string-interpolation) to apply.py, it could become SQL-injection-prone | accept | apply.py reads SQL from .sql files only; never interpolates from user input. Documented in the script's module docstring. |
| T-00.1-13 | Denial of Service | apply.py hangs forever if Oracle never reaches readiness | mitigate | `wait_for_oracle()` has a hard 5-minute deadline (per RESEARCH.md Pattern 2). Beyond that, raises `RuntimeError`. |
</threat_model>

<verification>
- `apply.py --verify` returns exit 0 with all per-table counts matching.
- `apply.py --reset` is idempotent across two consecutive invocations.
- No password leakage in stdout (visual check; per T-00.1-11 mitigation).
</verification>

<success_criteria>
- Single-file driver script committed at sibling repo root.
- All four flags work (`--reset`, `--verify`, `--oracle-only`, `--es-only`).
- LOCAL-DEV-05 idempotency requirement satisfied (double-reset cycle leaves counts unchanged).
</success_criteria>

<output>
After completion, create `.planning/phases/00.1-local-dev-seed-bootstrap/00.1-06-SUMMARY.md` in THIS repo.
</output>
