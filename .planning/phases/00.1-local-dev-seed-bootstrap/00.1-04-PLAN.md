---
phase: 00.1
plan: 04
type: execute
wave: 3
depends_on: ["00.1-02"]
files_modified:
  - ../rectrace-local-dev/es/rectrace_core_index.mapping.json
autonomous: true
requirements:
  - LOCAL-DEV-03
threat_model_ref: T-00.1-09

must_haves:
  truths:
    - "An explicit Elasticsearch index mapping JSON exists declaring every field that any docs Phase 0.1 will index uses."
    - "12 hyphen-sensitive fields are declared with a `.keyword` multi-field of type `keyword` and `ignore_above: 8192` (the Phase 8 hyphen-bug fix prerequisite per D-0.1.10)."
    - "The 12 hyphen-sensitive fields are exactly: file_name_pattern, recon, box_name, set_id, sub_acc, load_file_name_pattern, job_name, machine, run_calendar, exclude_calendar, tlm_instance, recon_id, recon_portal_id (13 fields per RESEARCH.md — the docstring 12 is off-by-one; correct count is 13)."
    - "Mapping file is valid JSON and can be POSTed to ES to create the index."
  artifacts:
    - path: "../rectrace-local-dev/es/rectrace_core_index.mapping.json"
      provides: "ES index settings + explicit field mappings for rectrace_core_index"
      contains: "\"keyword\""
  key_links:
    - from: "../rectrace-local-dev/es/rectrace_core_index.mapping.json"
      to: "backend/rectrace/src/main/resources/search-config-v4.json"
      via: "every field in mapping appears as a searchField or column in at least one V4 category"
      pattern: "set_id"
---

<objective>
Author the explicit Elasticsearch index mapping for `rectrace_core_index` with `.keyword` multi-fields on the 13 hyphen-sensitive fields per CONTEXT.md D-0.1.10 and RESEARCH.md §Code Examples §ES Mapping. The explicit mapping is the Phase 8 hyphen-bug fix prerequisite — Phase 8's `.keyword`-subfield query path depends on the subfield being declared at index-create time (not dynamically inferred — Pitfall 3).

Purpose: Per CONTEXT.md D-0.1.5/D-0.1.10 and RESEARCH.md Pitfall 3, default dynamic mapping creates `.keyword` subfields with `ignore_above: 256` which silently truncates long hyphenated identifiers. Phase 8's regression test (`set_id.keyword == "SET-ABC-123"` returns 1 hit) depends on the subfield being declared with `ignore_above: 8192`.

Output: Valid JSON mapping file under `../rectrace-local-dev/es/` committed to sibling repo.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/00.1-local-dev-seed-bootstrap/00.1-CONTEXT.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/00.1-local-dev-seed-bootstrap/00.1-RESEARCH.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/backend/rectrace/src/main/resources/search-config-v4.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create es/rectrace_core_index.mapping.json with explicit multi-fields and validate against ES</name>
  <files>
    /Users/aarun/Workspace/Projects/rectrace-local-dev/es/rectrace_core_index.mapping.json
  </files>
  <read_first>
    /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/00.1-local-dev-seed-bootstrap/00.1-RESEARCH.md §"Code Examples > ES Mapping for rectrace_core_index"
    /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/00.1-local-dev-seed-bootstrap/00.1-RESEARCH.md §"Pitfall 3: .keyword Subfield Must Be Explicit in the ES Mapping"
    /Users/aarun/Workspace/Projects/autosys-job-explorer/backend/rectrace/src/main/resources/search-config-v4.json (audit which fields appear with both parent and `.keyword` variant in any category's `searchFields`)
  </read_first>
  <action>
    Create directory `/Users/aarun/Workspace/Projects/rectrace-local-dev/es/`. Write `rectrace_core_index.mapping.json` exactly per RESEARCH.md "Code Examples > ES Mapping for rectrace_core_index" (single-source-of-truth — the researcher already validated against `search-config-v4.json`'s `searchFields` union).

    Exact content (one shard, zero replicas — single-node local; `ignore_above: 8192` to accommodate any realistic hyphenated identifier):

    ```json
    {
      "settings": {
        "number_of_shards": 1,
        "number_of_replicas": 0
      },
      "mappings": {
        "properties": {
          "file_name_pattern":      {"type": "text", "fields": {"keyword": {"type": "keyword", "ignore_above": 8192}}},
          "recon":                  {"type": "text", "fields": {"keyword": {"type": "keyword", "ignore_above": 8192}}},
          "box_name":               {"type": "text", "fields": {"keyword": {"type": "keyword", "ignore_above": 8192}}},
          "set_id":                 {"type": "text", "fields": {"keyword": {"type": "keyword", "ignore_above": 8192}}},
          "sub_acc":                {"type": "text", "fields": {"keyword": {"type": "keyword", "ignore_above": 8192}}},
          "load_file_name_pattern": {"type": "text", "fields": {"keyword": {"type": "keyword", "ignore_above": 8192}}},
          "job_name":               {"type": "text", "fields": {"keyword": {"type": "keyword", "ignore_above": 8192}}},
          "machine":                {"type": "text", "fields": {"keyword": {"type": "keyword", "ignore_above": 8192}}},
          "run_calendar":           {"type": "text", "fields": {"keyword": {"type": "keyword", "ignore_above": 8192}}},
          "exclude_calendar":       {"type": "text", "fields": {"keyword": {"type": "keyword", "ignore_above": 8192}}},
          "tlm_instance":           {"type": "text", "fields": {"keyword": {"type": "keyword", "ignore_above": 8192}}},
          "recon_id":               {"type": "text", "fields": {"keyword": {"type": "keyword", "ignore_above": 8192}}},
          "recon_portal_id":        {"type": "text", "fields": {"keyword": {"type": "keyword", "ignore_above": 8192}}},

          "ok_file_name":           {"type": "text"},
          "recon_engine":           {"type": "text"},
          "app_name":               {"type": "text"},
          "app_id":                 {"type": "text"},
          "support_email":          {"type": "text"},
          "support_hotline":        {"type": "text"},
          "receive_path":           {"type": "text"},
          "load_job":               {"type": "text"},
          "load_file_name":         {"type": "text"}
        }
      }
    }
    ```

    Validate the mapping by POSTing it to the running ES (from plan 00.1-02):
    ```
    # Delete any pre-existing index from prior experiments
    curl -fsS -X DELETE 'http://localhost:9200/rectrace_core_index' 2>/dev/null || true

    # Create with the explicit mapping
    curl -fsS -X PUT 'http://localhost:9200/rectrace_core_index' \
      -H 'Content-Type: application/json' \
      --data-binary @/Users/aarun/Workspace/Projects/rectrace-local-dev/es/rectrace_core_index.mapping.json

    # Verify the keyword subfield is in place
    curl -fsS 'http://localhost:9200/rectrace_core_index/_mapping' | python3 -c "
    import sys, json
    m = json.load(sys.stdin)['rectrace_core_index']['mappings']['properties']
    multi_fields = ['file_name_pattern','recon','box_name','set_id','sub_acc','load_file_name_pattern','job_name','machine','run_calendar','exclude_calendar','tlm_instance','recon_id','recon_portal_id']
    for f in multi_fields:
        assert m[f]['type']=='text', f
        assert m[f]['fields']['keyword']['type']=='keyword', f
        assert m[f]['fields']['keyword']['ignore_above']==8192, f
    print(f'OK: {len(multi_fields)} fields with .keyword subfield + ignore_above 8192')
    "
    ```

    Note on truth statement count: the must_haves originally said "12 hyphen-sensitive fields" but the RESEARCH.md inventory contains 13 (`file_name_pattern, recon, box_name, set_id, sub_acc, load_file_name_pattern, job_name, machine, run_calendar, exclude_calendar, tlm_instance, recon_id, recon_portal_id`). The plan ships 13 to match the researcher's authoritative inventory; the must_haves truths note the correction inline.

    Commit:
    ```
    git -C /Users/aarun/Workspace/Projects/rectrace-local-dev/ add es/
    git -C /Users/aarun/Workspace/Projects/rectrace-local-dev/ commit -m "ES index mapping with explicit .keyword subfields for hyphen fields"
    ```
  </action>
  <verify>
    <automated>test -f /Users/aarun/Workspace/Projects/rectrace-local-dev/es/rectrace_core_index.mapping.json \
      &amp;&amp; python3 -c "import json; m=json.load(open('/Users/aarun/Workspace/Projects/rectrace-local-dev/es/rectrace_core_index.mapping.json')); assert m['settings']['number_of_shards']==1; assert m['settings']['number_of_replicas']==0; props=m['mappings']['properties']; multi=[k for k,v in props.items() if isinstance(v.get('fields'),dict) and v['fields'].get('keyword',{}).get('type')=='keyword']; assert len(multi)==13, multi; print('JSON OK with',len(multi),'multi-fields')" \
      &amp;&amp; curl -fsS 'http://localhost:9200/rectrace_core_index/_mapping' | python3 -c "
import sys,json
m=json.load(sys.stdin)['rectrace_core_index']['mappings']['properties']
for f in ['file_name_pattern','recon','box_name','set_id','sub_acc','load_file_name_pattern','job_name','machine','run_calendar','exclude_calendar','tlm_instance','recon_id','recon_portal_id']:
    assert m[f]['type']=='text', f
    assert m[f]['fields']['keyword']['type']=='keyword', f
    assert m[f]['fields']['keyword']['ignore_above']==8192, f
print('ES index has explicit .keyword multi-fields with ignore_above:8192')
"</automated>
  </verify>
  <acceptance_criteria>
    - File `es/rectrace_core_index.mapping.json` exists and parses as valid JSON.
    - `settings.number_of_shards == 1`, `settings.number_of_replicas == 0`.
    - Exactly 13 fields have `type: text` with a `fields.keyword` multi-field of `type: keyword` and `ignore_above: 8192` (the hyphen-sensitive set).
    - 9 additional `text`-only fields (`ok_file_name`, `recon_engine`, `app_name`, `app_id`, `support_email`, `support_hotline`, `receive_path`, `load_job`, `load_file_name`).
    - The mapping applied successfully to local ES — `GET /rectrace_core_index/_mapping` returns the expected shape with `ignore_above: 8192` on every hyphen-sensitive field.
    - `git -C /Users/aarun/Workspace/Projects/rectrace-local-dev/ ls-files es/` returns `es/rectrace_core_index.mapping.json`.
  </acceptance_criteria>
  <done>Explicit ES mapping committed; index created in local ES with 13 `.keyword` multi-fields (Phase 8 hyphen-bug fix prerequisite met).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mapping JSON → local ES cluster | The mapping is authored in this workflow; applied to localhost:9200 (no auth per D-0.1.16). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00.1-09 | Information Disclosure | Local ES has `xpack.security.enabled=false` — anyone on the laptop's loopback can read the index | accept | D-0.1.16 explicitly authorizes ES security disabled for local dev only. Phase 9 territory; not Phase 0.1's concern. |
</threat_model>

<verification>
- Mapping file parses as valid JSON.
- ES `_mapping` API returns 13 hyphen-sensitive fields with `keyword` subfield + `ignore_above: 8192`.
</verification>

<success_criteria>
- `rectrace_core_index` exists in local ES with the explicit mapping.
- Mapping file committed to sibling repo.
- Phase 8's hyphen-bug fix has a runnable target locally (will be exercised in plan 00.1-05 by bulk-loading a hyphenated scenario).
</success_criteria>

<output>
After completion, create `.planning/phases/00.1-local-dev-seed-bootstrap/00.1-04-SUMMARY.md` in THIS repo.
</output>
