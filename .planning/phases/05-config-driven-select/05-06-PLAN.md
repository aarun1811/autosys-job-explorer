---
phase: 05-config-driven-select
plan: 06
type: execute
wave: 5
depends_on: [05-01, 05-05]
files_modified:
  - scripts/smoke-sql-search.sh
  - .planning/phases/05-config-driven-select/ANGULAR-WIRING.md
  - .planning/parity-matrix.md
autonomous: true
requirements: [SQL-07]
must_haves:
  truths:
    - "scripts/smoke-sql-search.sh runs green against the live local backend with Docker stack up — exits 0, asserts /config returns reconSummary, /ssrm/reconSummary returns SSRM shape with RECON-XYZ-42"
    - "ANGULAR-WIRING.md documents the exact one-line change in the existing Angular SearchV5GridComponent that swaps `/api/v4/search/...` for `/api/v4/sql-search/...` to consume the SQL tab — instructions only, no code edits"
    - "parity-matrix.md gains a new row for the reconSummary SQL tab marked `port` (or its current equivalent target verb) so the SQL surface is captured in the matrix"
    - "mvn -q test (full backend/rectrace suite) is green; ContextLoadsTest unaffected"
  artifacts:
    - path: "scripts/smoke-sql-search.sh"
      provides: "End-to-end smoke for the SQL search endpoint (enhanced from Plan 01 skeleton)"
    - path: ".planning/phases/05-config-driven-select/ANGULAR-WIRING.md"
      provides: "Documentation of how Angular V5 grid would consume the new endpoint (no Angular code edited)"
    - path: ".planning/parity-matrix.md"
      provides: "Updated matrix row for reconSummary tab"
  key_links:
    - from: "scripts/smoke-sql-search.sh"
      to: "/api/v4/sql-search/config and /api/v4/sql-search/ssrm/reconSummary"
      via: "curl with fail-fast"
      pattern: "curl -fsS .*sql-search"
---

<objective>
Close Phase 5. Run the smoke script against the live stack; refine the smoke if the Plan 01 skeleton missed an assertion; document the one-line Angular URL swap that consumes the new endpoint; update the parity-matrix to capture the new SQL surface. No production code edits — this is the evidence + documentation plan.

Purpose: SQL-07 ("at least one example end-to-end consumable by an existing grid"). The end-to-end evidence is the smoke green + the Angular wiring doc; we do not edit Angular per D-5.18 (deferred), but we document the exact change a future plan would make.
Output: enhanced smoke script + 1 new markdown doc + parity-matrix update.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/parity-matrix.md
@.planning/phases/05-config-driven-select/05-CONTEXT.md
@.planning/phases/05-config-driven-select/05-RESEARCH.md
@scripts/smoke-sql-search.sh
@scripts/smoke-ssrm.sh
@frontend/rectrace/src/app/search-v5/components/search-v5-grid/search-v5-grid.component.ts
@frontend/rectrace/src/app/services/search-v5.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run smoke-sql-search.sh against live stack; refine to 6 assertions</name>
  <read_first>
    - scripts/smoke-sql-search.sh (Plan 01 skeleton — confirm it asserts /config, SSRM shape, RECON-XYZ-42)
    - .planning/phases/05-config-driven-select/05-RESEARCH.md §Code Examples / Example 2 (smoke script skeleton)
    - The Plan 05 controller paths: `/rectrace/api/v4/sql-search/config` and `/rectrace/api/v4/sql-search/ssrm/{tabKey}` — confirm BASE_URL handling matches.
  </read_first>
  <action>
    Pre-checks (executor runs):
      1. Confirm Docker stack is up. `cd ../rectrace-local-dev && docker compose ps`. If any service is down, `docker compose up -d`; wait for Oracle healthy (poll `docker exec rectrace-oracle sqlplus -L rectrace_readonly/rectrace_readonly_pwd@//localhost:1521/FREEPDB1 <<< "SELECT 1 FROM dual;"` until exit 0).
      2. Confirm seed data: `docker exec rectrace-oracle sqlplus -L rectrace_readonly/rectrace_readonly_pwd@//localhost:1521/FREEPDB1 <<< "SELECT COUNT(*) FROM rectrace.rectrace_core WHERE recon = 'RECON-XYZ-42';"` returns count >= 1.
      3. Start backend: `cd backend/rectrace && mvn spring-boot:run -Dspring-boot.run.profiles=local &`. Poll `curl -fsS http://localhost:6088/rectrace/actuator/health` until 200 (max 60s).

    Run `scripts/smoke-sql-search.sh`. Capture full stdout/stderr into a transient file for SUMMARY.md.

    If the script exits 0 and prints "OK: SQL search smoke green" — proceed to extending it; capture initial output verbatim.

    If the script fails:
      - Diagnose which step failed (/config shape, /ssrm shape, or RECON-XYZ-42 presence). Read the failed curl response.
      - Grep robustness: if Jackson serializes with whitespace (`"key": "reconSummary"`) and the grep was `"key":"reconSummary"`, fix the grep to be whitespace-tolerant OR pipe through `python3 -c "import json,sys; d=json.load(sys.stdin); assert any(t['key']=='reconSummary' for t in d['tabs'])"`. Prefer the python structured assertion for robustness.
      - If shape OK but `RECON-XYZ-42` missing — confirm seed has the row; if it does, inspect the controller's DEBUG log line for the wrapped SQL (Plan 05). Most likely root cause is column-name case or pagination math. DO NOT loosen validator/controller logic; fix only the smoke script or the configured query JSON (must still satisfy SQL-02/SQL-05 — re-run boot after any JSON edit).

    Extend the smoke script with 3 additional assertions (idempotent edits — do not duplicate existing ones):
      - Assertion 4: rows[0] keys all lowercase. Add a step that pipes the SSRM response through:
        `python3 -c "import json,sys; d=json.load(sys.stdin); assert d.get('rows') and all(k==k.lower() for k in d['rows'][0].keys()), 'FAIL: uppercase keys present in rows[0]'" || { echo "FAIL: row keys not lowercase"; exit 1; }`
      - Assertion 5: Negative — `curl -s -o /tmp/sql-smoke-resp.json -w '%{http_code}' -X POST "${BASE_URL}/api/v4/sql-search/ssrm/nope-not-a-tab" -H 'Content-Type: application/json' -d '{"startRow":0,"endRow":10,"sortModel":[],"filterModel":{}}'` should print `400` AND `/tmp/sql-smoke-resp.json` should grep-match `"error_type":"UNKNOWN_TAB"`.
      - Assertion 6: Negative — POST to reconSummary with `sortModel:[{"colId":"DROP TABLE x","sort":"asc"}]` returns 400 + grep-matches `"error_type":"INVALID_REQUEST"`.

    Each new assertion emits its own `FAIL: ...` message on failure with `exit 1`. The final `echo "OK: SQL search smoke green"` runs only after all 6 assertions pass.

    Re-run the smoke against the live stack. Capture final output.

    Stop the backend: `pkill -f rectrace || true`. Confirm port 6088 free.
  </action>
  <verify>
    <automated>bash /Users/aarun/Workspace/Projects/autosys-job-explorer/scripts/smoke-sql-search.sh && grep -c "FAIL:" /Users/aarun/Workspace/Projects/autosys-job-explorer/scripts/smoke-sql-search.sh | awk '{if ($1 >= 5) print "OK FAIL count " $1; else { print "FAIL: expected >=5 FAIL messages, got " $1; exit 1 }}'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/smoke-sql-search.sh` exits 0 when run against a live local stack with backend + Oracle + ES up. Capture stdout in SUMMARY.md.
    - All 6 distinct assertions present in the script:
      1. /config exposes reconSummary tab.
      2. /ssrm/reconSummary response has `rows` and `lastRow` keys.
      3. Response body contains `RECON-XYZ-42`.
      4. `rows[0]` keys all lowercase.
      5. /ssrm/nope-not-a-tab returns 400 + UNKNOWN_TAB.
      6. /ssrm/reconSummary with malicious colId returns 400 + INVALID_REQUEST.
    - `grep -c "FAIL:" scripts/smoke-sql-search.sh` returns >= 5.
    - `bash -n scripts/smoke-sql-search.sh` exits 0.
    - `test -x scripts/smoke-sql-search.sh` true (still executable).
    - If any production code (`backend/rectrace/src/main/`) is touched during diagnosis, rationale recorded in SUMMARY.md AND `mvn -q test` remains green. Strong preference: do NOT touch production code; fix only the smoke or the configured query JSON if needed.
    - `mvn -q test` (full backend/rectrace suite) green at end of plan.
  </acceptance_criteria>
  <done>
    Smoke green end-to-end against live local stack with 6 assertions. Backend cleanly shut down. Full mvn test suite green.
  </done>
</task>

<task type="auto">
  <name>Task 2: ANGULAR-WIRING.md + parity-matrix.md update</name>
  <read_first>
    - frontend/rectrace/src/app/search-v5/components/search-v5-grid/search-v5-grid.component.ts (locate `createSSRMDatasource()` — the function building the SSRM datasource against `/api/v4/search/...`. Identify the URL constant or env-var reference.)
    - frontend/rectrace/src/app/services/search-v5.service.ts (if a service holds the base URL constants — locate the constants for `/api/v4/search/config` and `/api/v4/search/ssrm/{cat}`)
    - .planning/parity-matrix.md (existing structure — headers + existing rows to mirror)
    - .planning/phases/05-config-driven-select/05-RESEARCH.md §System Architecture Diagram (Angular consumption arrow)
  </read_first>
  <action>
    Create `.planning/phases/05-config-driven-select/ANGULAR-WIRING.md`. Sections:
      - H1 "Angular V5 Grid → SQL Search Endpoint (Phase 5 evidence, not wired)".
      - H2 "Why this doc exists": one paragraph citing D-5.18 (no frontend changes in Phase 5; Angular consumption documented but deferred).
      - H2 "The minimal change": cite the exact file path(s) discovered in read_first AND the exact line numbers (use `grep -n` results) holding the URL constants. Document as prose (no diff blocks needed — describe in plain text): "In `<file>:<line>`, the constant `/api/v4/search/config` becomes `/api/v4/sql-search/config`. In `<file>:<line>`, the SSRM datasource URL constant `/api/v4/search/ssrm/${category}` becomes `/api/v4/sql-search/ssrm/${tabKey}`." Note: `SSRMRequestV4`'s `initialFilter` field is harmless to send (server ignores for SQL tabs) — no Angular DTO change needed.
      - H2 "Verification recipe": three numbered steps — (1) apply the constant swap; (2) `npm start` in frontend/rectrace; (3) navigate to the relevant route and confirm grid loads reconSummary rows including `RECON-XYZ-42` in the recon column.
      - H2 "Why we did not wire it now": one paragraph citing strangler-fig strategy + D-5.18 + existence of `scripts/smoke-sql-search.sh` as backend evidence sufficient for SQL-07.

    Update `.planning/parity-matrix.md`. Read existing structure first; the matrix tracks Angular cellRenderers × categories × behaviors with target verbs (port / drop / replace-with-recviz). Add a new row (or new section if SQL tabs don't fit cleanly into the renderer-per-category schema):
      - Tab name: "reconSummary (Phase 5 SQL example)"
      - Source: "configured SELECT via sql-search-config-v4.json"
      - Renderers: "plain text (no custom cellRenderer)"
      - Target verb: `port` (future React plan mirrors Angular consumption against `/api/v4/sql-search/...`)
      - Status note: "Backend ready; frontend deferred per D-5.18; Angular wiring documented in `.planning/phases/05-config-driven-select/ANGULAR-WIRING.md`."

    If parity-matrix.md has a footer "Update log" / "Changelog" section, add a line: `- 2026-MM-DD: Phase 5 — reconSummary SQL tab added; backend ready, frontend deferred.`

    No Angular code is edited. No frontend/rectrace/* file modified.
  </action>
  <verify>
    <automated>test -f /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/05-config-driven-select/ANGULAR-WIRING.md && grep -q "reconSummary" /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/05-config-driven-select/ANGULAR-WIRING.md && grep -q "/api/v4/sql-search/" /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/phases/05-config-driven-select/ANGULAR-WIRING.md && grep -q "reconSummary" /Users/aarun/Workspace/Projects/autosys-job-explorer/.planning/parity-matrix.md && git diff --name-only frontend/rectrace/ | wc -l | awk '{if ($1 == 0) print "OK no angular touched"; else { print "FAIL: angular files modified: " $1; exit 1 }}'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-config-driven-select/ANGULAR-WIRING.md` exists.
    - File contains the literal `reconSummary` and `/api/v4/sql-search/`.
    - File cites at least one specific Angular file path + line number for the URL swap point.
    - `.planning/parity-matrix.md` contains `reconSummary` (new row landed).
    - `git diff --name-only frontend/rectrace/` returns empty (no Angular code touched).
    - `git diff --name-only backend/rectrace/src/main/` returns empty (no production backend touched in this plan).
  </acceptance_criteria>
  <done>
    Angular wiring documentation in place; parity-matrix updated; zero frontend or production-backend code modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local-dev smoke probe → live backend | Asserts behaviors against the real running stack; no network egress. |
| Documentation file → human reader | Docs do not modify runtime; risk is misleading instructions, not exploitation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-19 | T (Tampering) | Smoke loosened to mask a failing assertion | mitigate | Acceptance criteria mandates 6 distinct FAIL messages and asserts the negative probes return 400 with specific error_type strings — cannot be loosened without grep gates failing. |
| T-05-20 | I (Information Disclosure) | ANGULAR-WIRING.md leaks unintended endpoint details | accept | Doc lives in the planning folder; describes only public endpoint contract already shipped in Plan 05. |
</threat_model>

<verification>
- `bash scripts/smoke-sql-search.sh` exits 0 against live stack.
- `grep -c "FAIL:" scripts/smoke-sql-search.sh` returns >= 5.
- `mvn -q test` from backend/rectrace green (no production regression).
- `git diff --name-only frontend/rectrace/` returns empty.
- Both documentation files (ANGULAR-WIRING.md, parity-matrix.md) contain `reconSummary`.
</verification>

<success_criteria>
- 6-assertion smoke green end-to-end against live stack.
- ANGULAR-WIRING.md exists with concrete file:line citation for the future URL swap.
- parity-matrix.md updated with the reconSummary SQL tab row.
- Zero frontend code touched; zero production backend code touched.
- Full `mvn -q test` green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-config-driven-select/05-06-SUMMARY.md` capturing: full smoke output (all 6 assertions green), the exact Angular file:line citations recorded in ANGULAR-WIRING.md, the parity-matrix row diff, and a phase-close note "Phase 5 deliverables complete; SQL-01..SQL-07 all green; ready for /gsd-verify-work 5."
</output>
