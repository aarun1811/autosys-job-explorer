---
phase: 07-observability-sweep
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - backend/rectrace/src/main/resources/logback-spring.xml
  - backend/rectrace/src/main/resources/application.properties
  - backend/rectrace/src/main/resources/application-prod.properties
  - backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/filter/UserIdMdcFilter.java
  - backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/filter/AccessLogFilter.java
  - backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/ActuatorSecurityConfig.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/AccessLogJsonShapeTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/LogbackProdProfileTest.java
  - backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/ActuatorExposureTest.java
autonomous: true
requirements: [OBS-01, OBS-03, OBS-07]
tags: [observability, logback, splunk-hec, actuator-lockdown, mdc, json-logs]

must_haves:
  truths:
    - "backend/rectrace emits one JSON log line per HTTP request containing traceId, userId, path, method, status, durationMs MDC keys."
    - "In `!prod` profile, logs are human-readable console pattern; in `prod` profile, LogstashTcpSocketAppender wraps in AsyncAppender writing to ${splunk.hec.host}:${splunk.hec.port}."
    - "`/actuator/` exposes EXACTLY {health, info, prometheus, loggers, metrics} — no env, no heapdump, no shutdown, no beans, no configprops, no threaddump."
    - "`management.endpoint.health.show-details=when-authorized` — anonymous callers see `{\"status\":\"UP\"}` only."
    - "UserIdMdcFilter validates `x-citiportal-loginid` against `[A-Za-z0-9._-]{1,64}` (log-injection mitigation) and clears MDC in finally (Pitfall P-3)."
    - "AccessLogFilter runs at LOWEST_PRECEDENCE, captures durationMs, emits ONE info-level log line per request to logger named 'access', clears the four MDC keys in finally."
    - "ActuatorSecurityConfig stub permits anonymous access to /actuator/** (Phase 7 ships exposure lockdown only; Phase 9 hardens auth) but is structured so a Phase 9 commit drops in `.authenticated()` with no other change."
    - "Wave-0 tests AccessLogJsonShapeTest, LogbackProdProfileTest, ActuatorExposureTest are NOT @Disabled and PASS."
  artifacts:
    - path: backend/rectrace/src/main/resources/logback-spring.xml
      provides: "profile-aware JSON encoder + Splunk HEC TCP appender"
      contains: "LogstashEncoder"
    - path: backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/filter/UserIdMdcFilter.java
      provides: "MDC userId enricher with regex validation"
      contains: "x-citiportal-loginid"
    - path: backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/filter/AccessLogFilter.java
      provides: "OncePerRequestFilter emitting one JSON line per request"
      contains: "OncePerRequestFilter"
    - path: backend/rectrace/src/main/resources/application.properties
      provides: "actuator exposure allow-list + observability properties"
      contains: "management.endpoints.web.exposure.include=health,info,prometheus,loggers,metrics"
    - path: backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/ActuatorSecurityConfig.java
      provides: "Phase 7 stub SecurityFilterChain for /actuator/** (Phase 9 hardens)"
      contains: "SecurityFilterChain"
  key_links:
    - from: backend/rectrace/src/main/resources/logback-spring.xml
      to: ${splunk.hec.host}:${splunk.hec.port}
      via: "LogstashTcpSocketAppender inside <springProfile name=prod>"
      pattern: "LogstashTcpSocketAppender"
    - from: backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/filter/AccessLogFilter.java
      to: SLF4J MDC keys (path/method/status/durationMs)
      via: "MDC.put before access.info('request'); MDC.remove in finally"
      pattern: "MDC\\.put"
    - from: backend/rectrace/src/main/resources/application.properties
      to: management.endpoint.health.show-details
      via: "explicit `when-authorized` value"
      pattern: "show-details=when-authorized"
---

<objective>
Land the structured-logging and actuator-lockdown surface for `backend/rectrace`: rewrite `logback-spring.xml` profile-aware (Console in `!prod`, LogstashEncoder + LogstashTcpSocketAppender wrapped by AsyncAppender in `prod`), add the `UserIdMdcFilter` + `AccessLogFilter` pair, lock the actuator exposure list to exactly the five permitted endpoints, set `show-details=when-authorized`, and stub `ActuatorSecurityConfig` so Phase 9 can drop in auth requirements with no structural change. Enable three Wave-0 tests from Plan 01.

Purpose: OBS-01 (JSON logs with the six MDC keys), OBS-03 (locked-down actuator exposure), OBS-07 (Splunk HEC forwarder wired profile-gated). This plan owns the HTTP/log infrastructure half of the phase; Plan 03 owns the health/AOP/propagation half. The two plans modify disjoint files and run in the same wave.

Output: Six new/edited files in main; three test files un-disabled.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/autosys-job-explorer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-observability-sweep/07-CONTEXT.md
@.planning/phases/07-observability-sweep/07-RESEARCH.md
@.planning/phases/07-observability-sweep/07-01-SUMMARY.md
@backend/rectrace/src/main/resources/logback-spring.xml
@backend/rectrace/src/main/resources/application.properties
@backend/rectrace/src/main/java/com/citi/gru/rectrace/config/CorrelationIdPropagationConfig.java

<interfaces>
<!-- Existing primitives the executor reuses, no need to re-explore: -->
<!-- - Phase 2's CorrelationIdPropagationConfig already populates MDC.traceId from X-Correlation-Id or B3/W3C. DO NOT TOUCH IT. -->
<!-- - Existing logback-spring.xml (10-line ConsoleAppender) — Plan 02 REPLACES this file wholesale. -->
<!-- - application.properties already has server.port=6088, spring.servlet.context-path=/rectrace, and spring.lifecycle.timeout-per-shutdown-phase=60s. -->
<!-- - SecurityFilterChain pattern: a SecurityConfig.java already exists in the codebase (Phase 1 BOOT-04). Add a SECOND chain via @Order on ActuatorSecurityConfig with higher precedence than the main chain. -->
<!-- - OncePerRequestFilter is org.springframework.web.filter.OncePerRequestFilter. -->
<!-- - ListAppender for tests: ch.qos.logback.core.read.ListAppender<ch.qos.logback.classic.spi.ILoggingEvent>. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite logback-spring.xml profile-aware + add filters + lock actuator properties</name>
  <files>backend/rectrace/src/main/resources/logback-spring.xml, backend/rectrace/src/main/resources/application.properties, backend/rectrace/src/main/resources/application-prod.properties, backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/filter/UserIdMdcFilter.java, backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/filter/AccessLogFilter.java</files>
  <behavior>
- A `GET /api/v4/search/config` with no `x-citiportal-loginid` header logs a single info-level event on the `access` logger; its `getMDCPropertyMap()` contains keys `traceId` (32-hex, populated by Phase 2's propagator), `path=/api/v4/search/config`, `method=GET`, `status=200`, `durationMs=<digit>+`. `userId` is empty (filter only populates when header present and valid).
- Same call WITH header `x-citiportal-loginid: arun.tech_01` populates MDC.userId with the loginId; with header `arun\ninjected: log line` populates MDC.userId with EMPTY (regex rejects); with header `aaaaaa...80-char` populates EMPTY (length > 64 rejected).
- After the chain completes, MDC keys path/method/status/durationMs/userId are ALL cleared (Pitfall P-3). Next request on the same thread sees no leakage.
- In `!prod` profile, `LoggerContext` root logger has exactly one appender named `CONSOLE` of type `ConsoleAppender`; in `prod` profile, root logger has `STDOUT_JSON` (ConsoleAppender + LogstashEncoder) AND `SPLUNK_ASYNC` (AsyncAppender wrapping LogstashTcpSocketAppender).
- `/actuator/` HAL response lists exactly: health, info, prometheus, loggers, metrics; never env/heapdump/shutdown/beans/configprops/threaddump/scheduledtasks.
  </behavior>
  <action>
**File 1 — `backend/rectrace/src/main/resources/logback-spring.xml` (REPLACE):**
Replace the existing 10-line ConsoleAppender file with the full profile-aware template from 07-RESEARCH.md §3.1. Structure:
- `<include resource="org/springframework/boot/logging/logback/defaults.xml"/>`
- `<springProfile name="!prod">` → `CONSOLE` ConsoleAppender with pattern `%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [traceId=%X{traceId:-} userId=%X{userId:-}] %logger{36} - %msg%n`; root level INFO refs CONSOLE.
- `<springProfile name="prod">`:
  - `<springProperty>` for `appName` (source `spring.application.name`), `splunkHost` (default `splunk.citi.intra.example`), `splunkPort` (default `9997`).
  - `STDOUT_JSON` ConsoleAppender + `LogstashEncoder` with `<includeMdc>true</includeMdc>`, `<customFields>{"application":"${appName}","environment":"prod"}</customFields>`, `<timestampPattern>yyyy-MM-dd'T'HH:mm:ss.SSS'Z'</timestampPattern>`, `<timeZone>UTC</timeZone>`.
  - `SPLUNK_HEC` LogstashTcpSocketAppender with `<destination>${splunkHost}:${splunkPort}</destination>`, `<reconnectionDelay>30 seconds</reconnectionDelay>`, `<writeTimeout>10 seconds</writeTimeout>`, `<keepAliveDuration>5 minutes</keepAliveDuration>`, `<ringBufferSize>8192</ringBufferSize>`, identical LogstashEncoder.
  - `SPLUNK_ASYNC` AsyncAppender (ch.qos.logback.classic.AsyncAppender) wrapping `SPLUNK_HEC` with `<queueSize>2048</queueSize><discardingThreshold>0</discardingThreshold><neverBlock>true</neverBlock>` (Pitfall P-2).
  - `<root level="INFO">` refs both `STDOUT_JSON` AND `SPLUNK_ASYNC` (operator-recovery surface — STDOUT always emits even if Splunk fails).

Pitfall P-1: filename MUST stay `logback-spring.xml` (not `logback.xml`) — Spring profile blocks no-op silently otherwise.

**File 2 — `backend/rectrace/src/main/resources/application.properties` (EDIT, append):**
Append a `# OBS-03 / OBS-08 — actuator lockdown + observability` section setting:
```
management.endpoints.web.exposure.include=health,info,prometheus,loggers,metrics
management.endpoints.web.exposure.exclude=env,heapdump,shutdown,beans,configprops,threaddump,scheduledtasks
management.endpoint.health.show-details=when-authorized
management.endpoint.health.show-components=when-authorized
management.observations.annotations.enabled=true
management.endpoint.health.probes.enabled=true
# Phase 7 — slow-query threshold (Plan 03 wires the aspect)
observability.slow-query-threshold-ms=500
# Phase 7 — loader run-age health group (Pitfall P-11 / D-7.12 — separate group keeps readiness clean)
management.endpoint.health.group.loader.include=loaderRunAge
```
Do NOT edit `spring.lifecycle.timeout-per-shutdown-phase` (already set to 60s per Phase 6).

**File 3 — `backend/rectrace/src/main/resources/application-prod.properties` (EDIT, append):**
Append the Splunk HEC placeholders flagged D-7.14:
```
# OBS-07 — Splunk HEC endpoint. <TO_BE_FILLED> at deploy time by Citi ops.
splunk.hec.host=splunk.citi.intra.example
splunk.hec.port=9997
splunk.hec.token=<TO_BE_FILLED>
```
The token is unused by `LogstashTcpSocketAppender` (raw-TCP HEC is auth-by-source-IP); included as a documented placeholder for the day Splunk switches to HTTPS-HEC.

**File 4 — `UserIdMdcFilter.java` (NEW):**
`package com.citi.gru.rectrace.observability.filter;`
Class is `@Component` extending `OncePerRequestFilter` with `@Order(Ordered.HIGHEST_PRECEDENCE + 100)` (runs AFTER Phase 2's correlation-id filter at HIGHEST_PRECEDENCE so MDC.traceId is already set, BEFORE AccessLogFilter at LOWEST_PRECEDENCE).
- Read header `x-citiportal-loginid` (case-insensitive — `req.getHeader()` is case-insensitive per Servlet spec).
- Validate against `Pattern.compile("^[A-Za-z0-9._-]{1,64}$")` — store as `static final Pattern LOGIN_ID = ...`.
- If valid, `MDC.put("userId", value);` else SKIP (do not put empty, do not log warning — silent reject preserves anonymous probes).
- `try { chain.doFilter(req, res); } finally { MDC.remove("userId"); }`.

**File 5 — `AccessLogFilter.java` (NEW):**
`package com.citi.gru.rectrace.observability.filter;`
Class is `@Component` extending `OncePerRequestFilter` with `@Order(Ordered.LOWEST_PRECEDENCE)`.
- Static `Logger access = LoggerFactory.getLogger("access");`
- `doFilterInternal`: `long startNs = System.nanoTime(); try { chain.doFilter(req, res); } finally { long durationMs = (System.nanoTime() - startNs) / 1_000_000L; MDC.put("path", req.getRequestURI()); MDC.put("method", req.getMethod()); MDC.put("status", Integer.toString(res.getStatus())); MDC.put("durationMs", Long.toString(durationMs)); try { access.info("request"); } finally { MDC.remove("path"); MDC.remove("method"); MDC.remove("status"); MDC.remove("durationMs"); } }`
- Javadoc per Pitfall P-8: "traceId MDC value is sourced from `CorrelationIdPropagationConfig` (Phase 2); this filter NEVER overwrites it."

Per `<scope_reduction_prohibition>`: deliver ALL FIVE files in this task. The regex MUST be `[A-Za-z0-9._-]{1,64}` exactly (the period is allowed because Citi loginIds contain dots; underscore is allowed; the dash is intentionally last in the char class). DO NOT relax to `.+`.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/autosys-job-explorer && mvn -pl backend/rectrace -DskipTests compile && grep -c "management.endpoints.web.exposure.include=health,info,prometheus,loggers,metrics" backend/rectrace/src/main/resources/application.properties | grep -qv '^0$' && grep -c "LogstashEncoder\|LogstashTcpSocketAppender" backend/rectrace/src/main/resources/logback-spring.xml | grep -qE "^[2-9]" && grep -q "OncePerRequestFilter" backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/filter/UserIdMdcFilter.java && grep -q "OncePerRequestFilter" backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/filter/AccessLogFilter.java</automated>
  </verify>
  <done>Backend compiles; new properties present; logback-spring.xml has BOTH LogstashEncoder and LogstashTcpSocketAppender references; both filter classes extend OncePerRequestFilter and use try/finally MDC cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ActuatorSecurityConfig stub + enable three Wave-0 tests</name>
  <files>backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/ActuatorSecurityConfig.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/AccessLogJsonShapeTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/LogbackProdProfileTest.java, backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/ActuatorExposureTest.java</files>
  <behavior>
- `ActuatorSecurityConfig` declares a `SecurityFilterChain` bean with `@Order(Ordered.HIGHEST_PRECEDENCE)` (higher precedence than the main `SecurityConfig` from Phase 1) matching `EndpointRequest.toAnyEndpoint()`; permits all (Phase 7 stub); Phase 9 will swap `.permitAll()` for `.authenticated()` on `/actuator/loggers`+`/actuator/metrics` without changing the bean shape.
- Removing `@Disabled` from `AccessLogJsonShapeTest`, `LogbackProdProfileTest`, `ActuatorExposureTest` causes them to PASS against the changes from Task 1.
  </behavior>
  <action>
**File 1 — `ActuatorSecurityConfig.java` (NEW):**
`package com.citi.gru.rectrace.observability;`

```
@Configuration
@EnableWebSecurity
public class ActuatorSecurityConfig {
    @Bean
    @Order(Ordered.HIGHEST_PRECEDENCE)
    public SecurityFilterChain actuatorChain(HttpSecurity http) throws Exception {
        // Phase 7 ships EXPOSURE lockdown only (D-7.3). Phase 9 hardens AUTH on
        // /actuator/loggers and /actuator/metrics by swapping permitAll() for
        // authenticated() and adding an authentication provider.
        http
            .securityMatcher(EndpointRequest.toAnyEndpoint())
            .authorizeHttpRequests(a -> a.anyRequest().permitAll())
            .csrf(c -> c.disable());
        return http.build();
    }
}
```
- Imports: `org.springframework.boot.actuate.autoconfigure.security.servlet.EndpointRequest`, `org.springframework.security.config.annotation.web.builders.HttpSecurity`, `org.springframework.security.web.SecurityFilterChain`, `org.springframework.core.Ordered`, `org.springframework.context.annotation.{Bean,Configuration}`, `org.springframework.security.config.annotation.web.configuration.EnableWebSecurity`, `org.springframework.core.annotation.Order`.
- The `@Order(HIGHEST_PRECEDENCE)` ensures this chain matches actuator URLs BEFORE the Phase-1 main `SecurityConfig` chain.
- Javadoc explicitly cites D-7.3 and notes the Phase-9 hand-off contract.

**Files 2-4 — Un-disable three Wave-0 tests:**
For each of `AccessLogJsonShapeTest.java`, `LogbackProdProfileTest.java`, `ActuatorExposureTest.java` (under `backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/`):
1. Remove the class-level `@Disabled("Wave 0 scaffold — enabled by Plan 07-02")` annotation.
2. Remove the matching `import org.junit.jupiter.api.Disabled;` line.
3. Do NOT modify test bodies — Plan 01 wrote them complete.

If any test fails after un-disabling, the fix lives in Task 1's main-source artifacts (not in the test). The aim is to remove ONE annotation per file and have green tests.

Per `<scope_reduction_prohibition>`: all three tests must pass at end of Task 2. If LogbackProdProfileTest fails because the `prod` profile's Splunk hostname/port resolution differs, fix it via Task 1's logback-spring.xml template parameters — NOT by relaxing the test assertion.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/autosys-job-explorer && mvn -pl backend/rectrace test -Dtest='AccessLogJsonShapeTest,LogbackProdProfileTest,ActuatorExposureTest' 2>&1 | tee /tmp/p7-02-t2.log | grep -E "Tests run: ([3-9]|[1-9][0-9]+).*Failures: 0.*Errors: 0" && ! grep -q "@Disabled" backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/AccessLogJsonShapeTest.java && ! grep -q "@Disabled" backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/LogbackProdProfileTest.java && ! grep -q "@Disabled" backend/rectrace/src/test/java/com/citi/gru/rectrace/observability/ActuatorExposureTest.java</automated>
  </verify>
  <done>The three named tests run (no longer @Disabled), report Failures:0 Errors:0; ActuatorSecurityConfig compiles with @Order(HIGHEST_PRECEDENCE); main SecurityConfig chain still works (existing Phase 1/2 endpoints remain reachable — verified by mvn package green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Spring filter chain | x-citiportal-loginid header is untrusted user input; flows into MDC and into JSON logs |
| client → /actuator/** | actuator endpoints are partially anonymous (health/info/prometheus); other endpoints (loggers, metrics) are anonymous-permit in Phase 7 stub |
| application → Splunk HEC TCP socket | Outbound TCP carries JSON logs including loginIds (PII-equivalent for internal app) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering / Injection | UserIdMdcFilter — header value flows into log MDC | mitigate | Validate against `^[A-Za-z0-9._-]{1,64}$`; reject CR/LF and special chars; D-7.11 per CONTEXT |
| T-07-06 | Information Disclosure | /actuator/env, /actuator/heapdump leaking secrets/heap | mitigate | Explicit `exposure.exclude` list AND `exposure.include` allow-list per D-7.3 |
| T-07-07 | Information Disclosure | /actuator/health full details exposed to anonymous callers | mitigate | `management.endpoint.health.show-details=when-authorized` (D-7.3); anonymous sees only `{"status":"UP"}` |
| T-07-08 | Denial of Service | LogstashTcpSocketAppender back-pressuring app threads when Splunk slow | mitigate | AsyncAppender with `neverBlock=true` and `queueSize=2048` (Pitfall P-2); always also write to STDOUT_JSON as fallback |
| T-07-09 | Information Disclosure | MDC leak across thread reuse — wrong user's loginId tagged on next request | mitigate | Every MDC.put paired with MDC.remove in finally (Pitfall P-3); ActuatorHealthIntegrationTest exercises this implicitly via repeat requests |
| T-07-10 | Elevation of Privilege | Phase 7 stub ActuatorSecurityConfig permitAll() is permissive | accept (with handoff to Phase 9) | Documented in Javadoc + ROADMAP Phase 9 SEC scope; current behaviour matches Phase 1 baseline (no regression) |
| T-07-11 | Information Disclosure | Splunk HEC token committed in application-prod.properties | mitigate | Token is `<TO_BE_FILLED>` placeholder; deploy-time substitution by ops (D-7.14) |
</threat_model>

<verification>
- `mvn -pl backend/rectrace test -Dtest='AccessLogJsonShapeTest,LogbackProdProfileTest,ActuatorExposureTest'` → all three pass.
- `mvn -pl backend/rectrace -DskipTests package` → build succeeds.
- Manual smoke (per CLAUDE.md ops/rectrace-ops.sh start): `curl -s http://localhost:6088/rectrace/actuator | jq '._links | keys'` → `["health","info","loggers","metrics","prometheus","self"]`.
- `curl -s -H 'X-Correlation-Id: 1234567890abcdef1234567890abcdef' http://localhost:6088/rectrace/actuator/health` → response body is `{"status":"UP"}` (anonymous = no details).
- `grep -c "MDC.put\|MDC.remove" backend/rectrace/src/main/java/com/citi/gru/rectrace/observability/filter/AccessLogFilter.java | grep -v '^#' | grep -qE "^[8-9]|^[1-9][0-9]"` (4 puts + 4 removes minimum).
</verification>

<success_criteria>
- `logback-spring.xml` has `!prod` (Console) and `prod` (LogstashEncoder + Logstash TCP + AsyncAppender) branches; both visible in the file.
- `application.properties` exposes exactly five actuator endpoints; excludes six; `show-details=when-authorized`.
- `application-prod.properties` declares `splunk.hec.host`, `splunk.hec.port`, `splunk.hec.token` placeholders.
- `UserIdMdcFilter` regex is `^[A-Za-z0-9._-]{1,64}$` exact.
- `AccessLogFilter` runs at `LOWEST_PRECEDENCE`, captures durationMs, clears MDC in finally.
- `ActuatorSecurityConfig` declares one `@Order(HIGHEST_PRECEDENCE)` `SecurityFilterChain` bean matching `EndpointRequest.toAnyEndpoint()`.
- Three Wave-0 tests (`AccessLogJsonShapeTest`, `LogbackProdProfileTest`, `ActuatorExposureTest`) un-disabled and green.
- OBS-01, OBS-03, OBS-07 marked complete in phase summary.
</success_criteria>

<output>
After completion, create `.planning/phases/07-observability-sweep/07-02-SUMMARY.md` recording: the full diff of application.properties additions, a snapshot of the resolved logback-spring.xml, observed JSON log line sample (one entry from local smoke), and confirmation that the existing `/api/v4/search/*` endpoints still work (regression check).
</output>
