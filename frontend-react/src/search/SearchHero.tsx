// NOTE: minimal stub — Task 14 replaces this with the full centered hero
// (logo + animated placeholder + Search + Try + suggest dropdown + user chip).
export function SearchHero(): React.ReactElement {
  return <div data-testid="search-hero">Rectrace</div>
}
